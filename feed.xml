<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://www.wonbeomjang.kr/feed.xml" rel="self" type="application/atom+xml"/><link href="https://www.wonbeomjang.kr/" rel="alternate" type="text/html" hreflang="ko"/><updated>2026-07-07T13:23:17+00:00</updated><id>https://www.wonbeomjang.kr/feed.xml</id><title type="html">blank</title><subtitle>SK텔레콤 LLM Research Engineer 장원범입니다. LLM 학습, 평가, 데이터 설계에 대해 공부하고 공유합니다. </subtitle><entry><title type="html">Kubernetes 확장과 생태계 — Operator와 CNCF Projects</title><link href="https://www.wonbeomjang.kr/blog/2026/k8s-09-operator-cncf/" rel="alternate" type="text/html" title="Kubernetes 확장과 생태계 — Operator와 CNCF Projects"/><published>2026-07-07T00:00:00+00:00</published><updated>2026-07-07T00:00:00+00:00</updated><id>https://www.wonbeomjang.kr/blog/2026/k8s-09-operator-cncf</id><content type="html" xml:base="https://www.wonbeomjang.kr/blog/2026/k8s-09-operator-cncf/"><![CDATA[<blockquote> <p>이 글은 <strong>K8s 입문 시리즈</strong>의 아홉 번째 글이다.</p> <ul> <li><a href="/blog/2026/k8s-01-history/">01: Kubernetes의 탄생 — Google Borg에서 CNCF까지</a></li> <li><a href="/blog/2026/k8s-02-local-setup/">02: 내 노트북에 클러스터 만들기 — kind와 kubectl</a></li> <li><a href="/blog/2026/k8s-03-architecture/">03: Kubernetes 아키텍처 — Control Plane과 Node</a></li> <li><a href="/blog/2026/k8s-04-pod/">04: Pod의 모든 것 — 생성부터 스케줄링까지</a></li> <li><a href="/blog/2026/k8s-05-workloads/">05: 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</a></li> <li><a href="/blog/2026/k8s-06-networking/">06: 네트워킹 — Service와 Ingress</a></li> <li><a href="/blog/2026/k8s-07-storage-config/">07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</a></li> <li><a href="/blog/2026/k8s-08-rbac/">08: 권한 관리 — ServiceAccount와 RBAC</a></li> <li><strong>09: 확장과 생태계 — Operator와 CNCF Projects</strong> ← 현재 글</li> </ul> <p>이 시리즈의 커리큘럼은 SK Devocean의 <a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua) 글의 학습 로드맵을 바탕으로 구성했다.</p> </blockquote> <h1 id="1-들어가며--우리-회사만의-리소스를-만들-수-있다면">1. 들어가며 — “우리 회사만의 리소스”를 만들 수 있다면?</h1> <p>지금까지 시리즈에서 다룬 것은 전부 Kubernetes에 <strong>내장된</strong> 리소스였다. Pod, Deployment, Service, PVC, Role… 전부 <code class="language-plaintext highlighter-rouge">kubectl api-resources</code>를 치면 나오는, 설치하자마자 주어지는 어휘들이다.</p> <p>그런데 실무에서는 이런 요구가 금방 생긴다.</p> <ul> <li>“PostgreSQL 클러스터를 배포하고, <strong>백업·장애 조치·버전 업그레이드까지 자동으로</strong> 해주면 좋겠는데.”</li> <li>“인증서가 만료되기 전에 <strong>알아서 갱신</strong>되면 좋겠는데.”</li> <li>“우리 팀의 배포 단위는 ‘ML 모델 서빙’인데, 이걸 <code class="language-plaintext highlighter-rouge">kubectl get modelserving</code>처럼 다루고 싶은데.”</li> </ul> <p>Deployment와 Service만으로 이걸 조립하려면 사람이 runbook(운영 절차서)을 들고 매번 수동으로 개입해야 한다. Kubernetes 설계자들은 이 문제를 예견하고, Kubernetes를 <strong>완성된 제품이 아니라 확장 가능한 플랫폼</strong>으로 만들었다.</p> <p>비유하자면 이렇다. 지금까지 우리는 스마트폰의 <strong>기본 앱</strong>(전화, 카메라, 메시지)만 쓴 셈이다. 이번 편에서는 <strong>앱스토어</strong>의 존재를 배운다. 누구나 앱(새 리소스 타입 + 그것을 움직이는 컨트롤러)을 만들어 올릴 수 있고, 잘 만든 앱은 생태계(CNCF)의 검증을 거쳐 사실상 표준이 된다.</p> <hr/> <h1 id="2-컨트롤러-패턴-복습--모든-것은-reconcile-루프다">2. 컨트롤러 패턴 복습 — 모든 것은 reconcile 루프다</h1> <p>확장 얘기를 하기 전에, <a href="/blog/2026/k8s-03-architecture/">03편</a>과 <a href="/blog/2026/k8s-05-workloads/">05편</a>에서 본 Kubernetes의 핵심 동작 원리를 복습하자.</p> <p>Kubernetes의 모든 자동화는 하나의 패턴으로 요약된다.</p> <blockquote> <p><strong>원하는 상태(desired state)를 선언하면, 컨트롤러가 현재 상태(current state)를 계속 관찰하면서 둘의 차이를 메운다.</strong> 이 반복을 <strong>reconcile 루프</strong>(조정 루프)라고 부른다.</p> </blockquote> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-09-operator-cncf/reconcile-loop-480.webp 480w,/assets/post/image/k8s-09-operator-cncf/reconcile-loop-800.webp 800w,/assets/post/image/k8s-09-operator-cncf/reconcile-loop-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-09-operator-cncf/reconcile-loop.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="reconcile 루프 — 사용자가 원하는 상태를 선언하면 kube-apiserver가 etcd에 저장하고, 컨트롤러가 현재 상태를 관찰하며 원하는 상태와 비교해 같으면 다시 관찰로, 다르면 차이를 메우는 조치 후 다시 관찰로 돌아가는 순환 구조" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>사용자는 <strong>방법(how)이 아니라 결과(what)</strong> 를 선언한다. “replicas: 3”이라고 쓰지, “Pod를 하나씩 띄워라”라고 쓰지 않는다.</li> <li>컨트롤러는 <strong>한 번 실행하고 끝나는 스크립트가 아니라, 무한히 도는 루프</strong>다. Pod가 죽으면 루프의 다음 바퀴에서 차이가 감지되고 자동 복구된다.</li> <li>이 구조 덕분에 장애 복구, 스케일링, 롤링 업데이트가 전부 “차이 메우기”라는 같은 원리로 동작한다.</li> </ul> <p>일상 비유로는 <strong>보일러의 온도 조절기</strong>가 정확하다. “24도로 맞춰줘”(desired state)라고 설정하면, 온도 조절기는 현재 온도(current state)를 계속 재면서 낮으면 보일러를 켜고 높으면 끈다. 사용자가 보일러 밸브를 직접 조작하지 않는다.</p> <p>여기서 이번 편의 핵심 질문이 나온다.</p> <blockquote> <p>Deployment 컨트롤러가 “replicas 차이”를 메우듯이, <strong>“PostgreSQL 백업이 안 됐다”거나 “인증서가 곧 만료된다” 같은 차이를 메우는 컨트롤러를 우리가 직접 만들 수는 없을까?</strong></p> </blockquote> <p>만들 수 있다. 그러려면 두 가지가 필요하다. <strong>새 리소스 타입(명사)</strong> 과 <strong>그것을 움직이는 컨트롤러(동사)</strong> 다.</p> <hr/> <h1 id="3-crd--kubernetes-api에-새-명사를-등록한다">3. CRD — Kubernetes API에 새 명사를 등록한다</h1> <p><strong>CRD(Custom Resource Definition)</strong> 는 Kubernetes API에 새로운 리소스 타입을 등록하는 내장 메커니즘이다. CRD를 등록하면 그 순간부터 kube-apiserver가 새 타입을 이해하고, etcd에 저장해주고, <code class="language-plaintext highlighter-rouge">kubectl get/describe/delete</code>가 전부 동작한다.</p> <ul> <li><strong>CRD</strong>: “이제부터 <code class="language-plaintext highlighter-rouge">Certificate</code>라는 타입이 존재한다. 필드는 이렇다.” — <strong>타입의 정의</strong> (클래스 선언에 해당)</li> <li><strong>CR(Custom Resource)</strong>: “demo-cert라는 Certificate를 하나 만들어줘.” — <strong>그 타입의 인스턴스</strong> (객체 생성에 해당)</li> </ul> <p>왜 이런 방식일까? 설계 의도가 중요하다.</p> <ol> <li><strong>API 서버를 고치지 않고 확장한다.</strong> 새 리소스가 필요할 때마다 Kubernetes 소스 코드를 수정해서 재컴파일해야 한다면 생태계는 만들어질 수 없다. CRD는 실행 중인 클러스터에 YAML 한 장으로 타입을 추가한다.</li> <li><strong>기존 도구가 공짜로 따라온다.</strong> CR도 일반 리소스처럼 etcd에 저장되고 RBAC(<a href="/blog/2026/k8s-08-rbac/">08편</a>)의 통제를 받는다. <code class="language-plaintext highlighter-rouge">kubectl</code>, <code class="language-plaintext highlighter-rouge">kubectl auth can-i</code>, 감사 로그가 전부 그대로 동작한다.</li> <li><strong>선언형 모델이 유지된다.</strong> CR도 spec(원하는 상태)과 status(현재 상태)를 갖는다. 즉 reconcile 루프에 태울 수 있는 형태다.</li> </ol> <p>단, 여기서 중요한 함정이 있다. <strong>CRD만 등록하면 아무 일도 일어나지 않는다.</strong> kube-apiserver는 CR을 받아서 etcd에 저장할 뿐, 그 의미를 모른다. <code class="language-plaintext highlighter-rouge">Certificate</code>를 만들어도 실제 인증서를 발급해줄 주체가 없다. 명사만 있고 동사가 없는 상태다.</p> <p>그 동사 역할을 하는 것이 <strong>Custom Controller</strong> — 우리가 등록한 CR을 watch하면서 reconcile 루프를 도는 프로그램이다. 그리고 이 둘을 묶은 패턴에 이름이 붙어 있다.</p> <hr/> <h1 id="4-operator--crd--custom-controller--운영-지식">4. Operator = CRD + Custom Controller + 운영 지식</h1> <p><strong>Operator</strong>는 다음 세 가지의 결합이다.</p> <table> <thead> <tr> <th>구성 요소</th> <th>역할</th> <th>비유</th> </tr> </thead> <tbody> <tr> <td>CRD</td> <td>도메인 개념을 리소스 타입으로 정의</td> <td>새 어휘(명사) 등록</td> </tr> <tr> <td>Custom Controller</td> <td>CR을 watch하며 reconcile 루프 실행</td> <td>그 어휘를 행동으로 옮기는 일꾼</td> </tr> <tr> <td>운영 지식의 코드화</td> <td>백업, 장애 조치, 갱신 같은 절차를 코드로</td> <td>베테랑 운영자의 runbook을 자동화</td> </tr> </tbody> </table> <p>이름의 유래가 곧 정의다. 이 패턴은 2016년 CoreOS가 “Introducing Operators”라는 글에서 처음 제안했는데, <strong>사람 운영자(human operator)가 하던 일을 소프트웨어로 옮긴다</strong>는 뜻에서 Operator라고 이름 붙였다. 당시 CoreOS는 etcd Operator와 Prometheus Operator를 함께 공개했다. 예를 들어 “etcd 클러스터 멤버가 죽으면 새 멤버를 추가하고 데이터를 복제한다” 같은, 문서와 담당자의 머릿속에 있던 도메인 지식을 컨트롤러 코드에 넣은 것이다.</p> <p>현재는 Kubernetes 공식 문서에도 Operator pattern이 표준 확장 패턴으로 문서화되어 있다. 대표적인 Operator 몇 개만 보자.</p> <table> <thead> <tr> <th>Operator</th> <th>다루는 도메인</th> <th>등록하는 대표 CR</th> </tr> </thead> <tbody> <tr> <td>cert-manager</td> <td>TLS 인증서 수명주기</td> <td>Certificate, Issuer, ClusterIssuer</td> </tr> <tr> <td>Prometheus Operator</td> <td>모니터링 스택 운영</td> <td>Prometheus, ServiceMonitor, Alertmanager</td> </tr> <tr> <td>etcd Operator</td> <td>etcd 클러스터 운영</td> <td>EtcdCluster (최초의 Operator 중 하나)</td> </tr> </tbody> </table> <p>구조를 그림으로 보면 이렇다. cert-manager를 예로 들었다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-09-operator-cncf/operator-cert-manager-480.webp 480w,/assets/post/image/k8s-09-operator-cncf/operator-cert-manager-800.webp 800w,/assets/post/image/k8s-09-operator-cncf/operator-cert-manager-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-09-operator-cncf/operator-cert-manager.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="Operator 구조(cert-manager 예시) — Kubernetes API에 등록된 CRD와 CR(demo-cert)을 cert-manager 컨트롤러가 watch하고, 발급자(Issuer)에게 발급/갱신을 요청해 서명된 인증서를 받아 Secret(demo-cert-tls)에 저장하며 CR의 status를 갱신한다" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>사용자는 “이 도메인의 인증서가 있어야 한다”는 <strong>결과만 선언</strong>한다(CR).</li> <li>cert-manager 컨트롤러가 발급자에게 서명을 요청하고, 결과를 <a href="/blog/2026/k8s-07-storage-config/">07편</a>에서 배운 <strong>Secret</strong>으로 저장한다.</li> <li>만료가 다가오면? 사람이 달력을 볼 필요가 없다. reconcile 루프가 “곧 만료됨 = 원하는 상태와의 차이”로 감지하고 <strong>알아서 갱신</strong>한다. 이것이 운영 지식의 코드화다.</li> </ul> <p>핵심은 이것이다. <strong>Operator는 새로운 마법이 아니라, 2절의 reconcile 루프를 사용자 정의 도메인에 적용한 것뿐이다.</strong> Deployment 컨트롤러가 “Pod 개수”를 조정하듯, cert-manager는 “인증서의 유효 상태”를 조정한다.</p> <hr/> <h1 id="5-실습--cert-manager-설치하고-새-동사를-확인하기">5. 실습 — cert-manager 설치하고 새 동사를 확인하기</h1> <p>말로만 들으면 추상적이니, <a href="/blog/2026/k8s-02-local-setup/">02편</a>에서 만든 kind 클러스터에 cert-manager를 직접 설치해보자.</p> <h2 id="51-설치">5.1 설치</h2> <p>공식 문서의 기본 설치 방식은 정적 매니페스트 한 장을 apply하는 것이다. 버전은 계속 올라가므로, 실제 설치 전에 <a href="https://cert-manager.io/docs/installation/">공식 설치 문서</a>에서 최신 버전을 확인하고 URL의 버전 부분을 바꿔 쓰자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> https://github.com/cert-manager/cert-manager/releases/download/v1.20.3/cert-manager.yaml
</code></pre></div></div> <p>설치가 끝나면 cert-manager 네임스페이스에 컨트롤러 Pod들이 뜬다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-n</span> cert-manager
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                                       READY   STATUS    RESTARTS   AGE
cert-manager-5f6b885b4-xk2jp               1/1     Running   0          80s
cert-manager-cainjector-747d59b6c5-9wvxq   1/1     Running   0          80s
cert-manager-webhook-7d4f5d8f6b-tq8zl      1/1     Running   0          80s
</code></pre></div></div> <h2 id="52-클러스터에-새-명사가-생겼는지-확인">5.2 클러스터에 새 명사가 생겼는지 확인</h2> <p>설치 전에는 존재하지 않던 리소스 타입이 등록됐는지 CRD 목록으로 확인한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get crds | <span class="nb">grep </span>cert-manager
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>certificaterequests.cert-manager.io    2026-07-07T00:01:12Z
certificates.cert-manager.io           2026-07-07T00:01:12Z
challenges.acme.cert-manager.io        2026-07-07T00:01:13Z
clusterissuers.cert-manager.io         2026-07-07T00:01:13Z
issuers.cert-manager.io                2026-07-07T00:01:13Z
orders.acme.cert-manager.io            2026-07-07T00:01:13Z
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">Certificate</code>, <code class="language-plaintext highlighter-rouge">Issuer</code> 같은 타입이 Kubernetes API의 정식 시민이 됐다. 이제 <code class="language-plaintext highlighter-rouge">kubectl get certificate</code>가 에러 없이 동작한다.</p> <h2 id="53-certificate-리소스로-인증서-자동-발급">5.3 Certificate 리소스로 인증서 자동 발급</h2> <p>가장 간단한 발급자인 SelfSigned(자체 서명) Issuer로 인증서를 하나 발급해보자. 파일 하나에 Issuer와 Certificate를 함께 선언한다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># selfsigned-demo.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">cert-manager.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Issuer</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">selfsigned-issuer</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">selfSigned</span><span class="pi">:</span> <span class="pi">{}</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">cert-manager.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Certificate</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">demo-cert</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">secretName</span><span class="pi">:</span> <span class="s">demo-cert-tls</span>
  <span class="na">commonName</span><span class="pi">:</span> <span class="s">demo.example.local</span>
  <span class="na">dnsNames</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">demo.example.local</span>
  <span class="na">issuerRef</span><span class="pi">:</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">selfsigned-issuer</span>
    <span class="na">kind</span><span class="pi">:</span> <span class="s">Issuer</span>
</code></pre></div></div> <p>주목할 점: 우리는 <strong>openssl 명령을 한 줄도 쓰지 않았다.</strong> “이런 인증서가 존재해야 한다”고 선언했을 뿐이다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> selfsigned-demo.yaml
kubectl get certificate
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME        READY   SECRET          AGE
demo-cert   True    demo-cert-tls   6s
</code></pre></div></div> <p>READY가 True가 되는 몇 초 사이에 cert-manager 컨트롤러가 reconcile 루프를 돌며 키 생성 → 서명 요청 → Secret 저장을 대신 해줬다. 결과물을 확인하자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get secret demo-cert-tls
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME            TYPE                DATA   AGE
demo-cert-tls   kubernetes.io/tls   3      30s
</code></pre></div></div> <p><a href="/blog/2026/k8s-07-storage-config/">07편</a>에서 배운 <code class="language-plaintext highlighter-rouge">kubernetes.io/tls</code> 타입 Secret에 인증서(tls.crt)와 개인 키(tls.key)가 담겼다. 이 Secret은 <a href="/blog/2026/k8s-06-networking/">06편</a>의 Ingress에 그대로 연결해 HTTPS를 켤 수 있다. 시리즈에서 배운 조각들이 Operator를 통해 하나로 이어지는 순간이다.</p> <p>실습이 끝났으면 정리한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete <span class="nt">-f</span> selfsigned-demo.yaml
kubectl delete <span class="nt">-f</span> https://github.com/cert-manager/cert-manager/releases/download/v1.20.3/cert-manager.yaml
</code></pre></div></div> <hr/> <h1 id="6-패키지-관리--helm과-kustomize">6. 패키지 관리 — Helm과 Kustomize</h1> <p>시리즈 내내 우리는 YAML을 한 장씩 손으로 썼다. 배우는 단계에서는 그게 옳은 방법이지만, 실무로 가면 금방 한계가 온다.</p> <ul> <li>앱 하나를 배포하는 데 Deployment, Service, Ingress, ConfigMap, Secret… <strong>YAML이 대여섯 장</strong>씩 필요하다. 5절에서 apply한 cert-manager 매니페스트 한 장에도 수십 개의 리소스가 들어 있었다.</li> <li>같은 앱을 dev/staging/prod 세 환경에 배포하려면? 환경마다 replicas, 이미지 태그, 도메인, 리소스 제한이 다르다. 순진하게 풀면 <strong>디렉터리째 복사해서 세 벌</strong>을 만들게 된다.</li> <li>세 벌이 되는 순간부터 고통이 시작된다. 라벨 추가 같은 공통 수정 하나를 세 곳에 반복해야 하고, 한 곳을 빼먹으면 환경 간 설정이 조용히 어긋난다.</li> </ul> <p>프로그래밍이었다면 당연히 “중복을 함수로 뽑아내자”고 했을 것이다. Kubernetes 생태계에서 이 역할을 맡은 대표 도구가 <strong>Helm</strong>과 <strong>Kustomize</strong>다. 같은 문제를 정반대 철학으로 푸는 것이 흥미로운 지점이다.</p> <h2 id="61-helm--차트로-묶고-values로-채운다">6.1 Helm — 차트로 묶고, values로 채운다</h2> <p><strong>Helm</strong>은 Kubernetes의 <strong>패키지 매니저</strong>다. Ubuntu의 apt, macOS의 Homebrew가 프로그램 설치에 대해 하는 일을, Helm은 Kubernetes 리소스 뭉치에 대해 한다. 1절에서 “앱스토어”라는 비유를 썼는데, 그 비유가 가장 문자 그대로 실현된 곳이 Helm이다. 실제로 cert-manager도 5절의 정적 매니페스트 방식 외에 공식 Helm 차트 설치를 지원한다.</p> <p>핵심 개념은 세 개다.</p> <table> <thead> <tr> <th>개념</th> <th>의미</th> </tr> </thead> <tbody> <tr> <td><strong>차트(chart)</strong></td> <td>앱 실행에 필요한 리소스 정의 일체를 묶은 <strong>패키지</strong></td> </tr> <tr> <td><strong>values</strong></td> <td>차트의 <strong>설정값</strong>. 기본값은 차트 안의 values.yaml에 있고, 설치할 때 파일이나 옵션으로 덮어쓴다</td> </tr> <tr> <td><strong>release</strong></td> <td>차트를 클러스터에 설치한 <strong>인스턴스</strong>. 같은 차트를 이름만 바꿔 여러 번 설치할 수 있다</td> </tr> </tbody> </table> <p>동작 원리는 <strong>템플릿 렌더링</strong>이다. 차트 안의 매니페스트들은 replicas나 이미지 태그처럼 환경마다 달라질 자리를 이중 중괄호 플레이스홀더로 비워 둔 템플릿이고, 설치 시점에 values의 값이 그 자리에 채워지면서 최종 YAML이 만들어진다. 템플릿 문법 자체는 Helm 공식 문서에 잘 정리되어 있으니, 여기서는 “빈칸 뚫린 YAML 서식 + 값 주입”이라는 그림만 잡으면 된다.</p> <p>환경별 배포 문제는 이렇게 풀린다. <strong>차트는 하나, values 파일만 환경 수만큼.</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 차트 저장소를 등록하고 차트를 검색한다</span>
helm repo add &lt;저장소이름&gt; &lt;저장소URL&gt;
helm search repo &lt;키워드&gt;

<span class="c"># 같은 차트를 환경별 values 파일로 각각 설치한다</span>
helm <span class="nb">install </span>myapp-dev ./mychart <span class="nt">-f</span> values-dev.yaml
helm <span class="nb">install </span>myapp-prod ./mychart <span class="nt">-f</span> values-prod.yaml

<span class="c"># 업그레이드하고, 문제가 생기면 이전 리비전으로 되돌린다</span>
helm upgrade myapp-prod ./mychart <span class="nt">-f</span> values-prod.yaml
helm rollback myapp-prod 1

<span class="c"># 설치된 release 목록 확인과 제거</span>
helm list
helm uninstall myapp-dev
</code></pre></div></div> <p>Helm의 또 다른 무기는 <strong>릴리스 관리</strong>다. release는 업그레이드할 때마다 리비전 히스토리가 쌓이고, 배포가 잘못됐을 때 <code class="language-plaintext highlighter-rouge">helm rollback</code> 한 줄로 이전 리비전으로 되돌릴 수 있다. 남이 만든 앱을 설치하는 표준 통로이자, 우리 앱을 남에게 배포하는 포장 수단인 셈이다.</p> <h2 id="62-kustomize--템플릿-없이-패치를-겹친다">6.2 Kustomize — 템플릿 없이 패치를 겹친다</h2> <p><strong>Kustomize</strong>는 정반대 철학에서 출발한다. <strong>템플릿이 없다(template-free).</strong> 완성된 일반 YAML을 그대로 두고, 환경별 <strong>차이만 패치로 얹는다</strong>.</p> <p>구조는 <strong>base + overlay</strong>다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>myapp/
├── base/                        # 모든 환경의 공통분모 (그 자체로 유효한 YAML)
│   ├── deployment.yaml
│   ├── service.yaml
│   └── kustomization.yaml
└── overlays/
    ├── dev/
    │   └── kustomization.yaml   # base 참조 + replicas 1로 패치
    └── prod/
        └── kustomization.yaml   # base 참조 + replicas 5, 리소스 제한 패치
</code></pre></div></div> <p>각 디렉터리의 kustomization.yaml이 “무엇을 가져와서(resources) 어떻게 고칠지”를 선언한다. 패치 외에도 공통 라벨/네임스페이스/이름 접두사 부여, 파일로부터 ConfigMap·Secret 생성(configMapGenerator, secretGenerator) 같은 기능을 제공한다. 중요한 차이: base의 YAML은 빈칸 뚫린 템플릿이 아니라 <strong>그 자체로 완성된 매니페스트</strong>라서, 그냥 <code class="language-plaintext highlighter-rouge">kubectl apply -f</code>로 적용해도 동작한다.</p> <p>가장 큰 매력은 <strong>kubectl에 내장</strong>되어 있다는 점이다. kubectl 1.14부터 별도 설치 없이 바로 쓸 수 있다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 최종 렌더링 결과를 미리 확인한다</span>
kubectl kustomize overlays/prod

<span class="c"># 렌더링해서 바로 클러스터에 적용한다</span>
kubectl apply <span class="nt">-k</span> overlays/prod
</code></pre></div></div> <p>일상 비유로 정리하면 이렇다. Helm이 <strong>빈칸 뚫린 서식에 값을 채워 문서를 찍어내는</strong> 방식이라면, Kustomize는 <strong>완성된 원본 문서 위에 수정 스티커를 붙이는</strong> 방식이다.</p> <h2 id="63-무엇을-언제-쓰나">6.3 무엇을 언제 쓰나</h2> <table> <thead> <tr> <th>구분</th> <th>Helm</th> <th>Kustomize</th> </tr> </thead> <tbody> <tr> <td>접근 방식</td> <td>템플릿에 values를 채워 렌더링</td> <td>완성된 YAML(base)에 패치(overlay)를 겹침</td> </tr> <tr> <td>단위</td> <td>차트(패키지) → release(설치 인스턴스)</td> <td>디렉터리(base + overlay)</td> </tr> <tr> <td>도구</td> <td>별도 CLI(<code class="language-plaintext highlighter-rouge">helm</code>)</td> <td>kubectl 내장(<code class="language-plaintext highlighter-rouge">kubectl apply -k</code>)</td> </tr> <tr> <td>릴리스 관리</td> <td>리비전 히스토리 + <code class="language-plaintext highlighter-rouge">helm rollback</code></td> <td>없음 (Git 히스토리에 의존)</td> </tr> <tr> <td>장점</td> <td>패키지 공유·배포에 강함. 버전·롤백 관리 내장</td> <td>단순함. 원본 YAML이 그대로 읽힘</td> </tr> <tr> <td>단점</td> <td>템플릿 문법 학습 곡선. 렌더링 전에는 최종 YAML이 안 보임</td> <td>패키지 배포·버전 관리 기능이 없음</td> </tr> <tr> <td>언제 쓰나</td> <td>남이 만든 앱 설치, 여러 곳에 배포할 패키지 제작</td> <td>우리 팀 앱의 dev/prod 환경별 변형 관리</td> </tr> </tbody> </table> <p>둘 중 하나만 골라야 하는 것도 아니다. <strong>남의 앱은 Helm으로 설치하고, 우리 앱은 Kustomize로 관리</strong>하는 조합이 흔하다. 두 도구 모두 이제는 기본기로 취급된다 — 2025년 2월 개편된 CKA(Certified Kubernetes Administrator) 시험 커리큘럼의 “Cluster Architecture, Installation and Configuration” 도메인에 “Helm과 Kustomize를 사용해 클러스터 구성 요소를 설치한다”는 항목이 정식으로 들어갔다.</p> <p>그렇다면 Helm 같은 도구는 누가 검증하고 육성하는가? 이 질문이 자연스럽게 다음 절로 이어진다.</p> <hr/> <h1 id="7-cncf-projects--생태계의-지도를-읽는-법">7. CNCF Projects — 생태계의 지도를 읽는 법</h1> <p>5절에서 설치한 cert-manager는 개인 프로젝트가 아니다. <a href="/blog/2026/k8s-01-history/">01편</a>에서 소개한 <strong>CNCF(Cloud Native Computing Foundation)</strong> 가 육성해온 프로젝트다. 클라우드 네이티브 세계에는 이런 오픈소스 프로젝트가 수없이 많은데, 문제는 <strong>“어떤 걸 믿고 프로덕션에 도입할 것인가”</strong> 다.</p> <p>CNCF는 이 판단을 돕기 위해 프로젝트를 <strong>성숙도 3단계</strong>로 나눈다.</p> <table> <thead> <tr> <th>단계</th> <th>의미</th> <th>기술 채택 곡선 대응</th> </tr> </thead> <tbody> <tr> <td><strong>Sandbox</strong></td> <td>실험 단계. 아직 프로덕션에서 널리 검증되지 않은 초기 프로젝트</td> <td>Innovators</td> </tr> <tr> <td><strong>Incubating</strong></td> <td>소수의 사용자가 프로덕션에서 성공적으로 사용 중</td> <td>Early Adopters</td> </tr> <tr> <td><strong>Graduated</strong></td> <td>안정적이고 널리 채택됨. 프로덕션 준비 완료로 간주</td> <td>Early Majority</td> </tr> </tbody> </table> <p>CNCF는 이 3단계가 기술 채택 이론(Crossing the Chasm)의 Innovators → Early Adopters → Early Majority 층에 대응한다고 설명한다. 즉 <strong>회사의 위험 감수 성향에 따라 어느 단계 프로젝트까지 도입할지 판단하는 기준</strong>으로 쓰라는 것이다. 참고로 활동이 멈춘 프로젝트는 Archived로 분류된다.</p> <p>단계가 올라가는 것은 인기 투표가 아니다. Graduated로 승격하려면 CNCF 기술감독위원회(TOC)의 심사를 통과해야 하는데, 요구 사항에는 다음이 포함된다.</p> <ul> <li><strong>여러 조직 출신의 메인테이너</strong>: 최소 2개 이상 조직의 메인테이너 — 한 회사가 손을 떼도 프로젝트가 살아남는가(생존성)</li> <li><strong>제3자 보안 감사</strong>: 외부 보안 감사 수행과 발견 사항의 해결 추적</li> <li><strong>OpenSSF Best Practices 배지</strong>: 오픈소스 보안 모범 사례 준수 인증</li> <li><strong>실제 채택 검증</strong>: 프로덕션에서 사용하는 독립적인 채택 조직들(adopter) 확인</li> <li><strong>거버넌스 문서화</strong>: 메인테이너 선출·승계 절차, 기여 절차, 행동 강령(Code of Conduct)</li> </ul> <p>말하자면 Graduated는 <strong>“이 프로젝트는 특정 회사의 소유물이 아니고, 보안이 감사됐고, 실제로 여러 곳의 프로덕션에서 돌고 있다”는 CNCF의 보증 마크</strong>다.</p> <p>대표적인 Graduated 프로젝트를 보면 이 시리즈에서 만난 이름이 여럿 보인다.</p> <table> <thead> <tr> <th>프로젝트</th> <th>분야</th> <th>시리즈와의 연결</th> </tr> </thead> <tbody> <tr> <td>Kubernetes</td> <td>컨테이너 오케스트레이션</td> <td>이 시리즈 전체. CNCF의 1호 프로젝트(<a href="/blog/2026/k8s-01-history/">01편</a>)</td> </tr> <tr> <td>etcd</td> <td>분산 key-value 저장소</td> <td>클러스터의 상태 저장소(<a href="/blog/2026/k8s-03-architecture/">03편</a>)</td> </tr> <tr> <td>containerd</td> <td>컨테이너 런타임</td> <td>kubelet이 CRI로 위임하는 런타임(<a href="/blog/2026/k8s-03-architecture/">03편</a>)</td> </tr> <tr> <td>Prometheus</td> <td>모니터링·관측</td> <td>사실상 표준 메트릭 수집기. Prometheus Operator의 대상</td> </tr> <tr> <td>Envoy</td> <td>서비스 프록시</td> <td>L7 프록시. 많은 Ingress/서비스 메시의 데이터 플레인</td> </tr> <tr> <td>Helm</td> <td>패키지 매니저</td> <td>YAML 뭉치를 하나의 패키지(차트)로 설치</td> </tr> <tr> <td>Argo</td> <td>CI/CD·워크플로</td> <td>GitOps 배포(Argo CD)로 유명</td> </tr> <tr> <td>Fluentd</td> <td>로깅</td> <td>노드별 로그 수집 — DaemonSet의 대표 사례(<a href="/blog/2026/k8s-05-workloads/">05편</a>)</td> </tr> <tr> <td>cert-manager</td> <td>보안·인증서</td> <td>방금 실습한 그 프로젝트</td> </tr> </tbody> </table> <p>전체 목록과 각 프로젝트의 현재 단계는 <a href="https://www.cncf.io/projects/">cncf.io/projects</a>에서 확인할 수 있다. 프로젝트 수는 계속 변하므로 외울 필요는 없고, <strong>지도를 읽는 법</strong>만 기억하면 된다.</p> <p>마지막으로 cert-manager의 여정을 보면 이 제도가 실제로 어떻게 굴러가는지 보인다. cert-manager는 2020년 11월 CNCF Sandbox로 들어와, 2022년 9월 Incubating으로 승격했고, CNCF가 후원한 보안 감사를 거쳐 <strong>2024년 9월 Graduated가 됐다</strong>. 우리가 방금 <code class="language-plaintext highlighter-rouge">kubectl apply</code> 한 줄로 설치한 도구 뒤에는 이런 4년의 검증 과정이 있었던 것이다.</p> <hr/> <h1 id="8-시리즈-마무리--우리가-걸어온-길">8. 시리즈 마무리 — 우리가 걸어온 길</h1> <p>9편에 걸친 K8s 입문 시리즈가 끝났다. 한 줄씩 되짚어 보자.</p> <table> <thead> <tr> <th>편</th> <th>주제</th> <th>한 줄 요약</th> </tr> </thead> <tbody> <tr> <td><a href="/blog/2026/k8s-01-history/">01</a></td> <td>탄생과 배경</td> <td>Google Borg의 경험이 오픈소스가 되어 CNCF 생태계의 중심이 됐다</td> </tr> <tr> <td><a href="/blog/2026/k8s-02-local-setup/">02</a></td> <td>kind와 kubectl</td> <td>노트북 위 컨테이너로 진짜 클러스터를 만들고 kubeconfig로 접속한다</td> </tr> <tr> <td><a href="/blog/2026/k8s-03-architecture/">03</a></td> <td>아키텍처</td> <td>Control Plane(결정)과 Node(실행), 모든 통신은 kube-apiserver를 거친다</td> </tr> <tr> <td><a href="/blog/2026/k8s-04-pod/">04</a></td> <td>Pod</td> <td>배포의 최소 단위. YAML 선언이 스케줄링을 거쳐 컨테이너가 되기까지</td> </tr> <tr> <td><a href="/blog/2026/k8s-05-workloads/">05</a></td> <td>워크로드</td> <td>Deployment/StatefulSet/DaemonSet — Pod를 상황에 맞게 관리하는 상위 리소스</td> </tr> <tr> <td><a href="/blog/2026/k8s-06-networking/">06</a></td> <td>네트워킹</td> <td>바뀌는 Pod IP 대신 Service로 고정 접점을, Ingress로 L7 라우팅을</td> </tr> <tr> <td><a href="/blog/2026/k8s-07-storage-config/">07</a></td> <td>스토리지와 설정</td> <td>PV/PVC/StorageClass로 데이터를, ConfigMap/Secret으로 설정을 분리한다</td> </tr> <tr> <td><a href="/blog/2026/k8s-08-rbac/">08</a></td> <td>권한 관리</td> <td>누가(SA) 무엇을(Role) 할 수 있는지 — 최소 권한 원칙의 RBAC</td> </tr> <tr> <td>09 (이 글)</td> <td>확장과 생태계</td> <td>Operator로 확장하고, Helm/Kustomize로 YAML을 패키징하고, CNCF 성숙도로 생태계를 읽는다</td> </tr> </tbody> </table> <p>관통하는 원리는 하나였다. <strong>원하는 상태를 선언하면 컨트롤러가 차이를 메운다.</strong> Deployment도, Service의 Endpoints도, cert-manager의 인증서 갱신도 전부 이 reconcile 루프의 변주다. 이 원리 하나를 몸에 익혔다면 처음 보는 리소스를 만나도 “spec이 뭐고, 어느 컨트롤러가 reconcile하는가”부터 물으면 된다.</p> <p>다음 단계로는 이런 방향을 추천한다.</p> <ul> <li><strong>남에게 설명해보기</strong>: 이 시리즈의 로드맵 원글(seungkyua, SK Devocean)은 “남에게 설명할 수 있을 정도로” 공부하라고 조언한다. 각 편의 개념을 자기 말로 설명해보고, 막히는 곳이 다음 공부 지점이다.</li> <li><strong>자격증으로 검증</strong>: CKA(Certified Kubernetes Administrator) 같은 실기 시험은 kubectl 실습 위주라 이 시리즈의 연장선에서 준비하기 좋다. 2025년 개편 커리큘럼에는 6절에서 본 Helm·Kustomize도 포함됐다.</li> <li><strong>생태계 탐색</strong>: <a href="https://landscape.cncf.io/">CNCF landscape</a>에서 관심 분야(모니터링, CI/CD, 서비스 메시 등)의 프로젝트를 골라 kind 클러스터에 직접 설치해보자. 7절의 성숙도 지도가 나침반이 된다.</li> <li><strong>Operator 직접 만들기</strong>: Go의 kubebuilder 같은 프레임워크로 간단한 CRD와 컨트롤러를 직접 작성해보면 reconcile 루프가 완전히 체화된다.</li> </ul> <blockquote> <p>이 글로 K8s 입문 시리즈를 마친다. 이전 글은 <a href="/blog/2026/k8s-08-rbac/">08: 권한 관리 — ServiceAccount와 RBAC</a>이고, 처음부터 다시 보려면 <a href="/blog/2026/k8s-01-history/">01: Kubernetes의 탄생 — Google Borg에서 CNCF까지</a>에서 시작하면 된다. 완주를 축하한다.</p> </blockquote> <h1 id="참고-문헌">참고 문헌</h1> <ul> <li><a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua, SK Devocean, 2024) — 시리즈 로드맵 출처</li> <li><a href="https://kubernetes.io/docs/concepts/extend-kubernetes/operator/">Operator pattern — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/concepts/extend-kubernetes/api-extension/custom-resources/">Custom Resources — Kubernetes 공식 문서</a></li> <li><a href="https://www.redhat.com/en/blog/introducing-operators-putting-operational-knowledge-into-software">Introducing Operators: Putting Operational Knowledge into Software</a> (CoreOS, 2016 — 현재 Red Hat 블로그에 보존)</li> <li><a href="https://www.cncf.io/projects/">CNCF Projects — 성숙도 단계와 프로젝트 목록</a></li> <li><a href="https://github.com/cncf/toc/blob/main/process/graduation_criteria.md">CNCF Graduation Criteria (TOC)</a></li> <li><a href="https://www.cncf.io/announcements/2024/11/12/cloud-native-computing-foundation-announces-cert-manager-graduation/">Cloud Native Computing Foundation Announces cert-manager Graduation</a> (CNCF, 2024)</li> <li><a href="https://cert-manager.io/docs/installation/">cert-manager Installation — 공식 문서</a></li> <li><a href="https://cert-manager.io/docs/configuration/selfsigned/">cert-manager SelfSigned Issuer — 공식 문서</a></li> <li><a href="https://helm.sh/docs/intro/using_helm/">Using Helm — Helm 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/tasks/manage-kubernetes-objects/kustomization/">Declarative Management of Kubernetes Objects Using Kustomize — Kubernetes 공식 문서</a></li> <li><a href="https://training.linuxfoundation.org/certified-kubernetes-administrator-cka-program-changes/">Certified Kubernetes Administrator (CKA) Program Changes — Linux Foundation</a></li> </ul>]]></content><author><name></name></author><category term="infra"/><category term="kubernetes"/><category term="infra"/><category term="operator"/><category term="crd"/><category term="helm"/><category term="kustomize"/><category term="cncf"/><summary type="html"><![CDATA[CRD와 Custom Controller로 나만의 리소스를 만드는 Operator 패턴, cert-manager 실습, 그리고 CNCF 성숙도 3단계로 읽는 생태계 지도 — K8s 입문 시리즈 마지막 편]]></summary></entry><entry><title type="html">Kubernetes 권한 관리 — ServiceAccount와 RBAC</title><link href="https://www.wonbeomjang.kr/blog/2026/k8s-08-rbac/" rel="alternate" type="text/html" title="Kubernetes 권한 관리 — ServiceAccount와 RBAC"/><published>2026-07-06T23:00:00+00:00</published><updated>2026-07-06T23:00:00+00:00</updated><id>https://www.wonbeomjang.kr/blog/2026/k8s-08-rbac</id><content type="html" xml:base="https://www.wonbeomjang.kr/blog/2026/k8s-08-rbac/"><![CDATA[<blockquote> <p>이 글은 <strong>K8s 입문 시리즈</strong>의 여덟 번째 글이다.</p> <ul> <li><a href="/blog/2026/k8s-01-history/">01: Kubernetes의 탄생 — Google Borg에서 CNCF까지</a></li> <li><a href="/blog/2026/k8s-02-local-setup/">02: 내 노트북에 클러스터 만들기 — kind와 kubectl</a></li> <li><a href="/blog/2026/k8s-03-architecture/">03: Kubernetes 아키텍처 — Control Plane과 Node</a></li> <li><a href="/blog/2026/k8s-04-pod/">04: Pod의 모든 것 — 생성부터 스케줄링까지</a></li> <li><a href="/blog/2026/k8s-05-workloads/">05: 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</a></li> <li><a href="/blog/2026/k8s-06-networking/">06: 네트워킹 — Service와 Ingress</a></li> <li><a href="/blog/2026/k8s-07-storage-config/">07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</a></li> <li><strong>08: 권한 관리 — ServiceAccount와 RBAC</strong> ← 현재 글</li> <li><a href="/blog/2026/k8s-09-operator-cncf/">09: 확장과 생태계 — Operator와 CNCF Projects</a></li> </ul> <p>이 시리즈의 커리큘럼은 SK Devocean의 <a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua) 글의 학습 로드맵을 바탕으로 구성했다.</p> </blockquote> <p>지금까지 우리는 kind 클러스터에서 관리자 계정 하나로 모든 걸 해왔다. Pod를 만들고, Deployment를 롤백하고, Secret까지 읽었다. 로컬 실습이니까 괜찮았지만, 회사 클러스터라면 이야기가 다르다. 신입 개발자가 실수로 <code class="language-plaintext highlighter-rouge">kubectl delete namespace prod</code>를 칠 수 있는 클러스터, 로그 수집용 Pod가 Secret을 읽을 수 있는 클러스터는 사고가 나기 전의 상태일 뿐이다. 이번 글에서는 Kubernetes가 “누가, 무엇을 할 수 있는가”를 통제하는 방법 — ServiceAccount와 RBAC — 을 배우고, 읽기 전용 계정을 직접 만들어 검증까지 해본다.</p> <hr/> <h1 id="1-누가인증-무엇을-할-수-있나인가--api-서버의-관문">1. 누가(인증) 무엇을 할 수 있나(인가) — API 서버의 관문</h1> <p>건물 출입에 비유하면 이해가 빠르다. 1층 게이트에서 사원증을 찍는 것이 <strong>인증(Authentication)</strong> 이다. “너가 누구인지”를 확인할 뿐, 아직 아무 데도 못 간다. 사원증을 찍고 나서 서버실 문 앞에 섰을 때 문이 열리는지가 <strong>인가(Authorization)</strong> 다. 같은 직원이라도 서버실 출입 권한은 따로 부여받아야 한다.</p> <table> <thead> <tr> <th>질문</th> <th>단계</th> <th>Kubernetes에서의 수단</th> <th>실패 시 응답</th> </tr> </thead> <tbody> <tr> <td>“너 누구야?”</td> <td>인증 (Authentication)</td> <td>클라이언트 인증서, 토큰(SA JWT) 등</td> <td>401 Unauthorized</td> </tr> <tr> <td>“너 이거 해도 돼?”</td> <td>인가 (Authorization)</td> <td>RBAC (그 외 ABAC, Node, Webhook 모드)</td> <td>403 Forbidden</td> </tr> <tr> <td>“규칙에 맞는 요청?”</td> <td>어드미션 (Admission)</td> <td>Admission Controller (변형/검증)</td> <td>요청 즉시 거부</td> </tr> </tbody> </table> <p><a href="/blog/2026/k8s-03-architecture/">03편</a>에서 kube-apiserver가 모든 통신의 관문이고, 요청이 <strong>인증 → 인가 → 어드미션</strong> 파이프라인을 거친다고 했다. 이번 글의 주인공인 RBAC은 이 중 두 번째 단계, 인가를 담당한다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-08-rbac/api-request-pipeline-480.webp 480w,/assets/post/image/k8s-08-rbac/api-request-pipeline-800.webp 800w,/assets/post/image/k8s-08-rbac/api-request-pipeline-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-08-rbac/api-request-pipeline.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="kube-apiserver 요청 처리 파이프라인 — kubectl/Pod의 TLS 요청이 인증, 인가(RBAC), 어드미션 컨트롤을 차례로 거쳐 etcd에 저장되고, 인증 실패는 401, 인가 거부는 403으로 요청이 거부된다" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>인증에 실패하면 401, 인가에서 거부되면 403이 돌아온다. <code class="language-plaintext highlighter-rouge">kubectl</code> 에러 메시지에서 <code class="language-plaintext highlighter-rouge">Forbidden</code>을 봤다면 “누군지는 알겠는데 권한이 없다”는 뜻이다.</li> <li>어드미션 컨트롤은 <strong>인가가 통과된 뒤에만</strong> 실행된다. 권한이 있는 요청이라도 정책(리소스 쿼터 초과 등)에 걸리면 여기서 거부된다.</li> <li>Kubernetes의 인가 모드에는 RBAC 외에도 ABAC, Node, Webhook 등이 있지만, 오늘날 사실상의 표준은 RBAC(Role-Based Access Control)이다.</li> </ul> <p>여기서 중요한 설계 포인트 하나. RBAC에는 <strong>거부(deny) 규칙이 없다</strong>. 기본 상태가 전부 거부이고, 규칙은 오직 허용만 추가한다(purely additive). 블랙리스트가 아니라 화이트리스트 방식이라, “실수로 너무 넓게 거부해서 장애”가 아니라 “허용을 안 해서 403”이 기본 실패 모드가 된다. 보안 관점에서 훨씬 안전한 기본값이다.</p> <hr/> <h1 id="2-serviceaccount--pod의-신분증">2. ServiceAccount — Pod의 신분증</h1> <p>인가를 하려면 먼저 “누구”가 정의되어야 한다. Kubernetes의 클라이언트는 크게 둘이다.</p> <table> <thead> <tr> <th>구분</th> <th>User (사람)</th> <th>ServiceAccount (Pod)</th> </tr> </thead> <tbody> <tr> <td>대상</td> <td>개발자, 운영자</td> <td>Pod 안에서 실행되는 프로세스</td> </tr> <tr> <td>Kubernetes 오브젝트</td> <td>아님 — 클러스터 외부에서 관리</td> <td>맞음 — <code class="language-plaintext highlighter-rouge">kubectl create serviceaccount</code>로 생성</td> </tr> <tr> <td>소속</td> <td>클러스터 전역</td> <td>특정 네임스페이스</td> </tr> <tr> <td>인증 수단</td> <td>인증서, OIDC 토큰 등</td> <td>TokenRequest API 기반 JWT</td> </tr> <tr> <td>표기 예</td> <td><code class="language-plaintext highlighter-rouge">jane</code></td> <td><code class="language-plaintext highlighter-rouge">system:serviceaccount:dev:foo</code></td> </tr> </tbody> </table> <p>사람(User)은 의외로 Kubernetes API 오브젝트가 아니다. 인증서나 사내 SSO(OIDC) 같은 외부 수단으로 신원을 증명하면, API 서버는 거기 적힌 이름을 그대로 사용자명으로 쓴다. 반면 <strong>ServiceAccount는 클러스터 안에 존재하는 정식 리소스</strong>다. Pod가 API 서버에 뭔가를 요청해야 할 때 — 예를 들어 모니터링 에이전트가 Pod 목록을 읽을 때 — 그 Pod의 신분증 역할을 한다.</p> <p>알아둘 기본 동작 세 가지.</p> <ol> <li><strong>모든 네임스페이스에는 <code class="language-plaintext highlighter-rouge">default</code>라는 ServiceAccount가 자동 생성된다.</strong> Pod를 만들 때 따로 지정하지 않으면 이 <code class="language-plaintext highlighter-rouge">default</code> SA가 자동으로 할당된다. <code class="language-plaintext highlighter-rouge">kubectl get pod &lt;이름&gt; -o yaml</code>에서 <code class="language-plaintext highlighter-rouge">spec.serviceAccountName</code>을 확인해 보면 보인다.</li> <li><strong>토큰은 Pod 안에 자동 마운트된다.</strong> 경로는 <code class="language-plaintext highlighter-rouge">/var/run/secrets/kubernetes.io/serviceaccount/</code>이고 <code class="language-plaintext highlighter-rouge">token</code>(JWT), <code class="language-plaintext highlighter-rouge">ca.crt</code>(CA 인증서), <code class="language-plaintext highlighter-rouge">namespace</code> 세 파일이 들어 있다. Pod 안의 프로세스는 이 토큰으로 API 서버에 인증한다.</li> <li><strong>v1.24부터 토큰은 수명이 있는 단기 토큰이다.</strong> 예전에는 SA를 만들면 만료 없는 토큰 Secret이 자동 생성됐지만, 지금은 TokenRequest API로 발급된 <strong>수명 제한(bounded lifetime) 토큰</strong>이 projected volume으로 마운트되고, Pod가 삭제되면 무효화된다. 수동으로 발급해 보고 싶으면 <code class="language-plaintext highlighter-rouge">kubectl create token &lt;SA이름&gt;</code>을 쓰면 된다(<code class="language-plaintext highlighter-rouge">--duration=24h</code>처럼 기간 지정도 가능).</li> </ol> <p>토큰이 필요 없는 Pod라면 자동 마운트를 꺼두는 것이 안전하다. ServiceAccount나 Pod 스펙에 <code class="language-plaintext highlighter-rouge">automountServiceAccountToken: false</code>를 주면 되고, 둘 다 지정된 경우 <strong>Pod 쪽 설정이 우선</strong>한다.</p> <p>RBAC에서 ServiceAccount를 지칭할 때는 다음 형식의 전체 이름을 쓴다. 잠시 후 실습에서 그대로 사용한다.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>system:serviceaccount:&lt;네임스페이스&gt;:&lt;ServiceAccount 이름&gt;
</code></pre></div></div> <hr/> <h1 id="3-rbac-4요소--role-clusterrole-rolebinding-clusterrolebinding">3. RBAC 4요소 — Role, ClusterRole, RoleBinding, ClusterRoleBinding</h1> <p>RBAC은 리소스 네 개로 굴러간다. 구조는 단순하다. <strong>“무엇을 할 수 있는가”를 정의하는 문서(Role/ClusterRole)</strong> 와 <strong>“그 문서를 누구에게 주는가”를 정의하는 문서(RoleBinding/ClusterRoleBinding)</strong> 가 분리되어 있다. 도서관에 비유하면 Role은 “열람실 이용 규정”이고 RoleBinding은 “이 회원에게 열람증 발급”이다. 규정과 발급 대장을 분리했기 때문에, 같은 규정을 여러 사람에게 재사용할 수 있다.</p> <table> <thead> <tr> <th>리소스</th> <th>범위</th> <th>역할</th> </tr> </thead> <tbody> <tr> <td>Role</td> <td>네임스페이스</td> <td>특정 네임스페이스 안에서의 권한 집합 정의</td> </tr> <tr> <td>ClusterRole</td> <td>클러스터 전역</td> <td>클러스터 전체 또는 재사용 가능한 권한 정의</td> </tr> <tr> <td>RoleBinding</td> <td>네임스페이스</td> <td>Role(또는 ClusterRole)을 주체에게 연결</td> </tr> <tr> <td>ClusterRoleBinding</td> <td>클러스터 전역</td> <td>ClusterRole을 클러스터 전체 범위로 연결</td> </tr> </tbody> </table> <p>Role은 반드시 특정 네임스페이스에 속하고, ClusterRole은 네임스페이스가 없는 클러스터 전역 리소스다. 여기서 조합이 하나 더 생긴다. <strong>RoleBinding이 ClusterRole을 참조할 수 있다.</strong> 이때 권한은 ClusterRole에 적힌 대로지만, 효력 범위는 RoleBinding이 있는 네임스페이스로 한정된다. “공통 권한 세트를 한 번만 정의하고, 네임스페이스마다 바인딩만 만드는” 재사용 패턴이다.</p> <table> <thead> <tr> <th>조합</th> <th>권한이 미치는 범위</th> <th>대표 용도</th> </tr> </thead> <tbody> <tr> <td>Role + RoleBinding</td> <td>해당 네임스페이스</td> <td>팀/앱 단위 세밀한 권한</td> </tr> <tr> <td>ClusterRole + RoleBinding</td> <td>RoleBinding의 네임스페이스로 한정</td> <td>공통 권한 정의를 네임스페이스별로 재사용</td> </tr> <tr> <td>ClusterRole + ClusterRoleBinding</td> <td>클러스터 전체</td> <td>관리자, 노드·PV 같은 클러스터 범위 리소스</td> </tr> <tr> <td>Role + ClusterRoleBinding</td> <td>불가능</td> <td>ClusterRoleBinding의 roleRef는 ClusterRole만 참조 가능</td> </tr> </tbody> </table> <p>주의할 점 하나 — <strong>바인딩의 <code class="language-plaintext highlighter-rouge">roleRef</code>는 불변(immutable)이다.</strong> 만든 뒤에 참조할 Role을 바꾸려고 하면 검증 에러가 난다. 바꾸고 싶으면 바인딩을 지우고 새로 만들어야 한다. 바인딩 수정 권한을 가진 사람이 roleRef만 슬쩍 바꿔서 권한을 승격시키는 것을 막기 위한 설계다.</p> <p>바퀴를 새로 발명할 필요도 없다. Kubernetes에는 사용자용 기본 ClusterRole이 내장되어 있다.</p> <table> <thead> <tr> <th>기본 ClusterRole</th> <th>권한 요약</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">cluster-admin</code></td> <td>클러스터 전체 슈퍼유저. 무엇이든 가능</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">admin</code></td> <td>네임스페이스 안의 관리자. Role/RoleBinding 생성으로 권한 위임 가능</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">edit</code></td> <td>네임스페이스 안 대부분 리소스 읽기/쓰기. Role/RoleBinding은 불가</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">view</code></td> <td>네임스페이스 안 읽기 전용. Secret은 볼 수 없음</td> </tr> </tbody> </table> <p>네 요소가 연결되는 그림은 이렇다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-08-rbac/rbac-binding-chain-480.webp 480w,/assets/post/image/k8s-08-rbac/rbac-binding-chain-800.webp 800w,/assets/post/image/k8s-08-rbac/rbac-binding-chain-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-08-rbac/rbac-binding-chain.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="RBAC 4요소 연결 — ServiceAccount pod-viewer가 RoleBinding read-pods의 subjects로 지정되고, roleRef가 Role pod-reader를 참조하며, rbac-lab 네임스페이스의 pods 리소스에 get/list/watch가 허용된다" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>ServiceAccount 자체에는 권한 정보가 전혀 없다. 권한은 오직 바인딩을 통해서만 흘러들어온다.</li> <li>API 서버는 요청이 올 때마다 “이 주체가 subjects에 포함된 바인딩이 있는가 → 그 바인딩의 roleRef가 가리키는 Role에 이 동사+리소스가 허용되어 있는가”를 평가한다.</li> <li>셋 중 하나만 없어도(SA만 있고 바인딩이 없거나, 바인딩은 있는데 Role이 없거나) 결과는 403이다.</li> </ul> <hr/> <h1 id="4-규칙-해부--apigroups-resources-verbs">4. 규칙 해부 — apiGroups, resources, verbs</h1> <p>Role의 알맹이는 <code class="language-plaintext highlighter-rouge">rules</code> 배열이다. 공식 문서의 예시를 보자.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">rbac.authorization.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Role</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">default</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">pod-reader</span>
<span class="na">rules</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">apiGroups</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">"</span><span class="pi">]</span> <span class="c1"># ""는 core API 그룹</span>
    <span class="na">resources</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">pods"</span><span class="pi">]</span>
    <span class="na">verbs</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">get"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">watch"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">list"</span><span class="pi">]</span>
</code></pre></div></div> <p>규칙 한 줄은 “<strong>어느 API 그룹의</strong>(apiGroups) <strong>어떤 리소스에</strong>(resources) <strong>어떤 동작을</strong>(verbs) 허용한다”로 읽는다. 필드를 하나씩 뜯어 보자.</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">apiGroups</code></strong> — 리소스가 속한 API 그룹. Pod, Service, ConfigMap 같은 초창기 리소스는 <strong>core 그룹</strong>이라 빈 문자열 <code class="language-plaintext highlighter-rouge">""</code>로 쓴다. Deployment라면 <code class="language-plaintext highlighter-rouge">apps</code>, Ingress라면 <code class="language-plaintext highlighter-rouge">networking.k8s.io</code>처럼 그룹 이름을 적는다. <code class="language-plaintext highlighter-rouge">kubectl api-resources</code> 출력의 APIVERSION 열에서 슬래시 앞부분이 바로 이 값이다.</li> <li><strong><code class="language-plaintext highlighter-rouge">resources</code></strong> — 대상 리소스 종류. <code class="language-plaintext highlighter-rouge">pods</code>, <code class="language-plaintext highlighter-rouge">deployments</code>처럼 복수형 소문자로 쓴다.</li> <li><strong><code class="language-plaintext highlighter-rouge">verbs</code></strong> — 허용할 동작. REST API의 동사에 대응한다.</li> </ul> <table> <thead> <tr> <th>verb</th> <th>의미</th> <th>kubectl 대응 예</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">get</code></td> <td>단일 리소스 조회</td> <td><code class="language-plaintext highlighter-rouge">kubectl get pod nginx</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">list</code></td> <td>목록 조회</td> <td><code class="language-plaintext highlighter-rouge">kubectl get pods</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">watch</code></td> <td>변경 사항 실시간 구독</td> <td><code class="language-plaintext highlighter-rouge">kubectl get pods -w</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">create</code></td> <td>생성</td> <td><code class="language-plaintext highlighter-rouge">kubectl apply -f</code> (신규)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">update</code></td> <td>전체 수정</td> <td><code class="language-plaintext highlighter-rouge">kubectl replace</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">patch</code></td> <td>부분 수정</td> <td><code class="language-plaintext highlighter-rouge">kubectl patch</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">delete</code></td> <td>삭제</td> <td><code class="language-plaintext highlighter-rouge">kubectl delete pod nginx</code></td> </tr> </tbody> </table> <p>이번에는 바인딩 쪽이다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">rbac.authorization.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">RoleBinding</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">read-pods</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">default</span>
<span class="na">subjects</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">kind</span><span class="pi">:</span> <span class="s">User</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">jane</span> <span class="c1"># 대소문자 구분</span>
    <span class="na">apiGroup</span><span class="pi">:</span> <span class="s">rbac.authorization.k8s.io</span>
<span class="na">roleRef</span><span class="pi">:</span>
  <span class="na">kind</span><span class="pi">:</span> <span class="s">Role</span> <span class="c1"># Role 또는 ClusterRole</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">pod-reader</span> <span class="c1"># 참조할 Role 이름</span>
  <span class="na">apiGroup</span><span class="pi">:</span> <span class="s">rbac.authorization.k8s.io</span>
</code></pre></div></div> <ul> <li><strong><code class="language-plaintext highlighter-rouge">subjects</code></strong> — 권한을 받을 주체 목록. <code class="language-plaintext highlighter-rouge">kind</code>는 User, Group, ServiceAccount 세 가지다. User/Group은 <code class="language-plaintext highlighter-rouge">apiGroup: rbac.authorization.k8s.io</code>를 명시하지만, <strong>ServiceAccount 주체는 <code class="language-plaintext highlighter-rouge">apiGroup</code> 없이 <code class="language-plaintext highlighter-rouge">name</code>과 <code class="language-plaintext highlighter-rouge">namespace</code>를 적는다</strong>(코어 리소스이기 때문).</li> <li><strong><code class="language-plaintext highlighter-rouge">roleRef</code></strong> — 연결할 Role/ClusterRole 하나. 앞서 말했듯 생성 후 변경 불가.</li> <li>네임스페이스의 모든 SA에게 한 번에 주고 싶으면 <code class="language-plaintext highlighter-rouge">kind: Group</code>으로 <code class="language-plaintext highlighter-rouge">system:serviceaccounts:&lt;네임스페이스&gt;</code>를, 클러스터의 모든 SA라면 <code class="language-plaintext highlighter-rouge">system:serviceaccounts</code>를 주체로 쓸 수 있다.</li> </ul> <hr/> <h1 id="5-실습--읽기-전용-serviceaccount-만들기">5. 실습 — 읽기 전용 ServiceAccount 만들기</h1> <p>이제 kind 클러스터(<a href="/blog/2026/k8s-02-local-setup/">02편</a>에서 구축)에서 직접 만들어 본다. 목표는 <strong>“rbac-lab 네임스페이스의 Pod만 읽을 수 있는 계정”</strong> 이다.</p> <h2 id="51-네임스페이스와-serviceaccount-생성">5.1 네임스페이스와 ServiceAccount 생성</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create namespace rbac-lab
kubectl create serviceaccount pod-viewer <span class="nt">-n</span> rbac-lab
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace/rbac-lab created
serviceaccount/pod-viewer created
</code></pre></div></div> <h2 id="52-role과-rolebinding-작성">5.2 Role과 RoleBinding 작성</h2> <p>Pod에 대한 읽기 동사 세 개(get/list/watch)만 허용하는 Role, 그리고 그 Role을 <code class="language-plaintext highlighter-rouge">pod-viewer</code>에게 연결하는 RoleBinding이다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># pod-reader.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">rbac.authorization.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Role</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">rbac-lab</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">pod-reader</span>
<span class="na">rules</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">apiGroups</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">"</span><span class="pi">]</span> <span class="c1"># core 그룹 (Pod가 속한 곳)</span>
    <span class="na">resources</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">pods"</span><span class="pi">]</span>
    <span class="na">verbs</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">get"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">list"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">watch"</span><span class="pi">]</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">rbac.authorization.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">RoleBinding</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">read-pods</span>
  <span class="na">namespace</span><span class="pi">:</span> <span class="s">rbac-lab</span>
<span class="na">subjects</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">kind</span><span class="pi">:</span> <span class="s">ServiceAccount</span>
    <span class="na">name</span><span class="pi">:</span> <span class="s">pod-viewer</span>
    <span class="na">namespace</span><span class="pi">:</span> <span class="s">rbac-lab</span>
<span class="na">roleRef</span><span class="pi">:</span>
  <span class="na">kind</span><span class="pi">:</span> <span class="s">Role</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">pod-reader</span>
  <span class="na">apiGroup</span><span class="pi">:</span> <span class="s">rbac.authorization.k8s.io</span>
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> pod-reader.yaml
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>role.rbac.authorization.k8s.io/pod-reader created
rolebinding.rbac.authorization.k8s.io/read-pods created
</code></pre></div></div> <h2 id="53-kubectl-auth-can-i로-검증">5.3 kubectl auth can-i로 검증</h2> <p>권한을 확인하는 전용 명령이 있다. 문법은 다음과 같다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl auth can-i VERB <span class="o">[</span>TYPE | TYPE/NAME | NONRESOURCEURL]
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">--as</code> 플래그와 조합하면 <strong>다른 주체인 척(impersonation)</strong> 권한을 검사할 수 있다. ServiceAccount는 2절에서 본 <code class="language-plaintext highlighter-rouge">system:serviceaccount:&lt;네임스페이스&gt;:&lt;이름&gt;</code> 형식으로 지정한다. 임퍼소네이션 자체가 별도 권한이 필요한 동작이지만, kind가 만들어 준 기본 계정은 관리자 권한이라 바로 쓸 수 있다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 허용한 동작: pods 목록 조회 → yes</span>
kubectl auth can-i list pods <span class="se">\</span>
  <span class="nt">--as</span><span class="o">=</span>system:serviceaccount:rbac-lab:pod-viewer <span class="nt">-n</span> rbac-lab
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>yes
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 허용 안 한 동사: pods 삭제 → no</span>
kubectl auth can-i delete pods <span class="se">\</span>
  <span class="nt">--as</span><span class="o">=</span>system:serviceaccount:rbac-lab:pod-viewer <span class="nt">-n</span> rbac-lab
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>no
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 허용 안 한 리소스: secrets 조회 → no</span>
kubectl auth can-i get secrets <span class="se">\</span>
  <span class="nt">--as</span><span class="o">=</span>system:serviceaccount:rbac-lab:pod-viewer <span class="nt">-n</span> rbac-lab
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>no
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 다른 네임스페이스의 pods → no (Role은 rbac-lab에서만 유효)</span>
kubectl auth can-i list pods <span class="se">\</span>
  <span class="nt">--as</span><span class="o">=</span>system:serviceaccount:rbac-lab:pod-viewer <span class="nt">-n</span> default
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>no
</code></pre></div></div> <p>정확히 의도대로다. 허용한 것(rbac-lab의 pods 읽기)만 yes고, 동사가 달라도(delete), 리소스가 달라도(secrets), 네임스페이스가 달라도(default) 전부 no다. RBAC이 화이트리스트라는 것, Role이 네임스페이스 범위라는 것이 그대로 드러난다.</p> <p>주체가 가진 권한을 한눈에 보고 싶으면 <code class="language-plaintext highlighter-rouge">--list</code>를 쓴다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl auth can-i <span class="nt">--list</span> <span class="se">\</span>
  <span class="nt">--as</span><span class="o">=</span>system:serviceaccount:rbac-lab:pod-viewer <span class="nt">-n</span> rbac-lab
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Resources   Non-Resource URLs   Resource Names   Verbs
pods        []                  []               [get list watch]
...(모든 인증된 주체에게 주어지는 기본 권한 행은 생략)
</code></pre></div></div> <p>참고로 <code class="language-plaintext highlighter-rouge">kubectl auth can-i get pods --subresource=log</code>처럼 <code class="language-plaintext highlighter-rouge">--subresource</code> 플래그를 주면 Pod 로그 같은 서브리소스 권한도 검사할 수 있다. <code class="language-plaintext highlighter-rouge">kubectl logs</code>가 되려면 <code class="language-plaintext highlighter-rouge">pods</code> 권한이 아니라 <code class="language-plaintext highlighter-rouge">pods/log</code> 권한이 필요하기 때문에, 읽기 전용 계정을 설계할 때 자주 쓰게 된다.</p> <p>확인이 끝났으면 정리한다. 네임스페이스를 지우면 그 안의 SA, Role, RoleBinding이 함께 삭제된다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete namespace rbac-lab
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>namespace "rbac-lab" deleted
</code></pre></div></div> <hr/> <h1 id="6-실무-팁--최소-권한-원칙">6. 실무 팁 — 최소 권한 원칙</h1> <p>RBAC의 문법보다 중요한 것은 운영 습관이다.</p> <ol> <li><strong>최소 권한 원칙(Principle of Least Privilege).</strong> 지금 필요한 동사·리소스·네임스페이스만 허용한다. “일단 넓게 주고 나중에 줄이자”는 대부분 영원히 안 줄어든다. <code class="language-plaintext highlighter-rouge">verbs: ["*"]</code>나 <code class="language-plaintext highlighter-rouge">resources: ["*"]</code>는 정말 필요한지 세 번 의심할 것.</li> <li><strong><code class="language-plaintext highlighter-rouge">cluster-admin</code>을 아무에게나 바인딩하지 않는다.</strong> 특히 앱 Pod의 ServiceAccount에 cluster-admin을 주는 것은 “그 Pod가 뚫리면 클러스터 전체가 뚫린다”와 같은 말이다. CI/CD 도구나 대시보드에 습관처럼 cluster-admin을 주는 사례가 흔한데, 필요한 네임스페이스에 <code class="language-plaintext highlighter-rouge">edit</code> 정도로 시작하는 편이 낫다.</li> <li><strong><code class="language-plaintext highlighter-rouge">default</code> SA에는 권한을 부여하지 않는다.</strong> 네임스페이스의 모든 “SA를 지정하지 않은 Pod”가 그 권한을 물려받게 된다. API 접근이 필요한 워크로드마다 전용 SA를 만들어 명시적으로 연결하자.</li> <li><strong>토큰이 필요 없는 Pod는 <code class="language-plaintext highlighter-rouge">automountServiceAccountToken: false</code>.</strong> API를 안 쓰는 웹 서버 Pod에 토큰이 마운트되어 있으면 공격 표면만 넓어진다.</li> <li><strong>기본 ClusterRole(view/edit/admin)을 재사용한다.</strong> 흔한 요구사항은 이미 잘 정의되어 있다. <code class="language-plaintext highlighter-rouge">view</code> + RoleBinding 조합이면 “이 네임스페이스 읽기 전용” 요구가 YAML 몇 줄로 끝난다. 단, <code class="language-plaintext highlighter-rouge">view</code>는 Secret을 볼 수 없다는 점은 <a href="/blog/2026/k8s-07-storage-config/">07편</a>에서 강조한 Secret 보호와 연결되는 의도된 설계다.</li> <li><strong>권한 변경 후에는 <code class="language-plaintext highlighter-rouge">kubectl auth can-i</code>로 검증한다.</strong> 배포 파이프라인에서 스모크 테스트처럼 돌려도 좋다. yes/no 출력과 종료 코드로 확인할 수 있어 스크립트에 넣기도 쉽다.</li> </ol> <hr/> <h1 id="7-마무리">7. 마무리</h1> <table> <thead> <tr> <th>개념</th> <th>한 줄 요약</th> </tr> </thead> <tbody> <tr> <td>인증 vs 인가</td> <td>“너 누구야?”(401) 다음에 “이거 해도 돼?”(403) — RBAC은 인가 담당</td> </tr> <tr> <td>ServiceAccount</td> <td>Pod의 신분증. 네임스페이스마다 <code class="language-plaintext highlighter-rouge">default</code> SA 자동 생성, 토큰은 자동 마운트</td> </tr> <tr> <td>Role / ClusterRole</td> <td>권한 정의서 — 네임스페이스 범위 vs 클러스터 범위</td> </tr> <tr> <td>RoleBinding / ClusterRoleBinding</td> <td>권한 정의서를 주체(User/Group/SA)에게 연결. roleRef는 불변</td> </tr> <tr> <td>rules</td> <td>apiGroups(어느 그룹) + resources(어떤 리소스) + verbs(어떤 동작)</td> </tr> <tr> <td>kubectl auth can-i</td> <td><code class="language-plaintext highlighter-rouge">--as=system:serviceaccount:&lt;ns&gt;:&lt;name&gt;</code>로 임의 주체의 권한을 yes/no 검증</td> </tr> <tr> <td>운영 원칙</td> <td>화이트리스트뿐인 RBAC — 최소 권한, cluster-admin 남용 금지</td> </tr> </tbody> </table> <p>Kubernetes 보안의 첫 단추는 화려한 도구가 아니라 “필요한 만큼만 허용한다”는 원칙과, 그것을 코드(YAML)로 표현하는 RBAC이다. 이제 내장 리소스는 대부분 훑었다. 다음 글에서는 시리즈의 마지막 주제로, Kubernetes에 <strong>우리만의 리소스 타입</strong>을 추가하는 CRD와 Operator, 그리고 그 생태계를 살펴본다.</p> <blockquote> <p>이전 글: <a href="/blog/2026/k8s-07-storage-config/">07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</a></p> <p>다음 글 <a href="/blog/2026/k8s-09-operator-cncf/">09: 확장과 생태계 — Operator와 CNCF Projects</a>에서는 CRD와 Operator로 Kubernetes를 확장하는 방법과 CNCF 프로젝트 생태계를 다루며 시리즈를 마무리한다.</p> </blockquote> <hr/> <h1 id="참고-문헌">참고 문헌</h1> <ul> <li><a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua, SK Devocean, 2024) — 시리즈 로드맵 출처</li> <li><a href="https://kubernetes.io/docs/reference/access-authn-authz/rbac/">Using RBAC Authorization — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/reference/access-authn-authz/authorization/">Authorization — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/concepts/security/controlling-access/">Controlling Access to the Kubernetes API — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/tasks/configure-pod-container/configure-service-account/">Configure Service Accounts for Pods — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/reference/kubectl/generated/kubectl_auth/kubectl_auth_can-i/">kubectl auth can-i — Kubernetes 공식 레퍼런스</a></li> <li><a href="https://kubernetes.io/docs/reference/kubernetes-api/authorization-resources/cluster-role-binding-v1/">ClusterRoleBinding API Reference — Kubernetes 공식 문서</a></li> </ul>]]></content><author><name></name></author><category term="infra"/><category term="kubernetes"/><category term="infra"/><category term="rbac"/><category term="serviceaccount"/><category term="security"/><summary type="html"><![CDATA[인증과 인가의 차이부터 ServiceAccount, Role/RoleBinding 4요소, kubectl auth can-i 실습까지 — Kubernetes RBAC 입문]]></summary></entry><entry><title type="html">Kubernetes 스토리지와 설정 — PV/PVC, ConfigMap, Secret</title><link href="https://www.wonbeomjang.kr/blog/2026/k8s-07-storage-config/" rel="alternate" type="text/html" title="Kubernetes 스토리지와 설정 — PV/PVC, ConfigMap, Secret"/><published>2026-07-06T22:00:00+00:00</published><updated>2026-07-06T22:00:00+00:00</updated><id>https://www.wonbeomjang.kr/blog/2026/k8s-07-storage-config</id><content type="html" xml:base="https://www.wonbeomjang.kr/blog/2026/k8s-07-storage-config/"><![CDATA[<blockquote> <p>이 글은 <strong>K8s 입문 시리즈</strong>의 일곱 번째 글이다.</p> <ul> <li><a href="/blog/2026/k8s-01-history/">01: Kubernetes의 탄생 — Google Borg에서 CNCF까지</a></li> <li><a href="/blog/2026/k8s-02-local-setup/">02: 내 노트북에 클러스터 만들기 — kind와 kubectl</a></li> <li><a href="/blog/2026/k8s-03-architecture/">03: Kubernetes 아키텍처 — Control Plane과 Node</a></li> <li><a href="/blog/2026/k8s-04-pod/">04: Pod의 모든 것 — 생성부터 스케줄링까지</a></li> <li><a href="/blog/2026/k8s-05-workloads/">05: 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</a></li> <li><a href="/blog/2026/k8s-06-networking/">06: 네트워킹 — Service와 Ingress</a></li> <li><strong>07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</strong> ← 현재 글</li> <li><a href="/blog/2026/k8s-08-rbac/">08: 권한 관리 — ServiceAccount와 RBAC</a></li> <li><a href="/blog/2026/k8s-09-operator-cncf/">09: 확장과 생태계 — Operator와 CNCF Projects</a></li> </ul> <p>이 시리즈의 커리큘럼은 SK Devocean의 <a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua) 글의 학습 로드맵을 바탕으로 구성했다.</p> </blockquote> <h1 id="1-왜-스토리지와-설정을-따로-다뤄야-할까">1. 왜 스토리지와 설정을 따로 다뤄야 할까</h1> <p><a href="/blog/2026/k8s-06-networking/">06편</a>까지 왔으면 앱을 띄우고(Deployment) 트래픽을 연결하는 것(Service, Ingress)까지 할 수 있다. 하지만 실제 서비스를 올리려면 두 가지 문제가 남아 있다.</p> <p><strong>문제 1: 컨테이너 파일시스템은 휘발성이다.</strong> 컨테이너 안에서 쓴 파일은 컨테이너의 쓰기 계층에만 존재한다. <a href="/blog/2026/k8s-04-pod/">04편</a>에서 봤듯 Pod는 언제든 죽고 다시 만들어진다 — 노드 장애로, 롤링 업데이트로, 스케일 다운으로. 새 Pod는 이미지 그대로의 깨끗한 상태에서 시작하므로, 이전 Pod가 쓴 파일은 전부 사라진다. 호텔 방과 같다. 체크아웃하면 다음 손님을 위해 방이 초기화되고, 두고 나온 짐은 돌아오지 않는다. 데이터베이스를 아무 대책 없이 Pod에 띄우면 재배포 한 번에 데이터가 증발한다는 뜻이다.</p> <p><strong>문제 2: 설정을 이미지에 구우면 안 된다.</strong> DB 주소, 로그 레벨 같은 설정을 코드나 이미지에 하드코딩하면, dev/staging/prod 환경마다 이미지를 다시 빌드해야 한다. 같은 옷인데 상표만 바꾸려고 옷을 통째로 새로 만드는 격이다. 비밀번호는 더 심각하다. 이미지 레이어에 박힌 비밀번호는 그 이미지를 받을 수 있는 모든 사람에게 노출된다. “하나의 이미지를 빌드해서 모든 환경에 배포하고, 환경별 차이는 밖에서 주입한다”가 컨테이너 시대의 원칙이다.</p> <p>Kubernetes는 이 두 문제를 각각 다른 리소스로 푼다.</p> <table> <thead> <tr> <th>문제</th> <th>해결 리소스</th> <th>핵심 아이디어</th> </tr> </thead> <tbody> <tr> <td>데이터가 사라진다</td> <td>PV / PVC / StorageClass</td> <td>스토리지를 Pod 밖의 독립 리소스로 분리</td> </tr> <tr> <td>설정이 이미지에 박힌다</td> <td>ConfigMap</td> <td>설정을 별도 오브젝트로 만들어 주입</td> </tr> <tr> <td>비밀값이 노출된다</td> <td>Secret</td> <td>민감 정보를 전용 오브젝트로 분리 관리</td> </tr> </tbody> </table> <hr/> <h1 id="2-pv-pvc-storageclass--스토리지-3계층-추상화">2. PV, PVC, StorageClass — 스토리지 3계층 추상화</h1> <p>Kubernetes는 “디스크”를 세 개의 오브젝트로 쪼개서 추상화한다. 처음엔 과해 보이지만, 각 층이 서로 다른 사람의 관심사를 담당한다는 걸 알면 자연스럽다.</p> <ul> <li><strong>PV(PersistentVolume)</strong>: 클러스터에 준비된 실제 스토리지 한 조각. AWS EBS든 NFS든 노드의 로컬 디스크든, “여기 쓸 수 있는 디스크가 있다”를 표현하는 클러스터 수준 리소스다. 보통 관리자의 영역이다.</li> <li><strong>PVC(PersistentVolumeClaim)</strong>: 사용자가 내는 요청서. “ReadWriteOnce로 1Gi”처럼 원하는 스펙만 적는다. 그 디스크가 EBS인지 NFS인지는 몰라도 된다.</li> <li><strong>StorageClass</strong>: PVC가 들어오면 조건에 맞는 PV를 <strong>자동으로 만들어주는</strong> 템플릿. 프로비저너(provisioner)가 실제 생성을 담당한다.</li> </ul> <p>회사 비품에 비유하면 이렇다. PV는 총무팀이 창고에 쌓아둔 노트북, PVC는 직원이 내는 비품 신청서다. 직원은 “메모리 16GB 이상 노트북 1대”라고만 쓰면 되고, 그게 어느 브랜드인지는 총무팀이 알아서 매칭한다. StorageClass는 여기에 자판기를 더한 것이다. 신청서를 넣으면 창고 재고를 뒤지는 대신 그 자리에서 새 노트북을 주문해서 준다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-07-storage-config/pv-pvc-storageclass-480.webp 480w,/assets/post/image/k8s-07-storage-config/pv-pvc-storageclass-800.webp 800w,/assets/post/image/k8s-07-storage-config/pv-pvc-storageclass-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-07-storage-config/pv-pvc-storageclass.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="개발자 영역의 Pod와 PVC, 관리자 영역의 StorageClass와 PV, 그리고 실제 스토리지로 이어지는 3계층 추상화 구조" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>개발자는 PVC만 만들고, Pod는 PVC 이름만 참조한다. 실제 디스크 종류는 개발자에게 보이지 않는다.</li> <li>PV와 PVC는 1:1로 <strong>바인딩</strong>된다. 요청서 하나에 디스크 하나.</li> <li>StorageClass가 있으면 관리자가 PV를 미리 만들어 둘 필요가 없다. 이것이 <strong>동적 프로비저닝</strong>이다.</li> </ul> <h2 id="정적-프로비저닝-vs-동적-프로비저닝">정적 프로비저닝 vs 동적 프로비저닝</h2> <table> <thead> <tr> <th>구분</th> <th>정적(Static)</th> <th>동적(Dynamic)</th> </tr> </thead> <tbody> <tr> <td>PV 생성</td> <td>관리자가 미리 수동으로 생성</td> <td>StorageClass가 PVC 요청 시 자동 생성</td> </tr> <tr> <td>매칭 방식</td> <td>기존 PV 중 조건(크기, 모드)에 맞는 것 바인딩</td> <td>요청 스펙 그대로 새 PV를 만들어 바인딩</td> </tr> <tr> <td>관리 부담</td> <td>요청량 예측 실패 시 PV 부족/낭비</td> <td>프로비저너만 세팅하면 이후 자동</td> </tr> <tr> <td>주 사용처</td> <td>기존 스토리지(NFS 서버 등) 연결</td> <td>클라우드, 로컬 개발 환경 대부분</td> </tr> </tbody> </table> <p>요즘은 대부분 동적 프로비저닝을 쓴다. 사용자가 PVC를 내는 순간 StorageClass에 지정된 프로비저너가 실제 스토리지와 PV를 만들어 바인딩까지 끝내는 흐름이다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-07-storage-config/dynamic-provisioning-seq-480.webp 480w,/assets/post/image/k8s-07-storage-config/dynamic-provisioning-seq-800.webp 800w,/assets/post/image/k8s-07-storage-config/dynamic-provisioning-seq-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-07-storage-config/dynamic-provisioning-seq.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="동적 프로비저닝 시퀀스 — 사용자의 PVC 생성부터 프로비저너의 PV 생성과 바인딩, kubelet의 볼륨 마운트까지" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li><code class="language-plaintext highlighter-rouge">volumeBindingMode: WaitForFirstConsumer</code>는 “누가 쓸지 정해질 때까지 디스크를 만들지 않는다”는 옵션이다. 노드에 붙는 로컬 스토리지라면 Pod가 어느 노드에 뜰지 먼저 알아야 그 노드에 디스크를 만들 수 있기 때문이다. 잠시 뒤 실습에서 이 동작을 직접 보게 된다.</li> </ul> <hr/> <h1 id="3-accessmodes와-reclaimpolicy--요청서에-적는-두-가지-조건">3. accessModes와 reclaimPolicy — 요청서에 적는 두 가지 조건</h1> <p>PVC 요청서에서 가장 중요한 항목이 접근 모드(accessModes)다. 공식 문서 기준으로 네 가지가 있다.</p> <table> <thead> <tr> <th>모드</th> <th>약어</th> <th>의미</th> </tr> </thead> <tbody> <tr> <td>ReadWriteOnce</td> <td>RWO</td> <td><strong>하나의 노드</strong>에서 읽기/쓰기로 마운트</td> </tr> <tr> <td>ReadOnlyMany</td> <td>ROX</td> <td><strong>여러 노드</strong>에서 읽기 전용으로 마운트</td> </tr> <tr> <td>ReadWriteMany</td> <td>RWX</td> <td><strong>여러 노드</strong>에서 읽기/쓰기로 마운트</td> </tr> <tr> <td>ReadWriteOncePod</td> <td>RWOP</td> <td><strong>단 하나의 Pod</strong>에서만 읽기/쓰기 (CSI 볼륨 전용, v1.29에서 GA)</td> </tr> </tbody> </table> <p>여기서 가장 많이 하는 오해가 하나 있다. <strong>RWO는 “Pod 하나”가 아니라 “노드 하나” 제한이다.</strong> 같은 노드에 뜬 여러 Pod는 하나의 RWO 볼륨을 동시에 쓸 수 있다. “정확히 Pod 하나”를 강제하고 싶을 때 쓰라고 나온 것이 ReadWriteOncePod다. 또 하나, 어떤 모드를 실제로 지원하는지는 스토리지 종류에 달려 있다. 예를 들어 블록 스토리지(EBS 등)는 보통 RWX가 안 되고, NFS 같은 공유 파일시스템이 RWX의 대표 주자다.</p> <p>두 번째 조건은 <strong>reclaimPolicy</strong> — “요청서(PVC)가 반납되면 디스크를 어떻게 할까”이다.</p> <table> <thead> <tr> <th>정책</th> <th>PVC 삭제 시 동작</th> <th>비고</th> </tr> </thead> <tbody> <tr> <td>Retain</td> <td>PV와 데이터를 남긴다. 관리자가 수동으로 정리해야 재사용 가능</td> <td>소중한 데이터에 안전</td> </tr> <tr> <td>Delete</td> <td>PV와 실제 스토리지까지 삭제</td> <td>동적 프로비저닝의 기본값</td> </tr> <tr> <td>Recycle</td> <td>(사용 중단됨)</td> <td>동적 프로비저닝으로 대체</td> </tr> </tbody> </table> <p>동적 프로비저닝의 기본값이 Delete라는 점은 꼭 기억해야 한다. <strong>PVC를 지우면 데이터도 같이 사라진다.</strong> 운영 DB의 볼륨이라면 StorageClass나 PV의 정책을 Retain으로 바꿔두는 것이 안전하다.</p> <p>참고로 PV는 자기 상태를 단계(phase)로 보여준다. 아직 주인 없는 <code class="language-plaintext highlighter-rouge">Available</code>, PVC와 연결된 <code class="language-plaintext highlighter-rouge">Bound</code>, PVC가 삭제됐지만 아직 정리 전인 <code class="language-plaintext highlighter-rouge">Released</code>, 회수에 실패한 <code class="language-plaintext highlighter-rouge">Failed</code>. <code class="language-plaintext highlighter-rouge">kubectl get pv</code> 출력의 STATUS 컬럼이 바로 이것이다.</p> <hr/> <h1 id="4-실습-kind에서-pvc로-데이터-살려보기">4. 실습: kind에서 PVC로 데이터 살려보기</h1> <p><a href="/blog/2026/k8s-02-local-setup/">02편</a>에서 만든 kind 클러스터로 직접 확인해보자. 먼저 kind에 기본으로 깔려 있는 StorageClass를 본다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get sc
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                 PROVISIONER             RECLAIMPOLICY   VOLUMEBINDINGMODE      ALLOWVOLUMEEXPANSION   AGE
standard (default)   rancher.io/local-path   Delete          WaitForFirstConsumer   false                  15m
</code></pre></div></div> <p>출력을 뜯어보면 이 장의 개념이 전부 들어 있다.</p> <ul> <li>이름은 <code class="language-plaintext highlighter-rouge">standard</code>, 뒤에 <code class="language-plaintext highlighter-rouge">(default)</code>가 붙어 있다. PVC에서 storageClassName을 생략하면 이걸 쓴다는 뜻이다.</li> <li>프로비저너는 <code class="language-plaintext highlighter-rouge">rancher.io/local-path</code> — <a href="https://github.com/rancher/local-path-provisioner">local-path-provisioner</a>로, 노드의 로컬 디렉터리에 볼륨을 만들어주는 프로비저너다. 클라우드 디스크가 없는 로컬 환경에서 동적 프로비저닝을 흉내 내기에 딱 좋다. 단, 기본 설정에서는 ReadWriteOnce만 지원한다.</li> <li>RECLAIMPOLICY는 <code class="language-plaintext highlighter-rouge">Delete</code>, VOLUMEBINDINGMODE는 <code class="language-plaintext highlighter-rouge">WaitForFirstConsumer</code>다. 위에서 배운 그대로다.</li> </ul> <p>이제 PVC를 만든다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># pvc.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">PersistentVolumeClaim</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">data-pvc</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">accessModes</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">ReadWriteOnce</span>
  <span class="na">resources</span><span class="pi">:</span>
    <span class="na">requests</span><span class="pi">:</span>
      <span class="na">storage</span><span class="pi">:</span> <span class="s">1Gi</span>
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> pvc.yaml
kubectl get pvc
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME       STATUS    VOLUME   CAPACITY   ACCESS MODES   STORAGECLASS   AGE
data-pvc   Pending                                      standard       5s
</code></pre></div></div> <p>STATUS가 <code class="language-plaintext highlighter-rouge">Pending</code>이다. 고장이 아니다. <code class="language-plaintext highlighter-rouge">WaitForFirstConsumer</code> 때문에 <strong>이 PVC를 쓰는 Pod가 나타날 때까지</strong> 볼륨 생성을 미루고 있는 것이다. <code class="language-plaintext highlighter-rouge">kubectl describe pvc data-pvc</code>를 보면 “waiting for first consumer to be created before binding”이라는 이벤트를 확인할 수 있다. 그럼 소비자를 만들어주자.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># storage-test.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">storage-test</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">app</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">busybox:1.36</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">sh"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">-c"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">sleep</span><span class="nv"> </span><span class="s">3600"</span><span class="pi">]</span>
      <span class="na">volumeMounts</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">data</span>
          <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/data</span>
  <span class="na">volumes</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">data</span>
      <span class="na">persistentVolumeClaim</span><span class="pi">:</span>
        <span class="na">claimName</span><span class="pi">:</span> <span class="s">data-pvc</span>
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> storage-test.yaml
kubectl get pvc,pv
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                             STATUS   VOLUME                                     CAPACITY   ACCESS MODES   STORAGECLASS   AGE
persistentvolumeclaim/data-pvc   Bound    pvc-c1f0a2...                              1Gi        RWO            standard       2m

NAME                             CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS   CLAIM              STORAGECLASS   AGE
persistentvolume/pvc-c1f0a2...   1Gi        RWO            Delete           Bound    default/data-pvc   standard       10s
</code></pre></div></div> <p>Pod가 생기자 프로비저너가 PV를 자동 생성했고(동적 프로비저닝), PVC와 PV가 서로 <code class="language-plaintext highlighter-rouge">Bound</code> 상태가 됐다. 이제 핵심 실험이다. 파일을 쓰고, <strong>Pod를 완전히 삭제한 뒤 다시 만들어서</strong> 파일이 살아있는지 본다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">exec </span>storage-test <span class="nt">--</span> sh <span class="nt">-c</span> <span class="s1">'echo "hello from pod 1" &gt; /data/hello.txt'</span>
kubectl delete pod storage-test
kubectl apply <span class="nt">-f</span> storage-test.yaml   <span class="c"># 같은 스펙으로 새 Pod 생성</span>
kubectl <span class="nb">exec </span>storage-test <span class="nt">--</span> <span class="nb">cat</span> /data/hello.txt
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hello from pod 1
</code></pre></div></div> <p>Pod는 죽었다 살아났지만 데이터는 그대로다. 데이터의 생명주기가 Pod가 아니라 <strong>PVC에 묶여 있기 때문</strong>이다. 반대로 <code class="language-plaintext highlighter-rouge">kubectl delete pvc data-pvc</code>를 실행하면 reclaimPolicy가 Delete이므로 PV와 데이터까지 함께 사라진다. <a href="/blog/2026/k8s-05-workloads/">05편</a>의 StatefulSet이 Pod마다 PVC를 하나씩 만들어주던 것도 정확히 이 성질을 이용한 것이다.</p> <hr/> <h1 id="5-configmap--설정을-이미지-밖으로">5. ConfigMap — 설정을 이미지 밖으로</h1> <p>이제 두 번째 문제, 설정이다. <strong>ConfigMap</strong>은 키-값 쌍으로 된 설정 덩어리를 담는 오브젝트다. 짧은 값(환경 변수용)도, 파일 통째(설정 파일용)도 담을 수 있다. 단, 민감하지 않은 데이터 전용이고 크기는 1MiB를 넘을 수 없다(공식 문서 기준 — 더 크면 볼륨이나 별도 저장소를 쓰라고 안내한다).</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># app-config.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ConfigMap</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">app-config</span>
<span class="na">data</span><span class="pi">:</span>
  <span class="na">APP_MODE</span><span class="pi">:</span> <span class="s2">"</span><span class="s">production"</span>
  <span class="na">LOG_LEVEL</span><span class="pi">:</span> <span class="s2">"</span><span class="s">info"</span>
  <span class="na">config.yaml</span><span class="pi">:</span> <span class="pi">|</span>
    <span class="s">server:</span>
      <span class="s">port: 8080</span>
      <span class="s">timeout: 30s</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">APP_MODE</code>처럼 짧은 키-값과 <code class="language-plaintext highlighter-rouge">config.yaml</code>처럼 파일 하나가 통째로 들어간 키가 공존한다. 이걸 Pod에 주입하는 대표적인 방법이 두 가지다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># config-test.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">config-test</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">app</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">busybox:1.36</span>
      <span class="na">command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">sh"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">-c"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">sleep</span><span class="nv"> </span><span class="s">3600"</span><span class="pi">]</span>
      <span class="na">env</span><span class="pi">:</span> <span class="c1"># 방법 1: 환경 변수로 주입</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">APP_MODE</span>
          <span class="na">valueFrom</span><span class="pi">:</span>
            <span class="na">configMapKeyRef</span><span class="pi">:</span>
              <span class="na">name</span><span class="pi">:</span> <span class="s">app-config</span>
              <span class="na">key</span><span class="pi">:</span> <span class="s">APP_MODE</span>
      <span class="na">volumeMounts</span><span class="pi">:</span> <span class="c1"># 방법 2: 파일로 마운트</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">config</span>
          <span class="na">mountPath</span><span class="pi">:</span> <span class="s">/etc/config</span>
          <span class="na">readOnly</span><span class="pi">:</span> <span class="kc">true</span>
  <span class="na">volumes</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">config</span>
      <span class="na">configMap</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">app-config</span>
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> app-config.yaml <span class="nt">-f</span> config-test.yaml
kubectl <span class="nb">exec </span>config-test <span class="nt">--</span> sh <span class="nt">-c</span> <span class="s1">'echo $APP_MODE'</span>
kubectl <span class="nb">exec </span>config-test <span class="nt">--</span> <span class="nb">ls</span> /etc/config
kubectl <span class="nb">exec </span>config-test <span class="nt">--</span> <span class="nb">cat</span> /etc/config/config.yaml
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>production
APP_MODE
LOG_LEVEL
config.yaml
server:
  port: 8080
  timeout: 30s
</code></pre></div></div> <p>환경 변수로는 <code class="language-plaintext highlighter-rouge">APP_MODE=production</code>이 들어갔고, 볼륨으로는 ConfigMap의 <strong>키 하나가 파일 하나</strong>가 되어 <code class="language-plaintext highlighter-rouge">/etc/config</code> 아래에 깔렸다. 여러 키를 한 번에 환경 변수로 넣고 싶으면 <code class="language-plaintext highlighter-rouge">envFrom.configMapRef</code>를 쓰면 된다. 두 방식의 차이는 설정이 바뀌었을 때 드러난다.</p> <table> <thead> <tr> <th>구분</th> <th>env 주입</th> <th>volume 마운트</th> </tr> </thead> <tbody> <tr> <td>형태</td> <td>환경 변수</td> <td>디렉터리 안의 파일</td> </tr> <tr> <td>ConfigMap 수정 시</td> <td>반영 안 됨 — <strong>Pod 재시작 필요</strong></td> <td>kubelet 주기 동기화로 <strong>자동 갱신</strong> (subPath 마운트는 예외)</td> </tr> <tr> <td>어울리는 용도</td> <td>플래그, 짧은 값</td> <td>설정 파일 전체, 자동 리로드가 필요한 앱</td> </tr> </tbody> </table> <p>실무에서는 “설정이 바뀌면 어차피 재배포한다”는 팀도 많아서 env 방식도 널리 쓰인다. 반대로 설정 변경을 감지해 스스로 리로드하는 앱이라면 volume 방식이 맞다. 참고로 v1.19부터는 <code class="language-plaintext highlighter-rouge">immutable: true</code>를 붙여 수정 자체를 막는 ConfigMap도 만들 수 있다. 실수 방지와 대규모 클러스터 성능에 유리하다.</p> <hr/> <h1 id="6-secret--base64는-암호화가-아니다">6. Secret — base64는 암호화가 아니다</h1> <p>비밀번호, API 키, 인증서는 <strong>Secret</strong>에 담는다. 사용법은 ConfigMap과 거의 같다(env 주입, volume 마운트 모두 가능). 그런데 Secret에 대해 반드시 짚어야 할 사실이 하나 있다.</p> <p><strong>Secret의 값은 base64로 인코딩되어 있을 뿐, 암호화된 것이 아니다.</strong> base64는 누구나 한 줄로 되돌릴 수 있는 표현 방식일 뿐이다. 자물쇠가 아니라 반투명 봉투에 가깝다. 직접 확인해보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create secret generic db-secret <span class="se">\</span>
  <span class="nt">--from-literal</span><span class="o">=</span><span class="nv">username</span><span class="o">=</span>admin <span class="se">\</span>
  <span class="nt">--from-literal</span><span class="o">=</span><span class="nv">password</span><span class="o">=</span>supersecret

kubectl get secret db-secret <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.data.password}'</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>c3VwZXJzZWNyZXQ=
</code></pre></div></div> <p>읽을 수 없는 문자열처럼 보이지만, 디코딩은 한 줄이면 끝난다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get secret db-secret <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">=</span><span class="s1">'{.data.password}'</span> | <span class="nb">base64</span> <span class="nt">-d</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>supersecret
</code></pre></div></div> <p>즉 <strong>Secret을 읽을 권한이 있는 사람은 비밀값을 그대로 읽을 수 있다.</strong> 공식 문서도 Secret이 기본적으로 etcd에 <strong>암호화되지 않은 채</strong> 저장되며, API 접근 권한이 있거나 etcd에 접근할 수 있는 누구든 값을 가져올 수 있다고 명시한다. 그래서 다음 대책을 권장한다.</p> <ul> <li><strong>etcd 저장 데이터 암호화(Encryption at Rest)</strong> 활성화 — 저장소 수준에서 암호화한다.</li> <li><strong>RBAC로 최소 권한 부여</strong> — Secret을 읽을 수 있는 주체를 최소화한다. 이게 바로 다음 편(08)의 주제다.</li> <li><strong>특정 컨테이너만 Secret에 접근하도록 제한</strong>하고, 필요하면 <strong>외부 Secret 저장소</strong>(Vault 등과 연동하는 Secrets Store CSI Driver)를 검토한다.</li> </ul> <p>그럼에도 ConfigMap 대신 Secret을 쓰는 이유는, 민감 데이터를 <strong>별도 리소스 타입으로 격리</strong>해두면 RBAC로 권한을 따로 조일 수 있고 위 같은 보호 장치를 걸 대상이 명확해지기 때문이다.</p> <h2 id="secret-타입">Secret 타입</h2> <p>Secret에는 용도를 나타내는 타입이 있다. 공식 문서의 내장 타입 목록은 다음과 같다.</p> <table> <thead> <tr> <th>타입</th> <th>용도</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">Opaque</code></td> <td>임의의 사용자 정의 데이터 (기본값)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubernetes.io/service-account-token</code></td> <td>ServiceAccount 토큰</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubernetes.io/dockercfg</code></td> <td>직렬화된 <code class="language-plaintext highlighter-rouge">~/.dockercfg</code> 파일</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubernetes.io/dockerconfigjson</code></td> <td>직렬화된 <code class="language-plaintext highlighter-rouge">~/.docker/config.json</code> (프라이빗 레지스트리 인증)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubernetes.io/basic-auth</code></td> <td>Basic 인증 자격 증명 (username/password)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubernetes.io/ssh-auth</code></td> <td>SSH 인증 자격 증명</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubernetes.io/tls</code></td> <td>TLS 클라이언트/서버용 인증서와 키</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">bootstrap.kubernetes.io/token</code></td> <td>노드 부트스트랩 토큰</td> </tr> </tbody> </table> <p>일상적으로 가장 자주 만나는 것은 세 가지다. 일반 비밀값은 <code class="language-plaintext highlighter-rouge">Opaque</code>, 프라이빗 레지스트리에서 이미지를 당길 때는 <code class="language-plaintext highlighter-rouge">kubernetes.io/dockerconfigjson</code>(imagePullSecrets로 연결), <a href="/blog/2026/k8s-06-networking/">06편</a>의 Ingress에 HTTPS를 붙일 때는 <code class="language-plaintext highlighter-rouge">kubernetes.io/tls</code>다. 타입을 지정하면 필수 키가 있는지 Kubernetes가 검증해준다는 부수 효과도 있다. YAML로 Secret을 만들 때는 <code class="language-plaintext highlighter-rouge">data</code>(base64 인코딩 필요) 대신 <code class="language-plaintext highlighter-rouge">stringData</code> 필드에 평문을 적으면 인코딩을 대신 해준다.</p> <h2 id="configmap-vs-secret">ConfigMap vs Secret</h2> <table> <thead> <tr> <th>항목</th> <th>ConfigMap</th> <th>Secret</th> </tr> </thead> <tbody> <tr> <td>담는 것</td> <td>일반 설정 (URL, 플래그, 설정 파일)</td> <td>민감 정보 (비밀번호, 토큰, 인증서)</td> </tr> <tr> <td>값 저장</td> <td>평문</td> <td>base64 인코딩 (암호화 아님)</td> </tr> <tr> <td>타입 구분</td> <td>없음</td> <td>Opaque, tls, dockerconfigjson 등 내장 타입</td> </tr> <tr> <td>추가 보호</td> <td>해당 없음</td> <td>etcd 암호화, RBAC 최소 권한, 외부 저장소 연동</td> </tr> <tr> <td>주입 방식</td> <td>env, volume</td> <td>env, volume (동일)</td> </tr> </tbody> </table> <p>판단 기준은 단순하다. <strong>“이 값이 유출되면 사고인가?”</strong> — 그렇다면 Secret, 아니라면 ConfigMap이다.</p> <hr/> <h1 id="7-마무리">7. 마무리</h1> <p>이번 편의 핵심을 요약한다.</p> <table> <thead> <tr> <th>개념</th> <th>한 줄 요약</th> </tr> </thead> <tbody> <tr> <td>PV</td> <td>클러스터에 준비된 실제 스토리지 한 조각 (관리자/시스템 영역)</td> </tr> <tr> <td>PVC</td> <td>“이런 스토리지가 필요하다”는 사용자의 요청서, Pod는 이것만 참조</td> </tr> <tr> <td>StorageClass</td> <td>PVC 요청 시 PV를 자동으로 만들어주는 동적 프로비저닝 템플릿</td> </tr> <tr> <td>accessModes</td> <td>RWO(노드 1개)/ROX/RWX/RWOP(Pod 1개) — “노드” 기준임에 주의</td> </tr> <tr> <td>reclaimPolicy</td> <td>동적 프로비저닝 기본값은 Delete — PVC 삭제 시 데이터도 삭제</td> </tr> <tr> <td>ConfigMap</td> <td>일반 설정을 이미지 밖으로 — env 주입 또는 파일 마운트</td> </tr> <tr> <td>Secret</td> <td>민감 정보 전용 — base64는 인코딩일 뿐, RBAC·etcd 암호화로 보호</td> </tr> </tbody> </table> <p>데이터는 PVC로 Pod보다 오래 살게 만들었고, 설정과 비밀값은 이미지 밖으로 꺼냈다. 그런데 마지막 실습에서 찜찜한 대목이 있었다. <code class="language-plaintext highlighter-rouge">kubectl get secret</code> 한 줄이면 누구든 비밀번호를 읽을 수 있다면, <strong>“누가 무엇을 할 수 있는지”는 어떻게 통제할까?</strong></p> <blockquote> <p>다음 글 <a href="/blog/2026/k8s-08-rbac/">08: 권한 관리 — ServiceAccount와 RBAC</a>에서는 Pod의 신원인 ServiceAccount와, Role/RoleBinding으로 권한을 최소화하는 RBAC를 다룬다. Secret을 지키는 첫 번째 방어선이다.</p> </blockquote> <hr/> <h1 id="참고-문헌">참고 문헌</h1> <ul> <li><a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua, SK Devocean, 2024) — 시리즈 로드맵 출처</li> <li><a href="https://kubernetes.io/docs/concepts/storage/persistent-volumes/">Kubernetes 공식 문서 — Persistent Volumes</a> (accessModes, reclaimPolicy, PV 단계)</li> <li><a href="https://kubernetes.io/docs/concepts/storage/storage-classes/">Kubernetes 공식 문서 — Storage Classes</a></li> <li><a href="https://kubernetes.io/docs/concepts/configuration/configmap/">Kubernetes 공식 문서 — ConfigMaps</a></li> <li><a href="https://kubernetes.io/docs/concepts/configuration/secret/">Kubernetes 공식 문서 — Secrets</a> (Secret 타입 목록, 보안 권장 사항)</li> <li><a href="https://github.com/kubernetes-sigs/kind/blob/main/pkg/build/nodeimage/const_storage.go">kind 소스 코드 — 기본 StorageClass 정의 (const_storage.go)</a></li> <li><a href="https://github.com/rancher/local-path-provisioner">rancher/local-path-provisioner</a></li> </ul>]]></content><author><name></name></author><category term="infra"/><category term="kubernetes"/><category term="infra"/><category term="storage"/><category term="pv"/><category term="pvc"/><category term="configmap"/><category term="secret"/><summary type="html"><![CDATA[컨테이너의 휘발성 문제를 푸는 PV/PVC/StorageClass 3계층 추상화, 그리고 설정과 비밀값을 이미지에서 분리하는 ConfigMap·Secret을 kind 실습으로 익힌다]]></summary></entry><entry><title type="html">Kubernetes 네트워킹 — Service와 Ingress</title><link href="https://www.wonbeomjang.kr/blog/2026/k8s-06-networking/" rel="alternate" type="text/html" title="Kubernetes 네트워킹 — Service와 Ingress"/><published>2026-07-06T21:00:00+00:00</published><updated>2026-07-06T21:00:00+00:00</updated><id>https://www.wonbeomjang.kr/blog/2026/k8s-06-networking</id><content type="html" xml:base="https://www.wonbeomjang.kr/blog/2026/k8s-06-networking/"><![CDATA[<blockquote> <p>이 글은 <strong>K8s 입문 시리즈</strong>의 여섯 번째 글이다.</p> <ul> <li><a href="/blog/2026/k8s-01-history/">01: Kubernetes의 탄생 — Google Borg에서 CNCF까지</a></li> <li><a href="/blog/2026/k8s-02-local-setup/">02: 내 노트북에 클러스터 만들기 — kind와 kubectl</a></li> <li><a href="/blog/2026/k8s-03-architecture/">03: Kubernetes 아키텍처 — Control Plane과 Node</a></li> <li><a href="/blog/2026/k8s-04-pod/">04: Pod의 모든 것 — 생성부터 스케줄링까지</a></li> <li><a href="/blog/2026/k8s-05-workloads/">05: 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</a></li> <li><strong>06: 네트워킹 — Service와 Ingress</strong> ← 현재 글</li> <li><a href="/blog/2026/k8s-07-storage-config/">07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</a></li> <li><a href="/blog/2026/k8s-08-rbac/">08: 권한 관리 — ServiceAccount와 RBAC</a></li> <li><a href="/blog/2026/k8s-09-operator-cncf/">09: 확장과 생태계 — Operator와 CNCF Projects</a></li> </ul> <p>이 시리즈의 커리큘럼은 SK Devocean의 <a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua) 글의 학습 로드맵을 바탕으로 구성했다.</p> </blockquote> <h1 id="1-들어가며--pod-ip는-믿을-수-없다">1. 들어가며 — Pod IP는 믿을 수 없다</h1> <p><a href="/blog/2026/k8s-05-workloads/">05편</a>에서 Deployment로 nginx Pod를 여러 개 띄웠다. 그런데 곧바로 문제가 생긴다. <strong>이 Pod들에 어떻게 접속해야 할까?</strong></p> <p>Pod는 각자 클러스터 내부 IP를 하나씩 받는다. 문제는 이 IP가 <strong>일회용</strong>이라는 것이다. Pod가 죽고 ReplicaSet이 새 Pod를 만들면, 새 Pod는 새 IP를 받는다. 직접 확인해 보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl create deployment web <span class="nt">--image</span><span class="o">=</span>nginx <span class="nt">--replicas</span><span class="o">=</span>2
deployment.apps/web created

<span class="nv">$ </span>kubectl get pods <span class="nt">-o</span> wide
NAME                   READY   STATUS    RESTARTS   AGE   IP           NODE
web-5f9b6b7d4b-8xk2p   1/1     Running   0          20s   10.244.0.5   kind-control-plane
web-5f9b6b7d4b-tq7wn   1/1     Running   0          20s   10.244.0.6   kind-control-plane

<span class="nv">$ </span>kubectl delete pod web-5f9b6b7d4b-8xk2p
pod <span class="s2">"web-5f9b6b7d4b-8xk2p"</span> deleted

<span class="nv">$ </span>kubectl get pods <span class="nt">-o</span> wide
NAME                   READY   STATUS    RESTARTS   AGE   IP           NODE
web-5f9b6b7d4b-tq7wn   1/1     Running   0          2m    10.244.0.6   kind-control-plane
web-5f9b6b7d4b-zc4rd   1/1     Running   0          10s   10.244.0.7   kind-control-plane
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">10.244.0.5</code>였던 Pod가 사라지고 <code class="language-plaintext highlighter-rouge">10.244.0.7</code>을 가진 새 Pod가 생겼다. 만약 다른 애플리케이션이 <code class="language-plaintext highlighter-rouge">10.244.0.5</code>를 하드코딩해서 호출하고 있었다면 그대로 장애다. 게다가 롤링 업데이트 한 번이면 <strong>모든 Pod의 IP가 전부 갈아치워진다</strong>.</p> <p>일상 비유로 보면 이렇다. 어떤 회사의 담당 직원은 계속 바뀌고, 바뀔 때마다 휴대폰 번호도 바뀐다. 고객이 직원 개인 번호를 외워서 전화하는 방식은 지속 불가능하다. 해법은 회사가 <strong>대표 전화번호</strong> 하나를 만들고, 교환대가 지금 근무 중인 직원에게 전화를 돌려주는 것이다.</p> <p>Kubernetes에서 이 대표번호가 <strong>Service</strong>이고, “지금 근무 중인 직원 명단”을 관리하는 것이 <strong>EndpointSlice</strong>, 실제로 전화를 돌려주는 교환대가 <strong>kube-proxy</strong>다. 그리고 회사 건물 1층에서 “어떤 용건이신가요?”를 묻고 부서별로 안내하는 안내 데스크가 <strong>Ingress</strong>다. 이번 편에서 이 넷을 전부 다룬다.</p> <hr/> <h1 id="2-service의-동작-원리--selector-endpointslice-kube-proxy">2. Service의 동작 원리 — Selector, EndpointSlice, kube-proxy</h1> <p>Service는 “고정된 가상 IP(ClusterIP)와 DNS 이름”을 제공하고, 그 뒤에 있는 Pod 집합으로 트래픽을 분산한다. 핵심 질문은 두 가지다.</p> <p><strong>첫째, Service는 어떤 Pod에게 트래픽을 보낼지 어떻게 아는가?</strong> 답은 05편에서 배운 <strong>Label Selector</strong>다. Service는 <code class="language-plaintext highlighter-rouge">selector</code>에 적힌 라벨과 일치하는 Pod를 계속 추적한다. Pod가 죽든 새로 생기든, 라벨만 맞으면 자동으로 목록에 반영된다.</p> <p><strong>둘째, 추적한 결과는 어디에 저장되는가?</strong> Kubernetes는 Selector와 일치하는 Pod들의 IP·포트 목록을 <strong>EndpointSlice</strong>라는 별도 리소스로 만들어 관리한다(과거에는 Endpoints 리소스 하나에 다 담았는데, Pod가 수천 개면 객체가 너무 커져서 여러 조각(slice)으로 나눈 EndpointSlice가 도입됐다). Service는 “대표번호”라는 선언이고, EndpointSlice는 “지금 이 번호로 연결 가능한 실제 직원 명단”이다.</p> <p>마지막 퍼즐은 <a href="/blog/2026/k8s-03-architecture/">03편</a>에서 본 <strong>kube-proxy</strong>다. 각 노드의 kube-proxy는 Service와 EndpointSlice의 변화를 감시하다가, 노드의 iptables/IPVS 규칙을 갱신한다. 그래서 어떤 Pod가 ClusterIP로 패킷을 보내면, 그 패킷은 kube-proxy가 미리 깔아둔 규칙에 따라 실제 Pod IP 중 하나로 바꿔치기되어 전달된다. ClusterIP는 어떤 네트워크 인터페이스에도 붙어 있지 않은 <strong>가상 IP</strong>라는 점이 중요하다 — ping은 안 되지만 규칙에 의해 라우팅은 된다.</p> <p>Service를 만들고 이 연결 고리를 직접 확인해 보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl expose deployment web <span class="nt">--port</span><span class="o">=</span>80
service/web exposed

<span class="nv">$ </span>kubectl get svc web
NAME   TYPE        CLUSTER-IP     EXTERNAL-IP   PORT<span class="o">(</span>S<span class="o">)</span>   AGE
web    ClusterIP   10.96.156.10   &lt;none&gt;        80/TCP    8s

<span class="nv">$ </span>kubectl get endpointslices
NAME         ADDRESSTYPE   PORTS   ENDPOINTS               AGE
web-abc12    IPv4          80      10.244.0.6,10.244.0.7   8s
</code></pre></div></div> <p>EndpointSlice의 ENDPOINTS 목록이 정확히 현재 Pod들의 IP와 일치한다. 여기서 Pod 하나를 지우면, 잠시 후 EndpointSlice의 목록도 새 Pod IP로 자동 갱신된다. <strong>개발자는 명단을 관리할 필요가 없다</strong> — 라벨만 잘 붙이면 나머지는 컨트롤 플레인이 reconcile한다.</p> <hr/> <h1 id="3-service-3종-세트--clusterip-nodeport-loadbalancer">3. Service 3종 세트 — ClusterIP, NodePort, LoadBalancer</h1> <p>Service에는 “누가 접근할 수 있느냐”에 따라 세 가지 대표 타입이 있다. 먼저 비교표로 감을 잡자.</p> <table> <thead> <tr> <th>타입</th> <th>접근 범위</th> <th>접근 방법</th> <th>주 용도</th> </tr> </thead> <tbody> <tr> <td><strong>ClusterIP</strong></td> <td>클러스터 내부 전용 (기본값)</td> <td>ClusterIP 또는 DNS 이름</td> <td>마이크로서비스 간 내부 통신</td> </tr> <tr> <td><strong>NodePort</strong></td> <td>클러스터 외부 가능</td> <td><code class="language-plaintext highlighter-rouge">노드IP:노드포트</code> (기본 30000-32767)</td> <td>개발/테스트, 외부 LB를 직접 붙일 때</td> </tr> <tr> <td><strong>LoadBalancer</strong></td> <td>클러스터 외부 가능</td> <td>클라우드가 발급한 외부 IP</td> <td>클라우드 환경에서 프로덕션 서비스 노출</td> </tr> </tbody> </table> <p>세 타입은 배타적이 아니라 <strong>포개진 구조</strong>다. NodePort를 만들면 ClusterIP도 함께 생기고, LoadBalancer를 만들면 NodePort와 ClusterIP가 함께 생긴다. 이 외에 클러스터 외부 도메인으로 CNAME을 돌려주는 ExternalName 타입도 있지만, 입문 단계에서는 위 세 가지만 확실히 잡으면 된다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-06-networking/service-types-480.webp 480w,/assets/post/image/k8s-06-networking/service-types-800.webp 800w,/assets/post/image/k8s-06-networking/service-types-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-06-networking/service-types.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="클러스터 외부의 사용자가 LoadBalancer, NodePort, ClusterIP를 거쳐 Pod에 도달하고, 내부 Pod는 DNS로 Service에 직행하는 구조" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>외부 트래픽은 LoadBalancer → NodePort → ClusterIP 순으로 <strong>안쪽 계층에 올라탄다</strong>.</li> <li>내부 트래픽은 처음부터 ClusterIP(DNS 이름)로 직행한다.</li> <li>어느 경로로 들어오든 마지막 분산은 kube-proxy가 깔아둔 규칙이 담당한다.</li> </ul> <h2 id="31-clusterip--클러스터-내부의-대표번호와-dns">3.1 ClusterIP — 클러스터 내부의 대표번호와 DNS</h2> <p>위에서 만든 <code class="language-plaintext highlighter-rouge">web</code> Service가 바로 기본값인 ClusterIP 타입이다. 그런데 <code class="language-plaintext highlighter-rouge">10.96.156.10</code> 같은 ClusterIP도 결국 클러스터를 다시 만들면 바뀔 수 있는 값이다. 그래서 실무에서는 IP가 아니라 <strong>DNS 이름</strong>으로 접근한다.</p> <p>Kubernetes의 클러스터 DNS(kind에서는 CoreDNS가 <code class="language-plaintext highlighter-rouge">kube-system</code>에 떠 있다)는 모든 Service에 다음 형식의 도메인을 만들어 준다.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;서비스 이름&gt;.&lt;네임스페이스&gt;.svc.&lt;클러스터 도메인&gt;
</code></pre></div></div> <p>클러스터 도메인의 기본값은 <code class="language-plaintext highlighter-rouge">cluster.local</code>이다. 즉 <code class="language-plaintext highlighter-rouge">default</code> 네임스페이스의 <code class="language-plaintext highlighter-rouge">web</code> Service는 <code class="language-plaintext highlighter-rouge">web.default.svc.cluster.local</code>로 조회되고, 이 이름은 Service의 ClusterIP로 해석된다. 임시 Pod를 하나 띄워 직접 불러보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl run tmp <span class="nt">--rm</span> <span class="nt">-it</span> <span class="nt">--image</span><span class="o">=</span>curlimages/curl <span class="nt">--restart</span><span class="o">=</span>Never <span class="nt">--</span> sh
~ <span class="nv">$ </span>curl <span class="nt">-s</span> http://web.default.svc.cluster.local | <span class="nb">head</span> <span class="nt">-4</span>
&lt;<span class="o">!</span>DOCTYPE html&gt;
&lt;html&gt;
&lt;<span class="nb">head</span><span class="o">&gt;</span>
&lt;title&gt;Welcome to nginx!&lt;/title&gt;
~ <span class="nv">$ </span>curl <span class="nt">-s</span> http://web | <span class="nb">head</span> <span class="nt">-4</span>
&lt;<span class="o">!</span>DOCTYPE html&gt;
&lt;html&gt;
&lt;<span class="nb">head</span><span class="o">&gt;</span>
&lt;title&gt;Welcome to nginx!&lt;/title&gt;
</code></pre></div></div> <p>두 번째 명령처럼 <strong>같은 네임스페이스 안에서는 서비스 이름만으로도 접근된다</strong>. kubelet이 Pod의 <code class="language-plaintext highlighter-rouge">/etc/resolv.conf</code>에 <code class="language-plaintext highlighter-rouge">&lt;네임스페이스&gt;.svc.cluster.local</code> 등의 search 도메인을 넣어 두기 때문에, 짧은 이름이 자동으로 전체 이름으로 확장된다. 다른 네임스페이스의 서비스를 부를 때는 <code class="language-plaintext highlighter-rouge">web.default</code>처럼 네임스페이스를 붙이면 된다.</p> <p>이것이 Kubernetes의 <strong>서비스 디스커버리</strong>다. 백엔드가 데이터베이스를 찾을 때 IP를 설정 파일에 박는 것이 아니라 <code class="language-plaintext highlighter-rouge">db.default.svc.cluster.local</code>이라는 안정적인 이름 하나만 알면 된다. 참고로 <code class="language-plaintext highlighter-rouge">clusterIP: None</code>으로 만드는 Headless Service라는 변종도 있는데, 대표번호 없이 Pod 개개인의 번호를 그대로 알려주는 방식으로 05편의 StatefulSet과 짝을 이룬다.</p> <h2 id="32-nodeport--노드의-문을-연다">3.2 NodePort — 노드의 문을 연다</h2> <p>ClusterIP는 클러스터 밖에서는 보이지 않는다. 외부에서 접근하는 가장 원초적인 방법이 <strong>NodePort</strong>다. NodePort Service를 만들면 컨트롤 플레인이 <code class="language-plaintext highlighter-rouge">--service-node-port-range</code> 플래그로 지정된 범위(<strong>기본값 30000-32767</strong>)에서 포트를 하나 할당하고, <strong>모든 노드</strong>가 그 포트로 들어온 트래픽을 Service로 넘긴다. 어느 노드의 IP로 접근하든 같은 포트에서 같은 Service에 닿는다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl expose deployment web <span class="nt">--port</span><span class="o">=</span>80 <span class="nt">--type</span><span class="o">=</span>NodePort <span class="nt">--name</span><span class="o">=</span>web-nodeport
service/web-nodeport exposed

<span class="nv">$ </span>kubectl get svc web-nodeport
NAME           TYPE       CLUSTER-IP     EXTERNAL-IP   PORT<span class="o">(</span>S<span class="o">)</span>        AGE
web-nodeport   NodePort   10.96.201.44   &lt;none&gt;        80:31234/TCP   6s
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">80:31234/TCP</code>에서 <code class="language-plaintext highlighter-rouge">31234</code>가 할당된 NodePort다. 과연 30000-32767 범위 안이다.</p> <p>그런데 kind에서는 함정이 하나 있다. <a href="/blog/2026/k8s-02-local-setup/">02편</a>에서 봤듯 kind의 “노드”는 내 노트북 위의 <strong>Docker 컨테이너</strong>라서, 노드 IP가 호스트(노트북)에서 바로 접근되는 주소가 아니다. 그래서 노드 컨테이너 안에서 확인하거나,</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>docker <span class="nb">exec</span> <span class="nt">-it</span> kind-control-plane curl <span class="nt">-s</span> localhost:31234 | <span class="nb">head</span> <span class="nt">-4</span>
&lt;<span class="o">!</span>DOCTYPE html&gt;
&lt;html&gt;
&lt;<span class="nb">head</span><span class="o">&gt;</span>
&lt;title&gt;Welcome to nginx!&lt;/title&gt;
</code></pre></div></div> <p>호스트에서 직접 접근하고 싶다면 kind 클러스터 생성 시 <code class="language-plaintext highlighter-rouge">extraPortMappings</code>로 호스트 포트를 노드에 미리 매핑해야 한다. kind 공식 문서는 이때 <strong>노드의 <code class="language-plaintext highlighter-rouge">containerPort</code>와 Service의 <code class="language-plaintext highlighter-rouge">nodePort</code>를 같은 값으로 맞추라</strong>고 안내한다. 이 기능은 잠시 후 Ingress 실습에서 실제로 사용한다.</p> <h2 id="33-loadbalancer--클라우드의-힘-그리고-kind에서-pending인-이유">3.3 LoadBalancer — 클라우드의 힘, 그리고 kind에서 pending인 이유</h2> <p>프로덕션에서 외부 노출의 표준은 <strong>LoadBalancer</strong> 타입이다. AWS·GCP 같은 클라우드에서 이 타입의 Service를 만들면, 클라우드 컨트롤러가 실제 로드밸런서(예: AWS의 ELB)를 프로비저닝하고 외부 IP를 발급해 Service에 연결해 준다.</p> <p>kind에서 만들면 어떻게 될까?</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl expose deployment web <span class="nt">--port</span><span class="o">=</span>80 <span class="nt">--type</span><span class="o">=</span>LoadBalancer <span class="nt">--name</span><span class="o">=</span>web-lb
service/web-lb exposed

<span class="nv">$ </span>kubectl get svc web-lb
NAME     TYPE           CLUSTER-IP     EXTERNAL-IP   PORT<span class="o">(</span>S<span class="o">)</span>        AGE
web-lb   LoadBalancer   10.96.88.123   &lt;pending&gt;     80:32017/TCP   30s
</code></pre></div></div> <p>EXTERNAL-IP가 <code class="language-plaintext highlighter-rouge">&lt;pending&gt;</code>에서 영원히 멈춘다. 고장이 아니다. LoadBalancer 타입은 “외부 로드밸런서를 만들어 달라”는 <strong>요청서</strong>일 뿐이고, 그 요청을 실제로 처리할 클라우드 프로바이더 연동이 로컬 kind에는 없기 때문에 IP를 발급해 줄 주체가 없는 것이다. 공식 문서도 클라우드 연동이 없는 환경에서는 외부 IP 할당이 일어나지 않아 pending 상태로 남는다고 설명한다. (kind 진영에는 이 역할을 로컬에서 흉내 내주는 <code class="language-plaintext highlighter-rouge">cloud-provider-kind</code>라는 별도 프로젝트가 있다.)</p> <p>실습이 끝났으면 NodePort·LoadBalancer Service는 정리해 두자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl delete svc web-nodeport web-lb
service <span class="s2">"web-nodeport"</span> deleted
service <span class="s2">"web-lb"</span> deleted
</code></pre></div></div> <hr/> <h1 id="4-ingress--l7에서-길을-안내하는-데스크">4. Ingress — L7에서 길을 안내하는 데스크</h1> <p>Service만으로 외부 노출을 하려면 곧 한계에 부딪힌다. 서비스가 10개면 NodePort 10개 혹은 로드밸런서 10대(=비용 10배)가 필요하다. 게다가 Service는 L4(TCP/UDP) 수준에서 동작하므로 “URL 경로가 <code class="language-plaintext highlighter-rouge">/api</code>면 백엔드로, <code class="language-plaintext highlighter-rouge">/</code>면 프론트엔드로” 같은 <strong>HTTP 내용 기반 분기</strong>를 할 수 없다.</p> <p>이 문제를 푸는 것이 <strong>Ingress</strong>다. Ingress는 L7(HTTP/HTTPS)에서 <strong>호스트 이름(host)과 경로(path)를 보고 트래픽을 여러 Service로 라우팅하는 규칙</strong>이다. 건물 입구는 하나지만, 1층 안내 데스크가 “채용 문의는 3층 인사팀, AS 접수는 2층 서비스팀”처럼 용건에 따라 안내해 주는 그림이다.</p> <h2 id="41-ingress-리소스-vs-ingress-controller--규칙과-실행자는-다르다">4.1 Ingress 리소스 vs Ingress Controller — 규칙과 실행자는 다르다</h2> <p>입문자가 가장 많이 헷갈리는 지점이 여기다. Ingress는 <strong>두 조각</strong>으로 이루어진다.</p> <table> <thead> <tr> <th>구분</th> <th>Ingress 리소스</th> <th>Ingress Controller</th> </tr> </thead> <tbody> <tr> <td>정체</td> <td>YAML로 선언하는 라우팅 <strong>규칙</strong></td> <td>규칙을 읽어 실제로 트래픽을 처리하는 <strong>프로그램</strong>(Pod)</td> </tr> <tr> <td>비유</td> <td>안내 데스크의 업무 매뉴얼</td> <td>매뉴얼대로 일하는 안내원</td> </tr> <tr> <td>누가 만드나</td> <td>사용자(개발자)가 작성</td> <td>별도로 설치해야 함 (ingress-nginx, Contour 등)</td> </tr> <tr> <td>없으면?</td> <td>규칙만 있고 아무 일도 안 일어남</td> <td>—</td> </tr> </tbody> </table> <p>Kubernetes 공식 문서가 명시하듯, <strong>Ingress Controller가 없으면 Ingress 리소스를 아무리 만들어도 효과가 없다</strong>. Deployment를 만들면 내장 컨트롤러가 알아서 동작하는 것과 달리, Ingress의 실행자는 클러스터에 기본 내장되어 있지 않다. 매뉴얼만 써 놓고 안내원을 채용하지 않은 상태인 셈이다. 가장 널리 쓰이는 안내원이 Kubernetes 커뮤니티가 관리하는 <strong>ingress-nginx</strong>다.</p> <h2 id="42-실습--kind에-ingress-nginx-설치하기">4.2 실습 — kind에 ingress-nginx 설치하기</h2> <p>이제 ingress-nginx를 설치해 보자. 호스트의 80 포트로 들어온 요청이 클러스터 안까지 닿아야 하므로, <code class="language-plaintext highlighter-rouge">extraPortMappings</code>를 넣은 <strong>전용 클러스터를 새로 만든다</strong> (kind는 클러스터 생성이 1분이면 되니 부담이 없다).</p> <p>참고로 kind 공식 ingress 가이드는 최근 <code class="language-plaintext highlighter-rouge">cloud-provider-kind</code>(v0.9.0+)의 네이티브 Ingress 지원을 기본으로 안내하고, ingress-nginx 같은 서드파티 컨트롤러는 각자의 공식 문서를 따르라고 되어 있다. 여기서는 실무에서 가장 흔히 만나는 ingress-nginx를 직접 설치하는 경로로 간다. ingress-nginx 프로젝트는 kind 환경용 매니페스트를 별도로 제공한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | kind create cluster --name ingress --config=-
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 80
        hostPort: 80
        protocol: TCP
      - containerPort: 443
        hostPort: 443
        protocol: TCP
</span><span class="no">EOF
</span>Creating cluster <span class="s2">"ingress"</span> ...
 ✓ Ensuring node image <span class="o">(</span>kindest/node:...<span class="o">)</span> 🖼
 ✓ Preparing nodes 📦
 ...
Set kubectl context to <span class="s2">"kind-ingress"</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">extraPortMappings</code>는 호스트(노트북)의 80/443 포트를 노드 컨테이너의 80/443으로 포워딩한다. 이제 ingress-nginx의 kind 전용 매니페스트를 적용한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl apply <span class="nt">-f</span> https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/kind/deploy.yaml
namespace/ingress-nginx created
serviceaccount/ingress-nginx created
...
deployment.apps/ingress-nginx-controller created
ingressclass.networking.k8s.io/nginx created
...
</code></pre></div></div> <p>이 매니페스트를 열어 보면(작성 시점 기준 controller v1.15.1) 설계 의도가 보인다. 컨트롤러 Pod가 <code class="language-plaintext highlighter-rouge">hostPort: 80/443</code>으로 <strong>노드의 80/443 포트에 직접 바인딩</strong>되고, control-plane 노드에도 뜰 수 있게 toleration이 걸려 있다. 그래서 <code class="language-plaintext highlighter-rouge">호스트:80 → (extraPortMappings) → 노드:80 → (hostPort) → ingress-nginx 컨트롤러</code>라는 파이프가 완성된다. 컨트롤러가 준비될 때까지 기다리자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl <span class="nb">wait</span> <span class="nt">--namespace</span> ingress-nginx <span class="se">\</span>
    <span class="nt">--for</span><span class="o">=</span><span class="nv">condition</span><span class="o">=</span>ready pod <span class="se">\</span>
    <span class="nt">--selector</span><span class="o">=</span>app.kubernetes.io/component<span class="o">=</span>controller <span class="se">\</span>
    <span class="nt">--timeout</span><span class="o">=</span>120s
pod/ingress-nginx-controller-6b94c75599-xv2fk condition met
</code></pre></div></div> <h2 id="43-실습--path-기반-라우팅">4.3 실습 — path 기반 라우팅</h2> <p>안내원이 준비됐으니 부서 두 개를 만들자. 요청받은 문자열을 그대로 응답하는 <code class="language-plaintext highlighter-rouge">http-echo</code> 이미지로 <code class="language-plaintext highlighter-rouge">/foo</code>와 <code class="language-plaintext highlighter-rouge">/bar</code>에 각각 다른 앱을 배치한다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># echo-apps.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">foo-app</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">1</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">foo</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">foo</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http-echo</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">hashicorp/http-echo</span>
          <span class="na">args</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">-text=foo"</span><span class="pi">]</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">foo-service</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">foo</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">port</span><span class="pi">:</span> <span class="m">5678</span> <span class="c1"># http-echo의 기본 포트</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">bar-app</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">1</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">bar</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">bar</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">http-echo</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">hashicorp/http-echo</span>
          <span class="na">args</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">-text=bar"</span><span class="pi">]</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">bar-service</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">bar</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">port</span><span class="pi">:</span> <span class="m">5678</span>
</code></pre></div></div> <p>두 Service는 모두 기본값인 ClusterIP다. <strong>외부 노출은 Ingress가 담당하므로 Service를 NodePort로 만들 필요가 없다</strong>는 점을 눈여겨보자. 이제 라우팅 규칙, 즉 Ingress 리소스를 작성한다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># echo-ingress.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">networking.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Ingress</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">example-ingress</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">ingressClassName</span><span class="pi">:</span> <span class="s">nginx</span> <span class="c1"># 어떤 컨트롤러가 이 규칙을 처리할지 지정</span>
  <span class="na">rules</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">http</span><span class="pi">:</span>
        <span class="na">paths</span><span class="pi">:</span>
          <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/foo</span>
            <span class="na">pathType</span><span class="pi">:</span> <span class="s">Prefix</span>
            <span class="na">backend</span><span class="pi">:</span>
              <span class="na">service</span><span class="pi">:</span>
                <span class="na">name</span><span class="pi">:</span> <span class="s">foo-service</span>
                <span class="na">port</span><span class="pi">:</span>
                  <span class="na">number</span><span class="pi">:</span> <span class="m">5678</span>
          <span class="pi">-</span> <span class="na">path</span><span class="pi">:</span> <span class="s">/bar</span>
            <span class="na">pathType</span><span class="pi">:</span> <span class="s">Prefix</span>
            <span class="na">backend</span><span class="pi">:</span>
              <span class="na">service</span><span class="pi">:</span>
                <span class="na">name</span><span class="pi">:</span> <span class="s">bar-service</span>
                <span class="na">port</span><span class="pi">:</span>
                  <span class="na">number</span><span class="pi">:</span> <span class="m">5678</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">ingressClassName: nginx</code> — 클러스터에 컨트롤러가 여러 개일 수 있으므로, 이 규칙의 담당자를 지정한다. 방금 설치한 매니페스트가 <code class="language-plaintext highlighter-rouge">nginx</code>라는 IngressClass를 만들어 두었다.</li> <li><code class="language-plaintext highlighter-rouge">path</code> + <code class="language-plaintext highlighter-rouge">pathType: Prefix</code> — URL이 <code class="language-plaintext highlighter-rouge">/foo</code>로 시작하면 <code class="language-plaintext highlighter-rouge">foo-service</code>로 보낸다.</li> <li><code class="language-plaintext highlighter-rouge">rules</code>에 <code class="language-plaintext highlighter-rouge">host: foo.example.com</code>처럼 호스트를 추가하면 도메인 기반 분기도 가능하다.</li> </ul> <p>적용하고 호스트에서 바로 curl을 날려 보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl apply <span class="nt">-f</span> echo-apps.yaml <span class="nt">-f</span> echo-ingress.yaml
deployment.apps/foo-app created
service/foo-service created
deployment.apps/bar-app created
service/bar-service created
ingress.networking.k8s.io/example-ingress created

<span class="nv">$ </span>curl localhost/foo
foo
<span class="nv">$ </span>curl localhost/bar
bar
</code></pre></div></div> <p>같은 <code class="language-plaintext highlighter-rouge">localhost:80</code> 입구로 들어갔지만 경로에 따라 서로 다른 Service의 Pod가 응답했다. 전체 흐름을 그림으로 정리하면 이렇다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-06-networking/ingress-path-routing-480.webp 480w,/assets/post/image/k8s-06-networking/ingress-path-routing-800.webp 800w,/assets/post/image/k8s-06-networking/ingress-path-routing-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-06-networking/ingress-path-routing.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="curl 요청이 extraPortMappings와 hostPort를 거쳐 ingress-nginx 컨트롤러에 닿고, path에 따라 foo-service와 bar-service로 라우팅되는 흐름" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>Ingress 리소스는 점선이다 — 트래픽이 흐르는 곳이 아니라 컨트롤러가 <strong>읽는 설정</strong>이다.</li> <li>외부 노출 지점은 컨트롤러 하나뿐이고, 뒤의 Service들은 전부 내부 전용 ClusterIP다.</li> <li>실습이 끝나면 <code class="language-plaintext highlighter-rouge">kind delete cluster --name ingress</code>로 정리한다.</li> </ul> <hr/> <h1 id="5-hpa--트래픽에-맞춰-pod-수를-자동-조절한다">5. HPA — 트래픽에 맞춰 Pod 수를 자동 조절한다</h1> <p>입구 정리는 끝났다. Service가 대표번호를 만들고 Ingress가 경로를 안내한다. 그런데 그 입구로 <strong>트래픽이 밀려들면</strong> 어떻게 될까? <a href="/blog/2026/k8s-05-workloads/">05편</a>까지 replicas는 항상 사람이 정해 준 고정값이었다. 새벽 3시에 트래픽이 치솟으면 누군가 일어나서 <code class="language-plaintext highlighter-rouge">kubectl scale</code>을 쳐야 한다는 뜻이다.</p> <p>이 일을 대신해 주는 것이 <strong>HPA(HorizontalPodAutoscaler)</strong>다. HPA는 메트릭(대표적으로 CPU 사용률)을 지켜보다가 Deployment·StatefulSet 같은 워크로드의 <strong>replicas를 자동으로 늘리고 줄이는</strong> 리소스다. 마트에 손님이 몰리면 계산대를 더 열고, 한산해지면 닫는 것과 같다. 점장이 CCTV로 매장 혼잡도를 보고 판단하듯, HPA 컨트롤러는 <strong>metrics-server</strong>가 수집한 사용률을 보고 판단한다.</p> <h2 id="51-동작-원리--metrics-server와-스케일-공식">5.1 동작 원리 — metrics-server와 스케일 공식</h2> <p>HPA가 동작하려면 먼저 “지금 Pod들이 얼마나 바쁜가”를 알아야 한다. 이 CCTV 역할을 하는 것이 <strong>metrics-server</strong>다. 각 노드의 kubelet에서 CPU·메모리 사용량을 모아 Metrics API(<code class="language-plaintext highlighter-rouge">metrics.k8s.io</code>)로 노출하면, HPA 컨트롤러가 이 값을 주기적으로(기본 15초 간격) 조회한다. 주의할 점은 metrics-server가 <strong>클러스터에 기본 내장돼 있지 않은 애드온</strong>이라는 것이다. kind 클러스터에도 당연히 없으므로 직접 설치해야 한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl apply <span class="nt">-f</span> https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
</code></pre></div></div> <p>로컬 테스트 환경에서는 kubelet 인증서 검증을 끄는 <code class="language-plaintext highlighter-rouge">--kubelet-insecure-tls</code> 옵션을 metrics-server 컨테이너 args에 추가해야 할 수 있다. 공식 문서가 명시하듯 <strong>테스트 용도 전용</strong> 옵션이므로 프로덕션에서는 쓰지 않는다.</p> <p>메트릭이 준비되면 HPA 컨트롤러는 다음 공식으로 원하는 replicas를 계산한다.</p> \[\text{desiredReplicas} = \left\lceil \text{currentReplicas} \times \frac{\text{currentMetricValue}}{\text{desiredMetricValue}} \right\rceil\] <ul> <li><strong>currentMetricValue</strong> — 현재 Pod들의 평균 메트릭 값 (예: 평균 CPU 사용률 100%)</li> <li><strong>desiredMetricValue</strong> — 내가 선언한 목표 값 (예: CPU 사용률 50%)</li> <li><strong>ceil</strong> — 올림. Pod는 반 개로 띄울 수 없으니 올림한다.</li> </ul> <p>직관은 간단하다 — <strong>“지금이 목표의 몇 배나 바쁜가”만큼 Pod 수를 곱한다</strong>. 목표 사용률 50%인데 실제 100%면 2배로 늘리고, 실제 25%면 절반으로 줄인다. 현재/목표 비율이 1.0에서 기본 10%(tolerance) 이내면 굳이 움직이지 않는다 — 사용률 52% 정도로는 스케일이 일어나지 않는다는 뜻이다.</p> <p>여기서 CPU “사용률(utilization)”은 절대량이 아니라 <strong>Pod에 설정한 resource request 대비 백분율</strong>이다. request가 200m인 컨테이너가 100m을 쓰고 있으면 사용률 50%다. 뒤집어 말하면, <strong>request를 설정하지 않은 Pod에는 CPU 사용률 기반 HPA가 동작할 수 없다</strong>.</p> <h2 id="52-kubectl-autoscale--한-줄로-만들기">5.2 kubectl autoscale — 한 줄로 만들기</h2> <p>HPA도 YAML로 선언할 수 있지만, 간단한 경우는 명령 한 줄이면 된다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">$ </span>kubectl autoscale deployment web <span class="nt">--cpu-percent</span><span class="o">=</span>50 <span class="nt">--min</span><span class="o">=</span>2 <span class="nt">--max</span><span class="o">=</span>10
horizontalpodautoscaler.autoscaling/web autoscaled

<span class="nv">$ </span>kubectl get hpa
NAME   REFERENCE        TARGETS       MINPODS   MAXPODS   REPLICAS   AGE
web    Deployment/web   cpu: 0%/50%   2         10        2          30s
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">--cpu-percent=50</code> — 목표 평균 CPU 사용률.</li> <li><code class="language-plaintext highlighter-rouge">--min=2</code> / <code class="language-plaintext highlighter-rouge">--max=10</code> — replicas의 하한(minReplicas)과 상한(maxReplicas). HPA는 <strong>이 범위 안에서만</strong> 움직인다. 트래픽이 아무리 치솟아도 10개 이상 만들지 않고(비용 폭주 방지), 아무리 한산해도 2개는 남긴다(가용성 확보).</li> <li>HPA는 scale 서브리소스를 지원하는 워크로드(Deployment, StatefulSet 등)에만 붙일 수 있다. <strong>DaemonSet에는 적용되지 않는다</strong> — “노드당 1개”라는 정의 자체가 개수 조절과 맞지 않기 때문이다.</li> </ul> <p>참고로 HPA가 Pod의 <strong>개수</strong>를 조절하는 수평 확장이라면, Pod <strong>하나에 할당된 CPU/메모리 자체</strong>를 키우는 수직 확장은 별도 프로젝트인 <strong>VPA(Vertical Pod Autoscaler)</strong>가 담당한다.</p> <hr/> <h1 id="6-networkpolicy--기본값은-전부-허용이다">6. NetworkPolicy — 기본값은 “전부 허용”이다</h1> <p>여기까지 오면서 한 번도 언급하지 않은 사실이 있다. Kubernetes 클러스터 안에서는 기본적으로 <strong>모든 Pod가 다른 모든 Pod와 자유롭게 통신할 수 있다</strong>. 공식 문서 표현으로 Pod는 기본적으로 non-isolated — 들어오는(ingress) 트래픽도, 나가는(egress) 트래픽도 전부 허용이다. frontend Pod가 결제 DB Pod에 직접 붙는 것을 막는 장치가 아무것도 없다는 뜻이다.</p> <p>회사로 치면 <strong>모든 사무실 문이 항상 열려 있는 상태</strong>다. 인턴이 금고실에 들어가도 아무도 막지 않는다. 침입자가 사무실 하나를 뚫으면 건물 전체를 휘젓고 다닐 수 있다. 이 문에 출입증 규칙을 다는 것이 <strong>NetworkPolicy</strong>다 — 네임스페이스와 라벨을 기준으로 Pod 간 트래픽을 제한하는 클러스터 내부 방화벽 규칙이라고 보면 된다.</p> <h2 id="61-규칙의-구조--podselector-ingress-egress">6.1 규칙의 구조 — podSelector, Ingress, Egress</h2> <p>NetworkPolicy 하나는 크게 세 부분으로 이루어진다. “DB Pod에는 backend Pod만 접근할 수 있다”는 규칙을 예로 보자.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># db-allow-backend.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">networking.k8s.io/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">NetworkPolicy</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">db-allow-backend</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">podSelector</span><span class="pi">:</span> <span class="c1"># 1) 누구를 보호할 것인가</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">db</span>
  <span class="na">policyTypes</span><span class="pi">:</span> <span class="c1"># 2) 어느 방향을 제한할 것인가</span>
    <span class="pi">-</span> <span class="s">Ingress</span>
  <span class="na">ingress</span><span class="pi">:</span> <span class="c1"># 3) 무엇을 허용할 것인가</span>
    <span class="pi">-</span> <span class="na">from</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">podSelector</span><span class="pi">:</span>
            <span class="na">matchLabels</span><span class="pi">:</span>
              <span class="na">app</span><span class="pi">:</span> <span class="s">backend</span>
      <span class="na">ports</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">protocol</span><span class="pi">:</span> <span class="s">TCP</span>
          <span class="na">port</span><span class="pi">:</span> <span class="m">5432</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">podSelector</code> — 이 정책이 <strong>적용되는 대상</strong>. <code class="language-plaintext highlighter-rouge">app: db</code> 라벨이 붙은 Pod가 보호 대상이 된다.</li> <li><code class="language-plaintext highlighter-rouge">policyTypes</code> — <code class="language-plaintext highlighter-rouge">Ingress</code>(들어오는 트래픽), <code class="language-plaintext highlighter-rouge">Egress</code>(나가는 트래픽), 또는 둘 다. 여기서 말하는 Ingress는 방향(수신)을 뜻하는 단어로, 4장의 Ingress 리소스와는 <strong>이름만 같은 별개 개념</strong>이니 헷갈리지 말자.</li> <li><code class="language-plaintext highlighter-rouge">ingress.from</code> — 허용할 출발지 목록. 여기서는 같은 네임스페이스의 <code class="language-plaintext highlighter-rouge">app: backend</code> Pod만 TCP 5432 포트로 허용한다. <code class="language-plaintext highlighter-rouge">podSelector</code> 외에 <code class="language-plaintext highlighter-rouge">namespaceSelector</code>(특정 네임스페이스의 Pod들)와 <code class="language-plaintext highlighter-rouge">ipBlock</code>(IP 대역)도 쓸 수 있다.</li> </ul> <p>이 정책이 적용되는 순간 <code class="language-plaintext highlighter-rouge">app: db</code> Pod는 <strong>isolated 상태</strong>가 된다. NetworkPolicy는 차단 목록이 아니라 <strong>허용 목록(whitelist)</strong> 방식이라, 정책에 명시된 트래픽 외에는 전부 차단된다 — frontend Pod가 5432로 접속을 시도하면 거부된다. 정책 여러 개가 같은 Pod를 선택하면 허용 범위는 <strong>합집합</strong>으로 합쳐진다. 한 가지 더: 통신이 성립하려면 <strong>출발 Pod의 egress와 도착 Pod의 ingress 양쪽 모두</strong>에서 허용되어야 한다. 한쪽이라도 막으면 연결은 실패한다.</p> <h2 id="62-주의--cni가-지원해야-동작한다">6.2 주의 — CNI가 지원해야 동작한다</h2> <p>가장 중요한 함정이다. <strong>NetworkPolicy는 네트워크 플러그인(CNI)이 구현한다</strong>. 공식 문서가 명시하듯, 정책을 지원하는 네트워킹 솔루션 없이 NetworkPolicy 리소스만 만들면 <strong>아무 효과가 없다</strong>. 4장에서 본 Ingress 리소스와 Ingress Controller의 관계와 똑같은 구도다 — 규칙(리소스)과 실행자(CNI)가 분리되어 있고, 실행자가 없으면 규칙은 종이 조각이다. Calico, Cilium 등이 NetworkPolicy를 지원하는 대표적인 CNI다.</p> <p>kind에서 NetworkPolicy를 제대로 실습하고 싶다면, kind 공식 문서가 안내하는 대로 클러스터 설정에 <code class="language-plaintext highlighter-rouge">disableDefaultCNI: true</code>를 넣어 기본 CNI를 끄고 Calico 같은 CNI를 직접 설치하는 방법이 있다. 어떤 환경에서든 정책을 만들었는데 여전히 모든 트래픽이 통과한다면, 클러스터의 CNI가 NetworkPolicy를 강제하지 않고 있을 가능성부터 의심하자.</p> <hr/> <h1 id="7-gateway-api--ingress의-후계자">7. Gateway API — Ingress의 후계자</h1> <p>4장의 안내 데스크(Ingress)는 잘 동작하지만, 오래 쓰다 보면 한계가 드러난다.</p> <p><strong>첫째, 표현력이 부족하다.</strong> Ingress 스펙이 표준으로 표현할 수 있는 것은 사실상 host/path 기반 분기 정도다. 헤더 기반 매칭, 트래픽 가중치(카나리 배포) 같은 기능은 표준에 없어서 <strong>컨트롤러별 annotation</strong>으로 해결해 왔다 — Gateway API 공식 문서도 이런 기능들이 “Ingress에서는 커스텀 annotation으로만 가능했다”고 명시한다. annotation은 표준이 아니므로 ingress-nginx용으로 잔뜩 달아 둔 annotation은 다른 컨트롤러로 갈아타는 순간 전부 무용지물이 된다. 매뉴얼(표준 스펙)에 적을 수 없는 지시를 안내원(컨트롤러)마다 다른 방언으로 전달해 온 셈이다.</p> <p><strong>둘째, API가 동결(frozen)됐다.</strong> Kubernetes 공식 문서는 Ingress API가 GA(안정) 상태로 유지는 되지만 <strong>더 이상 개발되지 않으며 새 기능도 추가되지 않는다</strong>고 명시하고, Gateway API 사용을 권장한다.</p> <p><strong>셋째, 역할 분리가 없다.</strong> 인프라 설정과 앱의 라우팅 규칙이 한 리소스에 얽혀 있어서, “로드밸런서는 인프라팀이 관리하고 라우팅 규칙은 각 개발팀이 관리한다” 같은 흔한 조직 구조를 표현할 수 없다.</p> <p><strong>Gateway API</strong>는 이 문제들을 처음부터 다시 설계한 <strong>Ingress의 공식 후계자</strong>다. Kubernetes 내장 API가 아니라 CRD(커스텀 리소스) 애드온으로 제공되며, 핵심은 리소스를 <strong>역할별로 셋으로 쪼갠 것</strong>이다.</p> <table> <thead> <tr> <th>리소스</th> <th>역할</th> <th>주 담당자</th> </tr> </thead> <tbody> <tr> <td><strong>GatewayClass</strong></td> <td>어떤 구현체(컨트롤러)가 트래픽을 처리하는지 정의</td> <td>인프라 프로바이더 / 구현체</td> </tr> <tr> <td><strong>Gateway</strong></td> <td>트래픽을 받아들이는 인프라 인스턴스 (리스너·포트·프로토콜)</td> <td>클러스터 운영자</td> </tr> <tr> <td><strong>HTTPRoute</strong></td> <td>host/path/헤더 기반 라우팅 규칙, 백엔드 Service 지정</td> <td>앱 개발자</td> </tr> </tbody> </table> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-06-networking/gateway-api-roles-480.webp 480w,/assets/post/image/k8s-06-networking/gateway-api-roles-800.webp 800w,/assets/post/image/k8s-06-networking/gateway-api-roles-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-06-networking/gateway-api-roles.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="GatewayClass, Gateway, HTTPRoute가 인프라 프로바이더, 클러스터 운영자, 앱 개발자 역할별로 분리된 Gateway API 구조" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>인프라팀은 Gateway(공용 입구)를 한 번 세팅하고, 각 개발팀은 자기 HTTPRoute만 만들어 그 Gateway에 연결(parentRefs)한다. Ingress에서는 이 경계가 리소스 차원에서 존재하지 않았다.</li> <li>헤더 매칭·트래픽 가중치 같은 고급 라우팅이 annotation이 아니라 <strong>표준 스펙 필드</strong>다. HTTP 외에 gRPC 라우팅(GRPCRoute) 등도 표준으로 지원한다.</li> </ul> <p>Ingress와 나란히 놓고 비교하면 이렇다.</p> <table> <thead> <tr> <th>항목</th> <th>Ingress</th> <th>Gateway API</th> </tr> </thead> <tbody> <tr> <td>API 상태</td> <td>GA이지만 동결 — 신규 기능 추가 없음</td> <td>활발히 개발 중인 후계 표준</td> </tr> <tr> <td>제공 방식</td> <td>Kubernetes 내장 API</td> <td>CRD 애드온 (별도 설치)</td> </tr> <tr> <td>리소스 구조</td> <td>Ingress 하나에 전부</td> <td>GatewayClass / Gateway / HTTPRoute 역할별 분리</td> </tr> <tr> <td>고급 라우팅 (헤더 매칭, 가중치 등)</td> <td>컨트롤러별 annotation 의존</td> <td>표준 스펙에 포함</td> </tr> <tr> <td>프로토콜</td> <td>HTTP/HTTPS 중심</td> <td>HTTPRoute, GRPCRoute 등</td> </tr> <tr> <td>역할 분리</td> <td>없음</td> <td>인프라 운영자와 앱 개발자 분리</td> </tr> </tbody> </table> <p>이미 커뮤니티 표준의 무게 중심도 옮겨가는 중이다. 2025년 2월 18일부터 적용된 <strong>CKA(Certified Kubernetes Administrator) 시험 개편</strong>에서 “Use the Gateway API to manage Ingress traffic”가 Servicing and Networking 도메인의 공식 출제 항목으로 들어갔다.</p> <p>그렇다고 4장에서 배운 Ingress가 당장 쓸모없어지는 것은 아니다. 공식 문서 기준으로 Kubernetes 프로젝트는 Ingress를 제거할 계획이 없고, 기존 Ingress와 컨트롤러 생태계는 여전히 광범위하게 쓰인다. 입문 단계에서는 Ingress로 L7 라우팅 개념을 잡고, Gateway API는 <strong>같은 문제를 역할 분리와 표준화된 표현력으로 다시 푼 후계 표준</strong>으로 기억해 두면 충분하다.</p> <hr/> <h1 id="8-그래서-언제-무엇을-쓰나">8. 그래서 언제 무엇을 쓰나</h1> <table> <thead> <tr> <th>상황</th> <th>선택</th> <th>이유</th> </tr> </thead> <tbody> <tr> <td>클러스터 내부 서비스 간 통신 (백엔드 → DB 등)</td> <td>ClusterIP (기본)</td> <td>외부 노출이 필요 없고, DNS 이름으로 안정적으로 접근</td> </tr> <tr> <td>로컬/개발 환경에서 잠깐 외부 확인</td> <td>NodePort</td> <td>추가 인프라 없이 노드 포트(30000-32767)만 열면 됨</td> </tr> <tr> <td>클라우드에서 단일 서비스를 L4로 노출 (TCP, gRPC 등)</td> <td>LoadBalancer</td> <td>클라우드 LB가 외부 IP·고가용성을 제공</td> </tr> <tr> <td>여러 HTTP 서비스를 도메인/경로로 나눠 노출</td> <td>Ingress (+Controller)</td> <td>입구 하나로 L7 라우팅, LB 비용 절감, TLS 종료도 한곳에서</td> </tr> </tbody> </table> <p>한 줄로 압축하면 — <strong>내부 통신은 ClusterIP, L4 외부 노출은 LoadBalancer, HTTP 여러 개의 교통정리는 Ingress</strong>다.</p> <hr/> <h1 id="9-마무리">9. 마무리</h1> <p>이번 편의 핵심을 요약한다.</p> <table> <thead> <tr> <th>개념</th> <th>역할</th> <th>비유</th> </tr> </thead> <tbody> <tr> <td>Service</td> <td>계속 바뀌는 Pod 집합 앞의 고정 접점(가상 IP + DNS)</td> <td>회사 대표 전화번호</td> </tr> <tr> <td>EndpointSlice</td> <td>Selector로 추적한 실제 Pod IP 명단</td> <td>지금 근무 중인 직원 명단</td> </tr> <tr> <td>kube-proxy</td> <td>각 노드에서 Service 트래픽을 Pod로 라우팅</td> <td>전화 교환대</td> </tr> <tr> <td>Service DNS</td> <td><code class="language-plaintext highlighter-rouge">&lt;svc&gt;.&lt;ns&gt;.svc.cluster.local</code> → ClusterIP</td> <td>전화번호부</td> </tr> <tr> <td>Ingress 리소스</td> <td>host/path 기반 L7 라우팅 규칙 선언</td> <td>안내 데스크 업무 매뉴얼</td> </tr> <tr> <td>Ingress Controller</td> <td>규칙을 실제로 실행하는 별도 설치 컴포넌트</td> <td>매뉴얼대로 일하는 안내원</td> </tr> <tr> <td>HPA</td> <td>메트릭 기준으로 replicas를 min-max 안에서 자동 증감</td> <td>혼잡도 보고 계산대를 여닫는 점장</td> </tr> <tr> <td>NetworkPolicy</td> <td>라벨·네임스페이스 기반 Pod 트래픽 허용 규칙 (CNI가 강제)</td> <td>사무실 출입증 규칙</td> </tr> <tr> <td>Gateway API</td> <td>역할 분리로 다시 설계된 Ingress의 후계 표준</td> <td>업무 분담이 명확한 차세대 안내 시스템</td> </tr> </tbody> </table> <p>Pod IP가 아무리 바뀌어도 흔들리지 않는 접점을 만드는 것, 그것이 Kubernetes 네트워킹 추상화의 존재 이유다. 그런데 접점만 고정한다고 끝이 아니다. Pod가 재시작되면 <strong>컨테이너 안의 데이터도 함께 사라진다</strong>.</p> <blockquote> <p>다음 글 <a href="/blog/2026/k8s-07-storage-config/">07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</a>에서는 사라지는 데이터를 붙잡는 볼륨 추상화와, 설정을 이미지 밖으로 빼내는 방법을 다룬다.</p> </blockquote> <hr/> <h1 id="참고-문헌">참고 문헌</h1> <ul> <li><a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua, SK Devocean, 2024) — 시리즈 로드맵 출처</li> <li><a href="https://kubernetes.io/docs/concepts/services-networking/service/">Kubernetes Docs — Service</a></li> <li><a href="https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/">Kubernetes Docs — DNS for Services and Pods</a></li> <li><a href="https://kubernetes.io/docs/concepts/services-networking/ingress/">Kubernetes Docs — Ingress</a></li> <li><a href="https://kubernetes.io/docs/concepts/services-networking/endpoint-slices/">Kubernetes Docs — EndpointSlices</a></li> <li><a href="https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/">Kubernetes Docs — Horizontal Pod Autoscaling</a></li> <li><a href="https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale-walkthrough/">Kubernetes Docs — HorizontalPodAutoscaler Walkthrough</a></li> <li><a href="https://github.com/kubernetes-sigs/metrics-server">Kubernetes Metrics Server (kubernetes-sigs)</a></li> <li><a href="https://kubernetes.io/docs/concepts/services-networking/network-policies/">Kubernetes Docs — Network Policies</a></li> <li><a href="https://kubernetes.io/docs/concepts/services-networking/gateway/">Kubernetes Docs — Gateway API</a></li> <li><a href="https://gateway-api.sigs.k8s.io/">Gateway API — Kubernetes SIG-Network</a></li> <li><a href="https://training.linuxfoundation.org/certified-kubernetes-administrator-cka-program-changes/">Linux Foundation — CKA Program Changes (2025-02-18)</a></li> <li><a href="https://kind.sigs.k8s.io/docs/user/ingress/">kind — Ingress Guide</a></li> <li><a href="https://kind.sigs.k8s.io/docs/user/configuration/">kind — Configuration (Extra Port Mappings)</a></li> <li><a href="https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/kind/deploy.yaml">ingress-nginx — kind provider manifest</a></li> </ul>]]></content><author><name></name></author><category term="infra"/><category term="kubernetes"/><category term="infra"/><category term="service"/><category term="ingress"/><category term="networking"/><category term="hpa"/><category term="networkpolicy"/><category term="gateway-api"/><summary type="html"><![CDATA[Pod IP는 재시작마다 바뀐다 — ClusterIP·NodePort·LoadBalancer 세 가지 Service 타입과 Ingress로 Kubernetes 트래픽 라우팅을 이해한다]]></summary></entry><entry><title type="html">Kubernetes 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</title><link href="https://www.wonbeomjang.kr/blog/2026/k8s-05-workloads/" rel="alternate" type="text/html" title="Kubernetes 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet"/><published>2026-07-06T20:00:00+00:00</published><updated>2026-07-06T20:00:00+00:00</updated><id>https://www.wonbeomjang.kr/blog/2026/k8s-05-workloads</id><content type="html" xml:base="https://www.wonbeomjang.kr/blog/2026/k8s-05-workloads/"><![CDATA[<blockquote> <p>이 글은 <strong>K8s 입문 시리즈</strong>의 다섯 번째 글이다.</p> <ul> <li><a href="/blog/2026/k8s-01-history/">01: Kubernetes의 탄생 — Google Borg에서 CNCF까지</a></li> <li><a href="/blog/2026/k8s-02-local-setup/">02: 내 노트북에 클러스터 만들기 — kind와 kubectl</a></li> <li><a href="/blog/2026/k8s-03-architecture/">03: Kubernetes 아키텍처 — Control Plane과 Node</a></li> <li><a href="/blog/2026/k8s-04-pod/">04: Pod의 모든 것 — 생성부터 스케줄링까지</a></li> <li><strong>05: 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</strong> ← 현재 글</li> <li><a href="/blog/2026/k8s-06-networking/">06: 네트워킹 — Service와 Ingress</a></li> <li><a href="/blog/2026/k8s-07-storage-config/">07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</a></li> <li><a href="/blog/2026/k8s-08-rbac/">08: 권한 관리 — ServiceAccount와 RBAC</a></li> <li><a href="/blog/2026/k8s-09-operator-cncf/">09: 확장과 생태계 — Operator와 CNCF Projects</a></li> </ul> <p>이 시리즈의 커리큘럼은 SK Devocean의 <a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua) 글의 학습 로드맵을 바탕으로 구성했다.</p> </blockquote> <h1 id="1-왜-pod를-직접-만들지-않을까">1. 왜 Pod를 직접 만들지 않을까</h1> <p><a href="/blog/2026/k8s-04-pod/">04편</a>에서 nginx Pod를 직접 띄워봤다. 그런데 그 Pod에는 치명적인 약점이 있다. <strong>혼자 만든 Pod는 죽으면 그걸로 끝이다.</strong> 컨테이너가 크래시나면 kubelet이 재시작해주긴 하지만, 노드 자체가 죽으면 그 위의 Pod는 함께 사라지고 아무도 다시 만들어주지 않는다. 트래픽이 늘어서 Pod를 3개로 늘리고 싶으면? YAML을 세 번 복사해서 이름만 바꿔 적용해야 한다. 업데이트는 더 끔찍하다. 하나씩 지우고 새로 만들면서 서비스가 끊기지 않기를 기도해야 한다.</p> <p>일상 비유로 보면 이렇다. Pod를 직접 만드는 건 사장이 직원 한 명 한 명을 직접 고용하고, 누가 그만두면 직접 알아차려서 다시 뽑는 방식이다. 워크로드 리소스는 인력 관리 회사와 <strong>“우리 매장에 항상 3명이 근무하게 해달라”</strong>는 계약을 맺는 것이다. 누가 그만두든, 몇 명이 필요하든, 관리 회사(컨트롤러)가 알아서 채워 넣는다.</p> <p>이게 바로 <a href="/blog/2026/k8s-03-architecture/">03편</a>에서 본 <strong>reconcile 루프</strong>의 실체다. 사용자는 desired state(“nginx 3개”)만 선언하고, 컨트롤러가 current state를 끊임없이 비교하며 차이를 메꾼다. 이 글에서 다룰 워크로드 리소스 4종을 미리 훑어보면 다음과 같다.</p> <table> <thead> <tr> <th>리소스</th> <th>한 줄 요약</th> <th>대표 용도</th> </tr> </thead> <tbody> <tr> <td>ReplicaSet</td> <td>동일한 Pod를 항상 N개 유지</td> <td>직접 쓸 일은 드물다</td> </tr> <tr> <td>Deployment</td> <td>ReplicaSet + 롤링 업데이트/롤백</td> <td>무상태 웹/API 서버</td> </tr> <tr> <td>StatefulSet</td> <td>이름과 디스크가 유지되는 Pod 집합</td> <td>DB, 메시지 브로커</td> </tr> <tr> <td>DaemonSet</td> <td>모든(또는 일부) 노드에 Pod 1개씩</td> <td>로그 수집기, 모니터링 에이전트</td> </tr> </tbody> </table> <p>글 후반부에서는 여기에 더해, 리소스가 사는 공간을 구획하는 <strong>Namespace</strong>와 “끝나는 것이 정상”인 작업을 위한 <strong>Job/CronJob</strong>도 다룬다. 실습은 02편에서 만든 kind 클러스터를 그대로 사용한다.</p> <hr/> <h1 id="2-label과-selector--모든-워크로드의-연결-고리">2. Label과 Selector — 모든 워크로드의 연결 고리</h1> <p>본론에 들어가기 전에 반드시 짚어야 할 개념이 있다. 워크로드 컨트롤러는 자기가 관리할 Pod를 <strong>이름이 아니라 Label로 찾는다.</strong> Label은 리소스에 붙이는 key/value 메타데이터이고, Selector는 그 Label을 조건으로 리소스 집합을 골라내는 문법이다.</p> <p>비유하면 Label은 사원증에 적힌 부서명이다. “김 아무개를 찾아줘”(이름)가 아니라 “<strong>개발팀 소속 전원</strong> 회의실로”(조건)라고 부르는 방식이다. Pod는 언제든 죽고 새로 태어나며 그때마다 이름이 바뀌기 때문에, 이름 대신 조건으로 묶는 이 방식이 Kubernetes 전체를 관통하는 그룹핑 원리다.</p> <p>Selector 문법은 두 가지가 있다.</p> <table> <thead> <tr> <th>방식</th> <th>연산자</th> <th>예시</th> <th>의미</th> </tr> </thead> <tbody> <tr> <td>equality-based</td> <td><code class="language-plaintext highlighter-rouge">=</code>, <code class="language-plaintext highlighter-rouge">==</code>, <code class="language-plaintext highlighter-rouge">!=</code></td> <td><code class="language-plaintext highlighter-rouge">env=prod</code>, <code class="language-plaintext highlighter-rouge">tier!=frontend</code></td> <td>값이 같다 / 다르다</td> </tr> <tr> <td>set-based</td> <td><code class="language-plaintext highlighter-rouge">in</code>, <code class="language-plaintext highlighter-rouge">notin</code>, 키 존재 여부</td> <td><code class="language-plaintext highlighter-rouge">env in (dev, prod)</code>, <code class="language-plaintext highlighter-rouge">!partition</code></td> <td>값이 집합에 속한다 / 안 속한다 / 키가 있다</td> </tr> </tbody> </table> <p>직접 확인해보자. Label을 다르게 붙인 Pod 3개를 만든다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl run web-1 <span class="nt">--image</span><span class="o">=</span>nginx:1.25 <span class="nt">--labels</span><span class="o">=</span><span class="s2">"app=nginx,env=dev"</span>
kubectl run web-2 <span class="nt">--image</span><span class="o">=</span>nginx:1.25 <span class="nt">--labels</span><span class="o">=</span><span class="s2">"app=nginx,env=prod"</span>
kubectl run db-1 <span class="nt">--image</span><span class="o">=</span>redis:7 <span class="nt">--labels</span><span class="o">=</span><span class="s2">"app=redis,env=dev"</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">-l</code> 플래그로 조건에 맞는 Pod만 골라낸다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>nginx
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME    READY   STATUS    RESTARTS   AGE
web-1   1/1     Running   0          30s
web-2   1/1     Running   0          25s
</code></pre></div></div> <p>set-based 문법은 괄호가 셸에 해석되지 않도록 따옴표로 감싼다. 아래 명령은 <code class="language-plaintext highlighter-rouge">env</code>가 <code class="language-plaintext highlighter-rouge">dev</code>인 <code class="language-plaintext highlighter-rouge">web-1</code>과 <code class="language-plaintext highlighter-rouge">db-1</code>을 돌려준다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-l</span> <span class="s1">'env in (dev)'</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">kubectl get pods --show-labels</code>를 실행하면 각 Pod에 붙은 Label 전체를 볼 수 있다. 확인이 끝나면 정리한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod web-1 web-2 db-1
</code></pre></div></div> <p>앞으로 나올 모든 워크로드 리소스의 YAML에는 <code class="language-plaintext highlighter-rouge">selector</code> 필드가 있고, 그 값이 Pod template의 <code class="language-plaintext highlighter-rouge">labels</code>와 일치해야 한다. 이 연결 고리를 기억해두자.</p> <hr/> <h1 id="3-namespace--리소스를-나누는-가상의-벽">3. Namespace — 리소스를 나누는 가상의 벽</h1> <p>Label을 배웠으니 자연스럽게 이어지는 질문이 있다. 조건으로 <strong>골라내는</strong> 것 말고, 아예 리소스가 사는 <strong>공간을 나누는</strong> 방법은 없을까? 그 답이 Namespace다. 하나의 물리 클러스터 안에서 리소스 그룹을 격리하는 메커니즘으로, 클러스터를 여러 개의 가상 클러스터처럼 나눠 쓰게 해준다. 사원증 비유를 이어가면, Label이 사원증에 적힌 부서명이라면 Namespace는 건물의 <strong>층</strong>이다. 부서명은 어느 층에 있든 “개발팀 전원”을 불러 모으는 조건이고, 층은 아예 분리된 공간이라 3층과 5층에 같은 이름의 회의실이 있어도 충돌하지 않는다.</p> <p>격리의 실체는 두 가지다.</p> <ul> <li><strong>이름 충돌 방지</strong> — 리소스 이름은 같은 Namespace 안에서만 유일하면 된다. <code class="language-plaintext highlighter-rouge">dev</code>, <code class="language-plaintext highlighter-rouge">staging</code>, <code class="language-plaintext highlighter-rouge">prod</code> 네임스페이스에 같은 이름의 Deployment <code class="language-plaintext highlighter-rouge">api-server</code>를 동시에 띄울 수 있다. 같은 YAML을 네임스페이스만 바꿔 적용하면 환경별 사본이 생기는 셈이다.</li> <li><strong>정책의 경계</strong> — 팀별 CPU/메모리 한도(ResourceQuota)나 접근 권한(<a href="/blog/2026/k8s-08-rbac/">08편</a>에서 다룰 RBAC)을 네임스페이스 단위로 걸 수 있다.</li> </ul> <p>클러스터는 처음부터 네임스페이스 4개를 갖고 시작한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get namespace
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME              STATUS   AGE
default           Active   3d
kube-node-lease   Active   3d
kube-public       Active   3d
kube-system       Active   3d
</code></pre></div></div> <table> <thead> <tr> <th>Namespace</th> <th>용도</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">default</code></td> <td>네임스페이스를 지정하지 않은 리소스가 들어가는 곳. 지금까지 만든 Pod가 전부 여기 있었다</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kube-system</code></td> <td>Kubernetes 시스템이 만든 오브젝트용. <a href="/blog/2026/k8s-03-architecture/">03편</a>에서 본 시스템 컴포넌트들이 여기서 돈다</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kube-public</code></td> <td>인증 없이도 모든 클라이언트가 읽을 수 있는 공간. 주로 클러스터 내부 용도로 예약되어 있다</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kube-node-lease</code></td> <td>노드별 Lease 오브젝트 보관. kubelet이 하트비트를 보내 노드 생존을 알리는 데 쓴다</td> </tr> </tbody> </table> <p>직접 하나 만들어 써보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl create namespace dev
kubectl run nginx <span class="nt">--image</span><span class="o">=</span>nginx:1.25 <span class="nt">-n</span> dev
kubectl get pods <span class="nt">-n</span> dev
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME    READY   STATUS    RESTARTS   AGE
nginx   1/1     Running   0          10s
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">-n dev</code>를 빼고 <code class="language-plaintext highlighter-rouge">kubectl get pods</code>를 치면 이 Pod는 보이지 않는다. 네임스페이스를 지정하지 않은 명령은 전부 <code class="language-plaintext highlighter-rouge">default</code>를 향하기 때문이다. 매번 <code class="language-plaintext highlighter-rouge">-n</code>을 붙이기 귀찮다면 현재 컨텍스트의 기본 네임스페이스 자체를 바꿀 수 있다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config set-context <span class="nt">--current</span> <span class="nt">--namespace</span><span class="o">=</span>dev
</code></pre></div></div> <p>한 가지 주의할 점 — 모든 리소스가 네임스페이스에 속하는 것은 아니다. Pod, Deployment, Service처럼 네임스페이스 안에 사는 리소스(namespaced)가 있고, Node나 PersistentVolume처럼 클러스터 전체에 걸치는 리소스(cluster-scoped)가 있다. “이 노드는 dev팀 것”이라고 가를 수 없으니 당연한 구분이다. 어떤 리소스가 어느 쪽인지는 다음 명령으로 확인한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl api-resources <span class="nt">--namespaced</span><span class="o">=</span><span class="nb">true</span>   <span class="c"># 네임스페이스에 속하는 리소스</span>
kubectl api-resources <span class="nt">--namespaced</span><span class="o">=</span><span class="nb">false</span>  <span class="c"># 클러스터 전역 리소스</span>
</code></pre></div></div> <p>실습이 끝나면 정리한다. <strong>네임스페이스를 지우면 그 안의 리소스가 전부 함께 삭제되므로</strong> 운영 클러스터에서는 각별히 조심해야 하는 명령이다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config set-context <span class="nt">--current</span> <span class="nt">--namespace</span><span class="o">=</span>default
kubectl delete namespace dev
</code></pre></div></div> <p>Label과의 역할 차이를 한 줄로 정리하면 — <strong>Namespace는 격리(공간을 나눈다), Label은 그룹핑(조건으로 골라낸다)</strong>이다. 공식 문서도 같은 앱의 버전 구분처럼 살짝 다른 리소스는 네임스페이스가 아니라 Label로 구분하라고 안내한다. 참고로 Service의 DNS 주소(<code class="language-plaintext highlighter-rouge">&lt;service&gt;.&lt;namespace&gt;.svc.cluster.local</code>)에도 네임스페이스가 들어가는데, 이는 <a href="/blog/2026/k8s-06-networking/">06편</a>에서 다룬다.</p> <p>이제 본론인 워크로드 컨트롤러로 들어가자.</p> <hr/> <h1 id="4-replicaset--항상-n개의-보장">4. ReplicaSet — “항상 N개”의 보장</h1> <p>ReplicaSet의 목적은 단순하다. <strong>동일한 Pod가 항상 지정한 개수(replicas)만큼 떠 있도록 보장한다.</strong> YAML은 세 부분으로 구성된다. 몇 개를 원하는지(<code class="language-plaintext highlighter-rouge">replicas</code>), 어떤 Pod가 자기 소유인지(<code class="language-plaintext highlighter-rouge">selector</code>), 부족할 때 무엇을 만들지(<code class="language-plaintext highlighter-rouge">template</code>).</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># nginx-rs.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">ReplicaSet</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-rs</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">nginx-rs</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">nginx-rs</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.25</span>
</code></pre></div></div> <p>적용하면 <code class="language-plaintext highlighter-rouge">nginx-rs-</code> 뒤에 랜덤 접미사가 붙은 Pod 3개가 뜬다. 그중 하나를 일부러 지워보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> nginx-rs.yaml
kubectl delete pod nginx-rs-b2zdv   <span class="c"># 실제 Pod 이름은 kubectl get pods로 확인</span>
kubectl get pods <span class="nt">-l</span> <span class="nv">app</span><span class="o">=</span>nginx-rs
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME             READY   STATUS    RESTARTS   AGE
nginx-rs-8x4kp   1/1     Running   0          3s
nginx-rs-vcmts   1/1     Running   0          1m
nginx-rs-wtsmm   1/1     Running   0          1m
</code></pre></div></div> <p>지운 자리에 새 Pod가 즉시 생겨났다. 반대 실험도 재밌다. ReplicaSet의 selector와 같은 Label을 단 Pod를 손으로 하나 더 만들면, ReplicaSet은 그 Pod를 자기 소유로 흡수한 뒤 “원하는 개수는 3개인데 4개가 있네”라며 <strong>곧바로 종료시킨다.</strong> 컨트롤러 입장에서는 desired state 3개를 지키는 것이 전부이기 때문이다. 소유 관계는 Pod의 <code class="language-plaintext highlighter-rouge">metadata.ownerReferences</code> 필드에 기록된다.</p> <p>그런데 공식 문서조차 이렇게 말한다. <strong>ReplicaSet을 직접 쓰지 말고 Deployment를 쓰라.</strong> 왜일까? ReplicaSet은 개수만 보장할 뿐 <strong>업데이트 개념이 없다.</strong> template의 이미지를 바꿔서 다시 적용해도 이미 떠 있는 Pod는 그대로다. 새 버전으로 갈아타려면 Pod를 손으로 지워가며 교체해야 한다. 이 업데이트 오케스트레이션을 얹은 상위 리소스가 바로 Deployment다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete rs nginx-rs
</code></pre></div></div> <hr/> <h1 id="5-deployment--롤링-업데이트와-롤백">5. Deployment — 롤링 업데이트와 롤백</h1> <p>Deployment는 ReplicaSet을 직접 만들고 관리하는 상위 컨트롤러다. 계층 구조는 <code class="language-plaintext highlighter-rouge">Deployment → ReplicaSet → Pod</code>이고, 사용자는 맨 위의 Deployment만 만지면 된다. 핵심 가치는 <strong>선언적 업데이트</strong>다. 이미지 버전을 바꾸면 Deployment가 새 ReplicaSet을 만들고, 새것을 점진적으로 키우면서 옛것을 점진적으로 줄여 무중단으로 교체한다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># nginx-deploy.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Deployment</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">nginx-deploy</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.25</span>
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> nginx-deploy.yaml
kubectl get deploy,rs,pods
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                           READY   UP-TO-DATE   AVAILABLE   AGE
deployment.apps/nginx-deploy   3/3     3            3           25s

NAME                                      DESIRED   CURRENT   READY   AGE
replicaset.apps/nginx-deploy-7c5d6b8f9d   3         3         3       25s

NAME                                READY   STATUS    RESTARTS   AGE
pod/nginx-deploy-7c5d6b8f9d-8k2mp   1/1     Running   0          25s
pod/nginx-deploy-7c5d6b8f9d-p4xvz   1/1     Running   0          25s
pod/nginx-deploy-7c5d6b8f9d-tj6q8   1/1     Running   0          25s
</code></pre></div></div> <p>이름을 뜯어보면 계층이 보인다. Deployment 이름(<code class="language-plaintext highlighter-rouge">nginx-deploy</code>) 뒤에 ReplicaSet의 해시(<code class="language-plaintext highlighter-rouge">7c5d6b8f9d</code>)가 붙고, Pod는 거기에 랜덤 접미사가 하나 더 붙는다. Deployment는 자신이 만든 ReplicaSet의 selector에 <code class="language-plaintext highlighter-rouge">pod-template-hash</code> Label을 자동으로 추가해서, 리비전이 달라도 서로의 Pod가 섞이지 않게 만든다.</p> <h2 id="51-스케일링">5.1 스케일링</h2> <p>replicas 변경은 명령 한 줄이다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl scale deployment/nginx-deploy <span class="nt">--replicas</span><span class="o">=</span>5
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>deployment.apps/nginx-deploy scaled
</code></pre></div></div> <p>물론 YAML의 <code class="language-plaintext highlighter-rouge">replicas</code>를 고쳐서 <code class="language-plaintext highlighter-rouge">kubectl apply</code> 해도 결과는 같다. 실무에서는 YAML을 Git으로 관리하므로 후자가 기본이고, <code class="language-plaintext highlighter-rouge">scale</code> 명령은 급할 때 쓰는 응급 처치에 가깝다.</p> <h2 id="52-롤링-업데이트">5.2 롤링 업데이트</h2> <p>이제 하이라이트다. 이미지를 1.25에서 1.26으로 올려보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl <span class="nb">set </span>image deployment/nginx-deploy <span class="nv">nginx</span><span class="o">=</span>nginx:1.26
kubectl rollout status deployment/nginx-deploy
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>deployment.apps/nginx-deploy image updated
Waiting for deployment "nginx-deploy" rollout to finish: 2 out of 5 new replicas have been updated...
Waiting for deployment "nginx-deploy" rollout to finish: 1 old replicas are pending termination...
deployment "nginx-deploy" successfully rolled out
</code></pre></div></div> <p>내부에서 벌어진 일을 그림으로 보면 다음과 같다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-05-workloads/rolling-update-480.webp 480w,/assets/post/image/k8s-05-workloads/rolling-update-800.webp 800w,/assets/post/image/k8s-05-workloads/rolling-update-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-05-workloads/rolling-update.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="롤링 업데이트 흐름 — kubectl set image 실행 후 Deployment가 구 ReplicaSet(1.25)을 점진적으로 0까지 축소하며 Pod를 Terminating시키고, 신 ReplicaSet(1.26)을 점진적으로 5까지 확장해 새 Pod를 띄우는 구조" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>Deployment는 Pod를 직접 갈아치우지 않는다. <strong>새 ReplicaSet을 만들어 키우고, 구 ReplicaSet을 줄이는</strong> 방식으로 교체한다.</li> <li>교체 속도는 <code class="language-plaintext highlighter-rouge">maxSurge</code>(desired보다 몇 개까지 초과 생성 가능한지)와 <code class="language-plaintext highlighter-rouge">maxUnavailable</code>(몇 개까지 동시에 내려가도 되는지)로 제어하며, 기본값은 둘 다 25%다.</li> <li>업데이트 전략(<code class="language-plaintext highlighter-rouge">strategy.type</code>)의 기본값은 <code class="language-plaintext highlighter-rouge">RollingUpdate</code>다. <code class="language-plaintext highlighter-rouge">Recreate</code>로 바꾸면 전부 내리고 전부 올리는 방식이 된다.</li> <li>구 ReplicaSet은 replicas 0으로 남는다. 이것이 롤백의 발판이다.</li> </ul> <p>실제로 ReplicaSet 목록을 보면 구 리비전이 0으로 남아 있는 것을 확인할 수 있다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get rs
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                      DESIRED   CURRENT   READY   AGE
nginx-deploy-6f9c4b7d5b   5         5         5       50s
nginx-deploy-7c5d6b8f9d   0         0         0       6m
</code></pre></div></div> <h2 id="53-롤아웃-이력과-롤백">5.3 롤아웃 이력과 롤백</h2> <p>배포 이력은 <code class="language-plaintext highlighter-rouge">rollout history</code>로 조회한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout <span class="nb">history </span>deployment/nginx-deploy
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>deployment.apps/nginx-deploy
REVISION  CHANGE-CAUSE
1         &lt;none&gt;
2         &lt;none&gt;
</code></pre></div></div> <p>CHANGE-CAUSE가 비어 있는 이유는 이 컬럼이 <code class="language-plaintext highlighter-rouge">kubernetes.io/change-cause</code> 어노테이션 값을 보여주는 것이고, 그 어노테이션은 자동으로 붙지 않기 때문이다. 배포 사유를 남기고 싶으면 직접 기록한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl annotate deployment/nginx-deploy kubernetes.io/change-cause<span class="o">=</span><span class="s2">"nginx 1.26으로 업데이트"</span>
</code></pre></div></div> <p>특정 리비전의 상세 내용(Pod template, 이미지 등)은 <code class="language-plaintext highlighter-rouge">kubectl rollout history deployment/nginx-deploy --revision=2</code>처럼 <code class="language-plaintext highlighter-rouge">--revision</code> 플래그로 본다.</p> <p>새 버전에 문제가 있다면? 구 ReplicaSet이 남아 있으므로 되돌리는 것도 한 줄이다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout undo deployment/nginx-deploy
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>deployment.apps/nginx-deploy rolled back
</code></pre></div></div> <p>특정 리비전으로 지정해서 돌아갈 수도 있다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl rollout undo deployment/nginx-deploy <span class="nt">--to-revision</span><span class="o">=</span>2
</code></pre></div></div> <p>롤백도 결국 “구 ReplicaSet을 다시 키우는” 하나의 새 롤아웃이라서, 되돌린 설정은 새 리비전 번호로 다시 기록된다. 참고로 <code class="language-plaintext highlighter-rouge">rollout</code> 명령에는 <code class="language-plaintext highlighter-rouge">status</code>, <code class="language-plaintext highlighter-rouge">history</code>, <code class="language-plaintext highlighter-rouge">undo</code> 외에 <code class="language-plaintext highlighter-rouge">pause</code>/<code class="language-plaintext highlighter-rouge">resume</code>(롤아웃 일시 정지/재개), <code class="language-plaintext highlighter-rouge">restart</code>(이미지 변경 없이 Pod 전체를 롤링 방식으로 재기동)도 있고, Deployment뿐 아니라 DaemonSet과 StatefulSet에도 쓸 수 있다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete deployment nginx-deploy
</code></pre></div></div> <hr/> <h1 id="6-statefulset--이름과-디스크를-기억하는-워크로드">6. StatefulSet — 이름과 디스크를 기억하는 워크로드</h1> <p>Deployment의 Pod는 <strong>서로 완전히 교체 가능(interchangeable)</strong>하다. 이름은 랜덤 해시고, 누가 죽든 똑같은 놈이 새로 생기면 그만이다. 편의점 아르바이트처럼 누가 근무해도 같은 일을 하면 된다. 무상태(stateless) 웹 서버에는 이게 완벽하다.</p> <p>하지만 데이터베이스는 다르다. MySQL primary와 replica는 역할이 다르고, 각자 <strong>자기 데이터 디스크</strong>를 가지며, 클러스터 멤버끼리 서로를 <strong>고정된 주소</strong>로 알아봐야 한다. 병원의 담당 주치의처럼 “아무나”가 아니라 “그 사람”이어야 하는 것이다. 이런 요구를 위한 워크로드가 StatefulSet이고, 공식 문서 기준으로 다음을 보장한다.</p> <ol> <li><strong>안정적인 네트워크 ID</strong> — Pod 이름이 <code class="language-plaintext highlighter-rouge">&lt;StatefulSet 이름&gt;-&lt;서수&gt;</code> 형식으로 고정된다(<code class="language-plaintext highlighter-rouge">web-0</code>, <code class="language-plaintext highlighter-rouge">web-1</code>, <code class="language-plaintext highlighter-rouge">web-2</code>). Pod가 재스케줄링되어도 이 정체성은 유지되며, headless Service와 결합하면 <code class="language-plaintext highlighter-rouge">web-0.nginx.default.svc.cluster.local</code>처럼 <strong>Pod 개별 DNS 주소</strong>가 생긴다.</li> <li><strong>안정적인 스토리지</strong> — <code class="language-plaintext highlighter-rouge">volumeClaimTemplates</code>로 Pod마다 개별 PVC(예: <code class="language-plaintext highlighter-rouge">www-web-0</code>)가 만들어진다. StatefulSet을 삭제하거나 스케일 다운해도 <strong>PVC는 삭제되지 않는다.</strong> 데이터 안전을 위한 의도적 설계다.</li> <li><strong>순차적 기동과 종료</strong> — 기본 정책(<code class="language-plaintext highlighter-rouge">OrderedReady</code>)에서는 0번부터 순서대로 뜨고, 앞 Pod가 Running &amp; Ready 상태가 되어야 다음 Pod가 생성된다. 종료는 역순이다. 순서가 필요 없으면 <code class="language-plaintext highlighter-rouge">podManagementPolicy: Parallel</code>로 바꿀 수 있다.</li> </ol> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-05-workloads/statefulset-ordered-startup-480.webp 480w,/assets/post/image/k8s-05-workloads/statefulset-ordered-startup-800.webp 800w,/assets/post/image/k8s-05-workloads/statefulset-ordered-startup-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-05-workloads/statefulset-ordered-startup.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="StatefulSet 순차 기동 — web-0 생성 후 Running &amp; Ready를 확인하고 web-1을 생성, 다시 확인 후 web-2를 생성하는 순서" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>기동은 0 → 1 → 2 순서, 종료는 2 → 1 → 0 역순이다. DB 클러스터에서 primary가 먼저 떠야 replica가 붙을 수 있는 상황과 맞아떨어진다.</li> <li>Deployment라면 세 Pod가 동시에 뜬다. 순서 보장이 없는 대신 빠르다.</li> </ul> <p>YAML의 골격은 이렇다. Deployment와 달리 <code class="language-plaintext highlighter-rouge">serviceName</code>(반드시 headless Service를 가리켜야 하고, StatefulSet이 자동으로 만들어주지 않는다)과 <code class="language-plaintext highlighter-rouge">volumeClaimTemplates</code>가 있다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Service</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">clusterIP</span><span class="pi">:</span> <span class="s">None</span> <span class="c1"># headless Service — 06편에서 자세히 다룬다</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
  <span class="na">ports</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">port</span><span class="pi">:</span> <span class="m">80</span>
      <span class="na">name</span><span class="pi">:</span> <span class="s">web</span>
<span class="nn">---</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">apps/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">StatefulSet</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">web</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">serviceName</span><span class="pi">:</span> <span class="s2">"</span><span class="s">nginx"</span>
  <span class="na">replicas</span><span class="pi">:</span> <span class="m">3</span>
  <span class="na">selector</span><span class="pi">:</span>
    <span class="na">matchLabels</span><span class="pi">:</span>
      <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">metadata</span><span class="pi">:</span>
      <span class="na">labels</span><span class="pi">:</span>
        <span class="na">app</span><span class="pi">:</span> <span class="s">nginx</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.25</span>
          <span class="na">ports</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">80</span>
              <span class="na">name</span><span class="pi">:</span> <span class="s">web</span>
  <span class="na">volumeClaimTemplates</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">metadata</span><span class="pi">:</span>
        <span class="na">name</span><span class="pi">:</span> <span class="s">www</span>
      <span class="na">spec</span><span class="pi">:</span>
        <span class="na">accessModes</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">ReadWriteOnce"</span><span class="pi">]</span>
        <span class="na">resources</span><span class="pi">:</span>
          <span class="na">requests</span><span class="pi">:</span>
            <span class="na">storage</span><span class="pi">:</span> <span class="s">1Gi</span>
</code></pre></div></div> <p>PVC와 headless Service를 아직 안 배웠으니 실습은 각각 <a href="/blog/2026/k8s-07-storage-config/">07편</a>과 <a href="/blog/2026/k8s-06-networking/">06편</a>에서 이어간다. 지금은 Deployment와의 차이를 표로 정리해두자.</p> <table> <thead> <tr> <th>항목</th> <th>Deployment</th> <th>StatefulSet</th> </tr> </thead> <tbody> <tr> <td>Pod 이름</td> <td>랜덤 해시 (<code class="language-plaintext highlighter-rouge">nginx-deploy-7c5d…-8k2mp</code>)</td> <td>고정 서수 (<code class="language-plaintext highlighter-rouge">web-0</code>, <code class="language-plaintext highlighter-rouge">web-1</code>)</td> </tr> <tr> <td>Pod 정체성</td> <td>서로 교체 가능</td> <td>재스케줄링에도 고유 ID 유지</td> </tr> <tr> <td>스토리지</td> <td>Pod별 개별 볼륨 개념 없음</td> <td><code class="language-plaintext highlighter-rouge">volumeClaimTemplates</code>로 Pod마다 개별 PVC</td> </tr> <tr> <td>기동/종료 순서</td> <td>보장 없음 (동시)</td> <td>순차 기동, 역순 종료 (기본 <code class="language-plaintext highlighter-rouge">OrderedReady</code>)</td> </tr> <tr> <td>네트워크 접근</td> <td>Service로 집합 전체에 접근</td> <td>headless Service로 Pod별 DNS 주소</td> </tr> <tr> <td>어울리는 앱</td> <td>무상태 웹/API 서버</td> <td>DB, 메시지 브로커 등 상태 저장 앱</td> </tr> </tbody> </table> <p>한 가지 균형 잡힌 시각도 필요하다. 안정적 ID나 순차 기동이 <strong>필요 없다면</strong> StatefulSet을 쓸 이유가 없다. 공식 문서도 무상태 앱에는 Deployment를 쓰라고 명시한다. 운영 난이도는 StatefulSet 쪽이 확실히 높다.</p> <hr/> <h1 id="7-daemonset--노드마다-하나씩">7. DaemonSet — 노드마다 하나씩</h1> <p>마지막 워크로드는 발상이 다르다. “몇 개”가 아니라 <strong>“모든(또는 일부) 노드에 하나씩”</strong>이다. DaemonSet에는 <code class="language-plaintext highlighter-rouge">replicas</code> 필드가 없다. 노드 수가 곧 Pod 수다. 노드가 클러스터에 추가되면 그 노드에 Pod가 자동으로 생기고, 노드가 제거되면 해당 Pod도 정리된다.</p> <p>건물의 층마다 소화기를 하나씩 비치하는 것과 같다. 소화기 개수를 따로 세지 않는다. 층이 늘어나면 소화기도 늘어난다. 공식 문서가 드는 대표 용도는 다음 세 가지다.</p> <ul> <li>모든 노드에서 도는 <strong>클러스터 스토리지 데몬</strong></li> <li>모든 노드에서 도는 <strong>로그 수집 데몬</strong></li> <li>모든 노드에서 도는 <strong>노드 모니터링 데몬</strong></li> </ul> <p>사실 우리는 이미 DaemonSet을 쓰고 있다. kind 클러스터의 <code class="language-plaintext highlighter-rouge">kube-system</code> 네임스페이스를 보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get ds <span class="nt">-n</span> kube-system
</code></pre></div></div> <p>예상 출력 (노드 1개짜리 기본 kind 클러스터 기준, 세부 값은 환경에 따라 다르다):</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME         DESIRED   CURRENT   READY   UP-TO-DATE   AVAILABLE   NODE SELECTOR            AGE
kindnet      1         1         1       1            1           &lt;none&gt;                   3d
kube-proxy   1         1         1       1            1           kubernetes.io/os=linux   3d
</code></pre></div></div> <ul> <li><a href="/blog/2026/k8s-03-architecture/">03편</a>에서 본 <strong>kube-proxy</strong>(Service 트래픽 라우팅)와 <strong>kindnet</strong>(kind의 CNI 플러그인)이 둘 다 DaemonSet이다. “노드마다 반드시 하나”가 필요한 시스템 컴포넌트의 표준 배포 방식인 셈이다.</li> <li>02편에서 멀티노드 config로 클러스터를 만들었다면 DESIRED/CURRENT 숫자가 노드 수만큼 나온다.</li> <li><code class="language-plaintext highlighter-rouge">nodeSelector</code>나 node affinity를 지정하면 “GPU 노드에만”, “Linux 노드에만”처럼 일부 노드로 배치를 제한할 수 있다. 위 출력에서 kube-proxy의 NODE SELECTOR가 그 예다.</li> </ul> <hr/> <h1 id="8-job과-cronjob--끝이-있는-워크로드">8. Job과 CronJob — 끝이 있는 워크로드</h1> <p>지금까지 본 워크로드 4종에는 공통 전제가 있다. <strong>Pod가 계속 떠 있어야 한다</strong>는 것이다. Deployment의 Pod가 종료되면 컨트롤러는 장애로 간주하고 즉시 되살린다. 그런데 세상에는 <strong>끝나는 것이 정상</strong>인 작업도 있다. 배치 데이터 전처리, 모델 평가, DB 마이그레이션 같은 작업이 그렇다. 이런 작업을 Deployment로 돌리면 정상 종료한 Pod를 컨트롤러가 끝없이 재시작하는 이상한 상황이 벌어진다. 한 줄로 비교하면 — <strong>Deployment는 “계속 떠 있음”이 성공이고, Job은 “성공적으로 끝남”이 성공이다.</strong></p> <h2 id="81-job--완료를-보장하는-일회성-실행">8.1 Job — 완료를 보장하는 일회성 실행</h2> <p>Job은 지정한 개수의 Pod가 <strong>성공적으로 종료할 때까지</strong> Pod 생성과 재시도를 책임지는 컨트롤러다. 핵심 필드는 세 개다.</p> <table> <thead> <tr> <th>필드</th> <th>의미</th> <th>기본값</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">completions</code></td> <td>성공해야 하는 Pod 개수</td> <td>1</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">parallelism</code></td> <td>동시에 실행할 최대 Pod 개수</td> <td>1</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">backoffLimit</code></td> <td>Job을 실패로 마킹하기 전까지 허용하는 재시도 횟수</td> <td>6</td> </tr> </tbody> </table> <p>ML 파이프라인에서 흔한 예로, 새 모델 체크포인트가 나올 때마다 평가 스위트를 Job으로 돌리는 경우를 보자.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># model-eval-job.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">batch/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Job</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">model-eval</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">completions</span><span class="pi">:</span> <span class="m">1</span>
  <span class="na">parallelism</span><span class="pi">:</span> <span class="m">1</span>
  <span class="na">backoffLimit</span><span class="pi">:</span> <span class="m">4</span>
  <span class="na">template</span><span class="pi">:</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">containers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">eval</span>
          <span class="na">image</span><span class="pi">:</span> <span class="s">python:3.11-slim</span>
          <span class="na">command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">python"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">-c"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">print('accuracy:</span><span class="nv"> </span><span class="s">0.87')"</span><span class="pi">]</span>
      <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">Never</span>
</code></pre></div></div> <p>눈여겨볼 부분은 <code class="language-plaintext highlighter-rouge">restartPolicy: Never</code>다. Job의 Pod template에는 <code class="language-plaintext highlighter-rouge">restartPolicy</code>를 반드시 <code class="language-plaintext highlighter-rouge">Never</code> 또는 <code class="language-plaintext highlighter-rouge">OnFailure</code>로 지정해야 한다. Pod의 기본값인 <code class="language-plaintext highlighter-rouge">Always</code>는 “계속 떠 있어야 한다”는 의미라 Job과 양립할 수 없기 때문이다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> model-eval-job.yaml
kubectl get <span class="nb">jobs</span>
</code></pre></div></div> <p>실행 직후에는 COMPLETIONS가 <code class="language-plaintext highlighter-rouge">0/1</code>이고, 작업이 끝나면 <code class="language-plaintext highlighter-rouge">1/1</code>이 되면서 Job이 Complete 상태가 된다.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME         COMPLETIONS   DURATION   AGE
model-eval   0/1           3s         3s
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME         COMPLETIONS   DURATION   AGE
model-eval   1/1           6s         30s
</code></pre></div></div> <p>Deployment와 달리, 끝난 Pod는 삭제되지 않고 <code class="language-plaintext highlighter-rouge">Completed</code> 상태로 남는다. 로그를 나중에 확인할 수 있게 하기 위한 설계다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods
</code></pre></div></div> <p>예상 출력:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME               READY   STATUS      RESTARTS   AGE
model-eval-7k2np   0/1     Completed   0          40s
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">kubectl logs job/model-eval</code>로 평가 결과를 확인하고, <code class="language-plaintext highlighter-rouge">kubectl delete job model-eval</code>로 지우면 Pod도 함께 정리된다. 매번 지우기 번거로우면 <code class="language-plaintext highlighter-rouge">ttlSecondsAfterFinished</code> 필드로 완료 후 자동 삭제를 걸 수 있다.</p> <p>실패하면 어떻게 될까. Pod가 실패하면 Job 컨트롤러는 지수 백오프(10초, 20초, 40초, … 최대 6분)를 두고 재시도를 반복하고, 실패가 <code class="language-plaintext highlighter-rouge">backoffLimit</code> 횟수만큼 쌓이면 Job 전체를 Failed로 마킹하고 멈춘다. 위 예시는 <code class="language-plaintext highlighter-rouge">backoffLimit: 4</code>이므로 재시도 4번 안에 성공하지 못하면 포기한다는 뜻이다.</p> <p>병렬 실행도 간단하다. 대규모 이미지 데이터셋을 10개 샤드로 나눠 전처리한다면 <code class="language-plaintext highlighter-rouge">completions: 10</code>, <code class="language-plaintext highlighter-rouge">parallelism: 3</code>으로 선언한다. 동시에 3개씩 돌면서 총 10개의 Pod가 성공할 때까지 실행된다. 여기에 <code class="language-plaintext highlighter-rouge">completionMode: Indexed</code>를 추가하면 각 Pod가 0부터 9까지의 완료 인덱스를 부여받아 “내 번호의 샤드만 처리”하는 구조도 만들 수 있다.</p> <h2 id="82-cronjob--job을-스케줄에-태우기">8.2 CronJob — Job을 스케줄에 태우기</h2> <p>CronJob은 <strong>cron 스케줄에 따라 Job을 반복 생성</strong>하는 컨트롤러다. 계층 구조는 <code class="language-plaintext highlighter-rouge">CronJob → Job → Pod</code>로, <code class="language-plaintext highlighter-rouge">Deployment → ReplicaSet → Pod</code>와 같은 패턴이다. 스케줄 문법은 리눅스 cron과 동일한 5필드다.</p> <table> <thead> <tr> <th>위치 (왼쪽부터)</th> <th>의미</th> <th>허용 값</th> </tr> </thead> <tbody> <tr> <td>1</td> <td>분</td> <td>0–59</td> </tr> <tr> <td>2</td> <td>시</td> <td>0–23</td> </tr> <tr> <td>3</td> <td>일</td> <td>1–31</td> </tr> <tr> <td>4</td> <td>월</td> <td>1–12</td> </tr> <tr> <td>5</td> <td>요일</td> <td>0–6 (0=일요일)</td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">"0 2 * * *"</code>는 매일 02:00, <code class="language-plaintext highlighter-rouge">"*/10 * * * *"</code>는 10분마다, <code class="language-plaintext highlighter-rouge">"0 3 * * 1"</code>은 매주 월요일 03:00이다. 매일 새벽 2시에 어제 쌓인 데이터를 학습용으로 전처리하는 CronJob은 이렇게 쓴다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># nightly-preprocess.yaml</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">batch/v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">CronJob</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">nightly-preprocess</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">schedule</span><span class="pi">:</span> <span class="s2">"</span><span class="s">0</span><span class="nv"> </span><span class="s">2</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">*"</span>
  <span class="na">timeZone</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Asia/Seoul"</span>
  <span class="na">concurrencyPolicy</span><span class="pi">:</span> <span class="s">Forbid</span>
  <span class="na">jobTemplate</span><span class="pi">:</span>
    <span class="na">spec</span><span class="pi">:</span>
      <span class="na">backoffLimit</span><span class="pi">:</span> <span class="m">2</span>
      <span class="na">template</span><span class="pi">:</span>
        <span class="na">spec</span><span class="pi">:</span>
          <span class="na">containers</span><span class="pi">:</span>
            <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">preprocess</span>
              <span class="na">image</span><span class="pi">:</span> <span class="s">python:3.11-slim</span>
              <span class="na">command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">python"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">-c"</span><span class="pi">,</span> <span class="s2">"</span><span class="s">print('preprocessing</span><span class="nv"> </span><span class="s">done')"</span><span class="pi">]</span>
          <span class="na">restartPolicy</span><span class="pi">:</span> <span class="s">OnFailure</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">jobTemplate</code> 안이 곧 Job의 spec이다. 스케줄 시각이 되면 CronJob이 이 틀로 이름 뒤에 스케줄 시각 기반 접미사가 붙은 Job을 만들고, 그 Job이 Pod를 만든다.</li> <li><code class="language-plaintext highlighter-rouge">timeZone</code>을 지정하지 않으면 스케줄은 kube-controller-manager의 로컬 시간대 기준으로 해석된다. 컨테이너 환경은 대개 UTC라서, “새벽 2시” 작업이 KST 오전 11시에 도는 사고가 나기 쉽다.</li> <li>완료된 Job 이력은 기본적으로 성공 3개, 실패 1개까지 보관된다(<code class="language-plaintext highlighter-rouge">successfulJobsHistoryLimit</code>, <code class="language-plaintext highlighter-rouge">failedJobsHistoryLimit</code>).</li> </ul> <p>어제 작업이 아직 안 끝났는데 오늘 스케줄 시각이 오면? 그걸 정하는 것이 <code class="language-plaintext highlighter-rouge">concurrencyPolicy</code>다.</p> <table> <thead> <tr> <th>값</th> <th>동작</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">Allow</code></td> <td>동시 실행을 허용한다 (기본값)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Forbid</code></td> <td>이전 Job이 안 끝났으면 이번 회차를 건너뛴다</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Replace</code></td> <td>이전 Job을 중단시키고 새 Job으로 교체한다</td> </tr> </tbody> </table> <p>데이터 전처리처럼 같은 대상을 두 번 건드리면 안 되는 작업에는 <code class="language-plaintext highlighter-rouge">Forbid</code>가 안전하다. 마지막으로 설계 원칙 하나 — 공식 문서는 드물게 한 회차에 Job이 두 번 만들어지거나 아예 안 만들어질 수 있으므로 <strong>Job을 멱등(idempotent)하게 작성하라</strong>고 명시한다. 같은 작업이 두 번 돌아도 결과가 같아야 한다는 뜻이다.</p> <hr/> <h1 id="9-어떤-워크로드를-골라야-할까">9. 어떤 워크로드를 골라야 할까</h1> <p>배운 것을 선택 가이드 표 하나로 압축하면 이렇다.</p> <table> <thead> <tr> <th>상황</th> <th>리소스</th> <th>이유</th> </tr> </thead> <tbody> <tr> <td>무상태 웹/API 서버를 무중단으로 배포·스케일링</td> <td>Deployment</td> <td>롤링 업데이트 + 롤백 + replicas 보장</td> </tr> <tr> <td>DB, 메시지 브로커 등 상태 저장 앱</td> <td>StatefulSet</td> <td>고정된 Pod ID + Pod별 PVC + 순차 기동</td> </tr> <tr> <td>로그 수집기·모니터링 에이전트를 전 노드에 배치</td> <td>DaemonSet</td> <td>노드당 정확히 1개, 노드 증감에 자동 대응</td> </tr> <tr> <td>모델 평가·데이터 전처리 같은 일회성 배치 작업</td> <td>Job</td> <td>“성공 종료”가 목표, backoffLimit까지 재시도</td> </tr> <tr> <td>야간 전처리 등 주기적으로 반복되는 배치 작업</td> <td>CronJob</td> <td>cron 스케줄에 따라 Job을 반복 생성</td> </tr> <tr> <td>ReplicaSet을 직접 생성</td> <td>(거의 없음)</td> <td>Deployment가 대신 만들어 관리한다</td> </tr> </tbody> </table> <p>이 글의 핵심을 한 줄로 요약하면 — <strong>Pod는 직접 만들지 말고, 원하는 상태를 워크로드 리소스로 선언해서 컨트롤러가 지키게 하라.</strong></p> <p>그런데 아직 큰 구멍이 하나 남았다. Deployment가 Pod 5개를 잘 유지해줘도, Pod IP는 재시작할 때마다 바뀐다. 클라이언트는 대체 어디로 접속해야 할까? StatefulSet에서 슬쩍 등장한 headless Service의 정체는 무엇일까?</p> <blockquote> <p>이전 글 <a href="/blog/2026/k8s-04-pod/">04: Pod의 모든 것 — 생성부터 스케줄링까지</a>에서는 Pod 하나가 뜨는 전 과정을 해부했다. 다음 글 <a href="/blog/2026/k8s-06-networking/">06: 네트워킹 — Service와 Ingress</a>에서는 끊임없이 바뀌는 Pod들 앞에 고정된 접점을 세우는 방법을 다룬다.</p> </blockquote> <hr/> <h1 id="참고-문헌">참고 문헌</h1> <ul> <li>시리즈 로드맵 출처: <a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua, SK Devocean, 2024)</li> <li><a href="https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/">Labels and Selectors — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/">ReplicaSet — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/concepts/workloads/controllers/deployment/">Deployments — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/reference/kubernetes-api/workload-resources/deployment-v1/">Deployment v1 API Reference — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/reference/kubectl/generated/kubectl_rollout/">kubectl rollout — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/">StatefulSets — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/">DaemonSet — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/concepts/workloads/controllers/job/">Jobs — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/reference/kubernetes-api/workload-resources/job-v1/">Job v1 API Reference — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/concepts/workloads/controllers/cron-jobs/">CronJob — Kubernetes 공식 문서</a></li> <li><a href="https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/">Namespaces — Kubernetes 공식 문서</a></li> </ul>]]></content><author><name></name></author><category term="infra"/><category term="kubernetes"/><category term="infra"/><category term="deployment"/><category term="replicaset"/><category term="statefulset"/><category term="daemonset"/><category term="job"/><category term="cronjob"/><category term="namespace"/><summary type="html"><![CDATA[Pod를 감싸는 상위 워크로드 리소스 4종 — Label/Selector부터 롤링 업데이트와 롤백, StatefulSet의 순차 기동, DaemonSet까지 kind 클러스터로 실습한다]]></summary></entry><entry><title type="html">Pod의 모든 것 — 생성부터 스케줄링까지</title><link href="https://www.wonbeomjang.kr/blog/2026/k8s-04-pod/" rel="alternate" type="text/html" title="Pod의 모든 것 — 생성부터 스케줄링까지"/><published>2026-07-06T19:00:00+00:00</published><updated>2026-07-06T19:00:00+00:00</updated><id>https://www.wonbeomjang.kr/blog/2026/k8s-04-pod</id><content type="html" xml:base="https://www.wonbeomjang.kr/blog/2026/k8s-04-pod/"><![CDATA[<blockquote> <p>이 글은 <strong>K8s 입문 시리즈</strong>의 네 번째 글이다.</p> <ul> <li><a href="/blog/2026/k8s-01-history/">01: Kubernetes의 탄생 — Google Borg에서 CNCF까지</a></li> <li><a href="/blog/2026/k8s-02-local-setup/">02: 내 노트북에 클러스터 만들기 — kind와 kubectl</a></li> <li><a href="/blog/2026/k8s-03-architecture/">03: Kubernetes 아키텍처 — Control Plane과 Node</a></li> <li><strong>04: Pod의 모든 것 — 생성부터 스케줄링까지</strong> ← 현재 글</li> <li><a href="/blog/2026/k8s-05-workloads/">05: 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</a></li> <li><a href="/blog/2026/k8s-06-networking/">06: 네트워킹 — Service와 Ingress</a></li> <li><a href="/blog/2026/k8s-07-storage-config/">07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</a></li> <li><a href="/blog/2026/k8s-08-rbac/">08: 권한 관리 — ServiceAccount와 RBAC</a></li> <li><a href="/blog/2026/k8s-09-operator-cncf/">09: 확장과 생태계 — Operator와 CNCF Projects</a></li> </ul> <p>이 시리즈의 커리큘럼은 SK Devocean의 <a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua) 글의 학습 로드맵을 바탕으로 구성했다.</p> </blockquote> <p><a href="/blog/2026/k8s-03-architecture/">03편</a>에서 클러스터를 해부해 Control Plane과 Node의 컴포넌트를 하나씩 살펴봤다. 이제 그 무대 위에 올라가는 주인공, <strong>Pod</strong>를 다룰 차례다.</p> <p>Docker에서는 <code class="language-plaintext highlighter-rouge">docker run</code>으로 컨테이너를 직접 띄웠다. 그런데 Kubernetes에는 “컨테이너를 만들어라”라는 명령이 없다. Kubernetes가 만들고, 지우고, 옮기는 최소 단위는 컨테이너가 아니라 <strong>Pod</strong>이기 때문이다. 앞으로 배울 Deployment, StatefulSet, DaemonSet 같은 워크로드 리소스도 결국 전부 Pod를 찍어내는 틀이다. Pod를 정확히 이해하지 못하면 그 위의 모든 개념이 흔들린다.</p> <p>이번 글에서는 Pod가 무엇인지, 왜 컨테이너가 아니라 Pod인지 이해하고, 직접 nginx Pod를 띄워본 다음, <code class="language-plaintext highlighter-rouge">kubectl apply</code> 한 줄 뒤에서 03편의 컴포넌트들이 어떻게 협업해 Pod를 띄우는지 5단계로 추적한다. 후반부에서는 kubelet의 헬스체크인 <strong>probe</strong>, 스케줄링과 직결되는 <strong>리소스 requests/limits</strong>, 그리고 Pod가 이상할 때 원인을 찾는 <strong>디버깅 루틴</strong>까지 다진다.</p> <hr/> <h1 id="1-pod란-무엇인가">1. Pod란 무엇인가</h1> <p>공식 문서의 정의는 이렇다.</p> <blockquote> <p>Pod는 Kubernetes에서 생성하고 관리할 수 있는 <strong>가장 작은 배포 단위</strong>다. Pod는 하나 이상의 컨테이너 그룹이며, 스토리지와 네트워크를 공유하고, 컨테이너를 어떻게 실행할지에 대한 명세를 담는다.</p> </blockquote> <p>핵심은 세 가지다.</p> <table> <thead> <tr> <th>특징</th> <th>의미</th> </tr> </thead> <tbody> <tr> <td><strong>최소 배포 단위</strong></td> <td>Kubernetes는 컨테이너를 개별로 다루지 않는다. 스케줄링·생성·삭제 모두 Pod 단위다</td> </tr> <tr> <td><strong>컨테이너 그룹</strong></td> <td>하나의 Pod에 여러 컨테이너를 담을 수 있다 (대부분은 1개)</td> </tr> <tr> <td><strong>자원 공유</strong></td> <td>같은 Pod의 컨테이너들은 <strong>하나의 IP(네트워크 네임스페이스)</strong>와 <strong>볼륨</strong>을 공유한다</td> </tr> </tbody> </table> <p>비유하면 Pod는 <strong>셰어하우스 한 집</strong>이다.</p> <ul> <li><strong>주소(IP)는 집마다 하나</strong>다. 방(컨테이너)마다 주소가 따로 있지 않다.</li> <li>룸메이트(컨테이너)끼리는 집 안에서 바로 대화한다 — <strong>localhost 통신</strong>.</li> <li>거실(공유 볼륨)에 물건을 두면 모든 룸메이트가 쓸 수 있다.</li> <li>이사(스케줄링)를 가면 <strong>집 전체가 같이 움직인다</strong>. 룸메이트 한 명만 다른 동네(노드)로 갈 수 없다.</li> </ul> <p>같은 Pod 안의 컨테이너와 다른 Pod의 컨테이너는 이렇게 다르다.</p> <table> <thead> <tr> <th>항목</th> <th>같은 Pod 안의 컨테이너</th> <th>다른 Pod의 컨테이너</th> </tr> </thead> <tbody> <tr> <td>IP 주소</td> <td>하나를 공유</td> <td>각자 다름</td> </tr> <tr> <td>통신 방법</td> <td>localhost + 포트</td> <td>Pod IP 또는 Service(06편) 경유</td> </tr> <tr> <td>볼륨 공유</td> <td>가능</td> <td>기본적으로 불가</td> </tr> <tr> <td>배치(노드)</td> <td>항상 같은 노드</td> <td>서로 다른 노드일 수 있음</td> </tr> <tr> <td>생명주기</td> <td>함께 생성되고 함께 삭제됨</td> <td>독립적</td> </tr> </tbody> </table> <hr/> <h1 id="2-왜-컨테이너가-아니라-pod인가">2. 왜 컨테이너가 아니라 Pod인가</h1> <p>“어차피 Pod 하나에 컨테이너 하나가 대부분이라면, 그냥 컨테이너를 최소 단위로 하면 되지 않나?”라는 의문이 든다. Pod라는 한 겹의 추상화가 존재하는 이유는 <strong>강하게 결합된 컨테이너 묶음</strong>을 표현하기 위해서다.</p> <p>대표적인 예가 <strong>사이드카(sidecar) 패턴</strong>이다. 공식 문서는 사이드카를 이렇게 설명한다 — 주 애플리케이션 컨테이너의 코드를 고치지 않고, <strong>로깅·모니터링·보안·데이터 동기화</strong> 같은 부가 기능을 옆에 붙은 보조 컨테이너로 확장하는 패턴이다.</p> <p>로그 수집을 예로 들어보자. nginx는 로그를 파일로 남길 뿐, 중앙 로그 서버로 보내는 기능은 없다. 이때 nginx 이미지를 뜯어고치는 대신, <strong>로그 수집기 컨테이너를 같은 Pod에 태우면</strong> 된다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-04-pod/sidecar-pattern-480.webp 480w,/assets/post/image/k8s-04-pod/sidecar-pattern-800.webp 800w,/assets/post/image/k8s-04-pod/sidecar-pattern-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-04-pod/sidecar-pattern.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="사이드카 패턴 — 같은 Pod 안의 앱 컨테이너(nginx)와 사이드카 컨테이너(로그 수집기)가 공유 볼륨(/var/log/nginx)과 localhost로 통신하고, 사이드카가 중앙 로그 서버로 로그를 전송하는 구조" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>앱과 사이드카는 <strong>공유 볼륨</strong>으로 파일을 주고받는다. 네트워크 설정이 전혀 필요 없다.</li> <li>두 컨테이너는 <strong>항상 같은 노드에 함께 스케줄링</strong>된다. 로그 수집기만 다른 노드에 떨어지는 일이 없다.</li> <li>앱 컨테이너는 자기 일(웹 서빙)만 하고, 로그 전송은 사이드카가 맡는다 — <strong>관심사의 분리</strong>.</li> <li>필요하면 localhost로도 통신할 수 있다. 같은 네트워크 네임스페이스라 IP가 같고 포트만 나눠 쓴다.</li> </ul> <p>참고로 Kubernetes v1.29부터는 사이드카 컨테이너가 정식 기능으로 기본 활성화됐다. <code class="language-plaintext highlighter-rouge">initContainers</code>에 <code class="language-plaintext highlighter-rouge">restartPolicy: Always</code>를 지정하면, Pod가 살아 있는 동안 계속 실행되는 사이드카가 된다. 지금은 “같은 Pod에 보조 컨테이너를 태우는 패턴”이라는 개념만 기억하면 충분하다.</p> <p>정리하면 — <strong>Pod는 “반드시 함께 배포되고, 함께 스케일링되어야 하는 컨테이너들”의 경계선</strong>이다. 이 경계선이 있어야 스케줄러는 “이 묶음은 쪼갤 수 없다”는 것을 알고 통째로 배치할 수 있다.</p> <hr/> <h1 id="3-실습-1-nginx-pod-띄워보기">3. 실습 1: nginx Pod 띄워보기</h1> <p><a href="/blog/2026/k8s-02-local-setup/">02편</a>에서 만든 kind 클러스터에 nginx Pod를 직접 띄워보자. 아래 YAML은 공식 문서의 예제 그대로다. <code class="language-plaintext highlighter-rouge">nginx-pod.yaml</code>로 저장한다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Pod</span>
<span class="na">metadata</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
<span class="na">spec</span><span class="pi">:</span>
  <span class="na">containers</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">nginx</span>
      <span class="na">image</span><span class="pi">:</span> <span class="s">nginx:1.14.2</span>
      <span class="na">ports</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">80</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">apiVersion: v1</code>, <code class="language-plaintext highlighter-rouge">kind: Pod</code> — “Pod라는 종류의 리소스를 core API 그룹 v1 스펙으로 만든다”는 선언.</li> <li><code class="language-plaintext highlighter-rouge">metadata.name</code> — 이 Pod의 이름. 같은 네임스페이스 안에서 유일해야 한다.</li> <li><code class="language-plaintext highlighter-rouge">spec.containers</code> — 이 Pod에 담을 컨테이너 목록. 지금은 nginx 하나뿐이다.</li> </ul> <h2 id="31-생성--kubectl-apply">3.1 생성 — kubectl apply</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> nginx-pod.yaml
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pod/nginx created
</code></pre></div></div> <h2 id="32-확인--get-pods--o-wide">3.2 확인 — get pods -o wide</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-o</span> wide
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME    READY   STATUS    RESTARTS   AGE   IP           NODE                 NOMINATED NODE   READINESS GATES
nginx   1/1     Running   0          30s   10.244.0.5   kind-control-plane   &lt;none&gt;           &lt;none&gt;
</code></pre></div></div> <p>(IP·노드 이름·시간은 환경마다 다르다.)</p> <ul> <li><code class="language-plaintext highlighter-rouge">READY 1/1</code> — 컨테이너 1개 중 1개가 준비됨.</li> <li><code class="language-plaintext highlighter-rouge">STATUS Running</code> — Pod가 노드에 배치됐고 컨테이너가 실행 중이다 (뒤의 생명주기 절에서 자세히).</li> <li><code class="language-plaintext highlighter-rouge">-o wide</code>를 붙이면 <strong>Pod의 IP</strong>와 <strong>어느 노드에 배치됐는지</strong>까지 보인다. 이 IP가 바로 셰어하우스의 “집 주소”다.</li> </ul> <h2 id="33-상세-조회--describe">3.3 상세 조회 — describe</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl describe pod nginx
</code></pre></div></div> <p>출력이 길어서 마지막의 <code class="language-plaintext highlighter-rouge">Events</code> 부분만 보면 이렇다.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Events:
  Type    Reason     Age   From               Message
  ----    ------     ----  ----               -------
  Normal  Scheduled  50s   default-scheduler  Successfully assigned default/nginx to kind-control-plane
  Normal  Pulling    49s   kubelet            Pulling image "nginx:1.14.2"
  Normal  Pulled     40s   kubelet            Successfully pulled image "nginx:1.14.2"
  Normal  Created    40s   kubelet            Created container nginx
  Normal  Started    40s   kubelet            Started container nginx
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">From</code> 열을 보면 <strong>누가 무슨 일을 했는지</strong>가 그대로 드러난다. <code class="language-plaintext highlighter-rouge">default-scheduler</code>가 노드를 배정했고, 그 뒤는 전부 <code class="language-plaintext highlighter-rouge">kubelet</code>의 작업이다. 이 순서가 바로 다음 절에서 다룰 “Pod가 뜨는 5단계”의 흔적이다.</p> <h2 id="34-접속--port-forward">3.4 접속 — port-forward</h2> <p>Pod IP는 클러스터 내부 주소라서 노트북 브라우저로는 바로 접근할 수 없다. <code class="language-plaintext highlighter-rouge">kubectl port-forward</code>로 로컬 포트를 Pod 포트에 연결한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl port-forward pod/nginx 8080:80
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Forwarding from 127.0.0.1:8080 -&gt; 80
Forwarding from [::1]:8080 -&gt; 80
</code></pre></div></div> <p>이 명령은 종료되지 않고 계속 떠 있는다. <strong>다른 터미널</strong>에서 접속해 보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-s</span> localhost:8080 | <span class="nb">head</span> <span class="nt">-n</span> 4
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;head&gt;
&lt;title&gt;Welcome to nginx!&lt;/title&gt;
</code></pre></div></div> <p>nginx 환영 페이지가 나오면 성공이다. 확인이 끝나면 port-forward 터미널에서 Ctrl+C로 끊는다.</p> <h2 id="35-삭제--delete">3.5 삭제 — delete</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod nginx
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pod "nginx" deleted
</code></pre></div></div> <p>여기서 중요한 사실 하나 — <strong>Pod는 스스로 부활하지 않는다.</strong> 지금처럼 직접 만든 Pod는 지우면 그걸로 끝이다. “죽으면 다시 살려라”는 <a href="/blog/2026/k8s-05-workloads/">05편</a>의 워크로드 리소스가 하는 일이다.</p> <hr/> <h1 id="4-pod가-뜨는-5단계">4. Pod가 뜨는 5단계</h1> <p><code class="language-plaintext highlighter-rouge">kubectl apply</code>를 치고 <code class="language-plaintext highlighter-rouge">Running</code>이 되기까지, 뒤에서는 무슨 일이 벌어질까? <a href="/blog/2026/k8s-03-architecture/">03편</a>에서 배운 컴포넌트들이 릴레이처럼 일을 넘겨받는다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-04-pod/pod-creation-flow-480.webp 480w,/assets/post/image/k8s-04-pod/pod-creation-flow-800.webp 800w,/assets/post/image/k8s-04-pod/pod-creation-flow-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-04-pod/pod-creation-flow.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="Pod가 뜨는 5단계 시퀀스 다이어그램 — 사용자가 kubectl apply를 실행하면 kubectl이 kube-apiserver로 Pod 스펙을 전송하고, api-server가 etcd에 저장한 뒤, kube-scheduler가 노드를 선택해 바인딩을 요청하고, kubelet이 컨테이너를 실행하는 흐름" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>각 단계를 03편의 컴포넌트와 연결해 뜯어보자.</p> <p><strong>1단계 — 사용자가 YAML을 작성한다.</strong> YAML은 “이런 Pod가 존재해야 한다”는 <strong>desired state의 선언</strong>이다. 명령(“실행해!”)이 아니라 상태(“존재해야 한다”)를 적는다는 점이 Kubernetes 철학의 핵심이다.</p> <p><strong>2단계 — kubectl이 kube-apiserver로 보낸다.</strong> kubectl은 kubeconfig(02편)에 적힌 api-server 주소로 REST 요청을 날리는 클라이언트일 뿐이다. 마법은 전부 서버 쪽에서 일어난다.</p> <p><strong>3단계 — kube-apiserver가 etcd에 저장한다.</strong> api-server는 인증·인가·어드미션 검증을 통과한 Pod 객체를 etcd에 기록한다(03편에서 봤듯 etcd에 접근하는 유일한 컴포넌트다). 이 시점의 Pod는 <strong>아직 어느 노드에서 돌지 정해지지 않은 레코드</strong>일 뿐이다. <code class="language-plaintext highlighter-rouge">kubectl apply</code>가 성공했다는 것은 “컨테이너가 떴다”가 아니라 <strong>“희망 상태가 저장됐다”</strong>는 뜻이다.</p> <p><strong>4단계 — kube-scheduler가 노드를 고른다.</strong> 스케줄러는 api-server를 watch하다가 <strong>노드가 배정되지 않은 새 Pod</strong>를 발견하면, 두 단계로 최적의 노드를 고른다(03편에서 예고한 그 2단계다).</p> <table> <thead> <tr> <th>단계</th> <th>하는 일</th> <th>비유</th> </tr> </thead> <tbody> <tr> <td><strong>필터링(Filtering)</strong></td> <td>이 Pod를 돌릴 수 <strong>있는</strong> 노드만 남긴다 (예: 요청한 CPU/메모리가 충분한가)</td> <td>서류 전형 — 자격 미달 탈락</td> </tr> <tr> <td><strong>스코어링(Scoring)</strong></td> <td>남은 노드에 점수를 매겨 <strong>가장 적합한</strong> 노드를 고른다</td> <td>면접 — 최고 득점자 선발</td> </tr> </tbody> </table> <p>최고 점수 노드가 여럿이면 그중 하나를 무작위로 고른다. 결정이 끝나면 스케줄러는 api-server에 “이 Pod는 이 노드로”라고 알리는데, 이를 <strong>바인딩(binding)</strong>이라 한다. 스케줄러가 직접 노드에 명령하는 것이 아니라는 점에 주목하자.</p> <p><strong>5단계 — kubelet이 컨테이너를 실행한다.</strong> 배정된 노드의 kubelet은 api-server를 통해 자기 노드 몫의 Pod를 발견하고, CRI를 통해 컨테이너 런타임에 실행을 위임한다. 이미지를 당겨오고(Pulling), 컨테이너를 만들고(Created), 시작한다(Started). kubelet은 이후에도 PodSpec대로 컨테이너가 건강하게 돌고 있는지 계속 책임진다.</p> <p>이 흐름에서 관찰할 점:</p> <ul> <li><strong>컴포넌트끼리 직접 명령하지 않는다.</strong> 모든 상호작용은 api-server를 거치고, 각 컴포넌트는 etcd에 저장된 상태를 watch하다가 <strong>자기 몫의 일</strong>을 알아서 한다. 덕분에 컴포넌트 하나가 잠시 죽어도 재시작 후 상태를 보고 이어서 일할 수 있다.</li> <li><strong>비동기다.</strong> apply 성공(3단계)과 컨테이너 실행(5단계) 사이에는 시간차가 있다. <code class="language-plaintext highlighter-rouge">STATUS</code>가 <code class="language-plaintext highlighter-rouge">Pending</code>에서 <code class="language-plaintext highlighter-rouge">Running</code>으로 바뀌는 데 시간이 걸리는 이유다.</li> <li>03편의 “desired state vs current state” 구도가 그대로 재현된다. YAML(희망 상태)을 저장하면, 나머지 컴포넌트들이 현실을 그 상태에 맞춘다.</li> </ul> <hr/> <h1 id="5-실습-2-이벤트로-5단계-추적하기">5. 실습 2: 이벤트로 5단계 추적하기</h1> <p>방금 설명한 5단계는 이론이 아니라 <strong>이벤트(Event)</strong>로 직접 확인할 수 있다. Pod를 다시 만들고 이벤트를 시간순으로 정렬해 보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl apply <span class="nt">-f</span> nginx-pod.yaml
kubectl get events <span class="nt">--sort-by</span><span class="o">=</span>.metadata.creationTimestamp
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LAST SEEN   TYPE     REASON      OBJECT      MESSAGE
20s         Normal   Scheduled   pod/nginx   Successfully assigned default/nginx to kind-control-plane
19s         Normal   Pulling     pod/nginx   Pulling image "nginx:1.14.2"
12s         Normal   Pulled      pod/nginx   Successfully pulled image "nginx:1.14.2"
12s         Normal   Created     pod/nginx   Created container nginx
12s         Normal   Started     pod/nginx   Started container nginx
</code></pre></div></div> <p>(메시지 형식과 소요 시간은 버전·환경에 따라 조금 다르다. 이미지가 이미 캐시되어 있으면 Pulling 없이 바로 Pulled가 나오기도 한다.)</p> <p>이벤트와 5단계를 매핑하면 이렇다.</p> <table> <thead> <tr> <th>이벤트 Reason</th> <th>발행 주체</th> <th>5단계 중</th> <th>의미</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">Scheduled</code></td> <td>default-scheduler</td> <td>4단계 (바인딩)</td> <td>노드 배정 완료</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Pulling</code></td> <td>kubelet</td> <td>5단계</td> <td>컨테이너 이미지 다운로드 시작</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Pulled</code></td> <td>kubelet</td> <td>5단계</td> <td>이미지 다운로드 완료</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Created</code></td> <td>kubelet</td> <td>5단계</td> <td>컨테이너 생성</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Started</code></td> <td>kubelet</td> <td>5단계</td> <td>컨테이너 시작</td> </tr> </tbody> </table> <p>1~3단계(작성→전송→저장)는 이벤트로 남지 않는다. 이벤트는 “저장된 희망 상태를 현실로 만드는 과정”에서 발생하기 때문이다. 문제가 생겼을 때 <code class="language-plaintext highlighter-rouge">kubectl describe pod &lt;이름&gt;</code>의 Events 섹션을 가장 먼저 보라는 조언이 나오는 이유가 여기에 있다 — <strong>어느 단계에서 멈췄는지</strong>가 그대로 보인다.</p> <p>확인이 끝났으면 정리하자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl delete pod nginx
</code></pre></div></div> <hr/> <h1 id="6-pod-생명주기--5가지-phase">6. Pod 생명주기 — 5가지 phase</h1> <p><code class="language-plaintext highlighter-rouge">kubectl get pods</code>의 STATUS 열 뒤에는 공식적으로 정의된 <strong>phase</strong>가 있다. Pod는 생애 동안 다음 다섯 phase 중 하나에 속한다.</p> <table> <thead> <tr> <th>Phase</th> <th>의미</th> <th>언제 보이나</th> </tr> </thead> <tbody> <tr> <td><strong>Pending</strong></td> <td>클러스터가 Pod를 수락했지만, 컨테이너가 아직 하나도 준비되지 않음</td> <td>스케줄링 대기 중이거나 이미지 다운로드 중 (4~5단계 사이)</td> </tr> <tr> <td><strong>Running</strong></td> <td>노드에 바인딩됐고 모든 컨테이너가 생성됨. 최소 1개가 실행 중이거나 시작·재시작 중</td> <td>정상 동작 중인 대부분의 서비스 Pod</td> </tr> <tr> <td><strong>Succeeded</strong></td> <td>모든 컨테이너가 <strong>성공적으로 종료</strong>됐고 재시작되지 않음</td> <td>배치 작업처럼 끝이 있는 Pod가 정상 완료됐을 때</td> </tr> <tr> <td><strong>Failed</strong></td> <td>모든 컨테이너가 종료됐고, 최소 1개가 <strong>실패</strong>로 종료됨(0이 아닌 종료 코드 등)</td> <td>작업이 비정상 종료됐을 때</td> </tr> <tr> <td><strong>Unknown</strong></td> <td>Pod 상태를 알 수 없음</td> <td>주로 Pod가 있는 노드와의 통신 장애</td> </tr> </tbody> </table> <p>phase와 별개로, Pod 안의 <strong>컨테이너</strong>는 <code class="language-plaintext highlighter-rouge">Waiting</code>(시작 준비 중 — 이미지 풀 등), <code class="language-plaintext highlighter-rouge">Running</code>(실행 중), <code class="language-plaintext highlighter-rouge">Terminated</code>(종료됨)의 세 가지 상태를 가진다.</p> <p>한 가지 주의 — <code class="language-plaintext highlighter-rouge">kubectl get pods</code>의 STATUS 열에는 <code class="language-plaintext highlighter-rouge">CrashLoopBackOff</code>, <code class="language-plaintext highlighter-rouge">Terminating</code> 같은 값도 보이는데, 이것들은 <strong>phase가 아니다</strong>. STATUS는 사용자가 상황을 직관적으로 파악하도록 kubectl이 가공해 보여주는 필드이고, phase는 Pod API에 정의된 공식 값 5가지뿐이다.</p> <hr/> <h1 id="7-probes--kubelet의-헬스체크">7. Probes — kubelet의 헬스체크</h1> <p><code class="language-plaintext highlighter-rouge">STATUS</code>가 <code class="language-plaintext highlighter-rouge">Running</code>이라는 것은 “컨테이너 프로세스가 떠 있다”는 뜻이지, “서비스할 준비가 됐다”는 뜻이 아니다. 프로세스는 살아 있는데 데드락에 걸려 응답을 못 할 수도 있고, 이제 막 떠서 초기화가 안 끝났을 수도 있다. 그래서 kubelet은 <strong>probe</strong>라는 헬스체크로 컨테이너에게 주기적으로 질문을 던진다. probe를 정의하지 않으면 kubelet은 결과를 성공으로 간주한다 — “프로세스만 떠 있으면 건강하다”는 가장 느슨한 기준이 적용되는 셈이다.</p> <p>probe는 세 종류가 있고, 핵심은 <strong>실패했을 때 벌어지는 일이 서로 다르다</strong>는 점이다.</p> <ul> <li><strong>livenessProbe — “살아 있나?”</strong> 실패가 누적되면 kubelet이 컨테이너를 죽이고 restartPolicy에 따라 <strong>재시작</strong>한다. 프로세스는 떠 있지만 응답하지 못하는 데드락 같은 상황을 재시작으로 복구하는 용도다.</li> <li><strong>readinessProbe — “지금 트래픽 받을 수 있나?”</strong> 실패하면 Pod가 unready로 표시되고 Service(06편)의 엔드포인트 목록(EndpointSlice)에서 Pod IP가 <strong>제외</strong>된다. 트래픽만 끊길 뿐 <strong>컨테이너는 재시작되지 않으며</strong>, 다시 성공하면 트래픽도 돌아온다. 워밍업·일시적 과부하·외부 의존성 대기처럼 “죽진 않았지만 지금은 곤란한” 상황에 쓴다.</li> <li><strong>startupProbe — “시작이 끝났나?”</strong> 이 probe가 성공할 때까지 Kubernetes는 <strong>liveness·readiness probe를 실행하지 않는다</strong>. 기동이 느린 앱이 초기화를 마치기도 전에 liveness에게 “죽었다”고 오판받아 재시작당하는 것을 막는다. 실패 한도를 넘기면 컨테이너는 죽고 restartPolicy를 따른다. 시작할 때만 동작하고, 한 번 성공하면 다시 실행되지 않는다.</li> </ul> <p>식당에 비유하면 이렇다.</p> <ul> <li><strong>liveness</strong> = “주방장 쓰러졌어?” — 쓰러졌으면 새 주방장으로 교체한다(재시작).</li> <li><strong>readiness</strong> = “지금 주문 받을 수 있어?” — 재료 준비가 안 됐으면 ‘준비중’ 팻말만 건다(트래픽 차단). 주방장을 자르진 않는다.</li> <li><strong>startup</strong> = 개업 준비 기간 — 준비가 끝나기 전까지는 위의 두 질문 자체를 하지 않는다.</li> </ul> <p>질문하는 방식은 probe마다 하나를 골라 지정한다.</p> <table> <thead> <tr> <th>방식</th> <th>하는 일</th> <th>성공 기준</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">httpGet</code></td> <td>지정한 경로·포트로 HTTP GET 요청</td> <td>상태 코드 200 이상 400 미만</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">tcpSocket</code></td> <td>지정한 포트로 TCP 연결 시도</td> <td>연결 성공(포트 열림)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">exec</code></td> <td>컨테이너 안에서 명령 실행</td> <td>종료 코드 0</td> </tr> </tbody> </table> <p>(gRPC 서버라면 <code class="language-plaintext highlighter-rouge">grpc</code> 방식도 쓸 수 있다.)</p> <p>공식 문서의 예제를 컨테이너 spec 하나에 모으면 이런 모양이다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">containers</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">web</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">my-web:1.0</span>
    <span class="na">ports</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">containerPort</span><span class="pi">:</span> <span class="m">8080</span>
    <span class="na">startupProbe</span><span class="pi">:</span>
      <span class="na">httpGet</span><span class="pi">:</span>
        <span class="na">path</span><span class="pi">:</span> <span class="s">/healthz</span>
        <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">failureThreshold</span><span class="pi">:</span> <span class="m">30</span>
      <span class="na">periodSeconds</span><span class="pi">:</span> <span class="m">10</span>
    <span class="na">livenessProbe</span><span class="pi">:</span>
      <span class="na">httpGet</span><span class="pi">:</span>
        <span class="na">path</span><span class="pi">:</span> <span class="s">/healthz</span>
        <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">initialDelaySeconds</span><span class="pi">:</span> <span class="m">10</span>
      <span class="na">periodSeconds</span><span class="pi">:</span> <span class="m">5</span>
    <span class="na">readinessProbe</span><span class="pi">:</span>
      <span class="na">httpGet</span><span class="pi">:</span>
        <span class="na">path</span><span class="pi">:</span> <span class="s">/ready</span>
        <span class="na">port</span><span class="pi">:</span> <span class="m">8080</span>
      <span class="na">periodSeconds</span><span class="pi">:</span> <span class="m">5</span>
</code></pre></div></div> <ul> <li><strong>startupProbe</strong> — 10초 간격(<code class="language-plaintext highlighter-rouge">periodSeconds</code>)으로 최대 30번(<code class="language-plaintext highlighter-rouge">failureThreshold</code>)까지 실패를 허용한다. 즉 최대 <strong>30 × 10 = 300초</strong>의 기동 시간을 벌어준다. 그 안에 <code class="language-plaintext highlighter-rouge">/healthz</code>가 한 번이라도 응답하면 그 즉시 liveness·readiness가 가동을 시작한다.</li> <li><strong>livenessProbe</strong> — 이후 5초마다 <code class="language-plaintext highlighter-rouge">/healthz</code>를 확인하고, 실패가 누적되면 컨테이너를 재시작한다. <code class="language-plaintext highlighter-rouge">kubectl get pods</code>의 <code class="language-plaintext highlighter-rouge">RESTARTS</code>가 올라가는 흔한 원인 중 하나다.</li> <li><strong>readinessProbe</strong> — 5초마다 <code class="language-plaintext highlighter-rouge">/ready</code>를 확인한다. liveness와 <strong>다른 경로</strong>를 보는 것에 주목하자. “살아 있는가”와 “서비스 가능한가”는 다른 질문이므로 엔드포인트도 나누는 것이 보통이다.</li> </ul> <p>세 probe를 한 표로 비교하면 다음과 같다.</p> <table> <thead> <tr> <th> </th> <th>livenessProbe</th> <th>readinessProbe</th> <th>startupProbe</th> </tr> </thead> <tbody> <tr> <td>질문</td> <td>살아 있나?</td> <td>트래픽 받을 준비 됐나?</td> <td>시작이 끝났나?</td> </tr> <tr> <td>실패하면</td> <td>컨테이너 kill 후 재시작</td> <td>Service 엔드포인트에서 제외</td> <td>한도 초과 시 kill 후 재시작</td> </tr> <tr> <td>재시작 유발</td> <td>O</td> <td>X</td> <td>O</td> </tr> <tr> <td>실행 시점</td> <td>startup 성공 후 계속 주기적으로</td> <td>startup 성공 후 계속 주기적으로</td> <td>시작할 때만</td> </tr> <tr> <td>주 용도</td> <td>데드락 복구</td> <td>트래픽 제어</td> <td>느린 기동 보호</td> </tr> </tbody> </table> <hr/> <h1 id="8-리소스-requests와-limits">8. 리소스 requests와 limits</h1> <p>4절의 필터링 표에 “요청한 CPU/메모리가 충분한가”라고 썼다. 그 “요청”의 정체가 바로 컨테이너 spec의 <code class="language-plaintext highlighter-rouge">resources.requests</code>다. 공식 문서의 예제 수치를 그대로 옮기면 이렇다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">containers</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">app</span>
    <span class="na">image</span><span class="pi">:</span> <span class="s">my-app:1.0</span>
    <span class="na">resources</span><span class="pi">:</span>
      <span class="na">requests</span><span class="pi">:</span>
        <span class="na">memory</span><span class="pi">:</span> <span class="s2">"</span><span class="s">64Mi"</span>
        <span class="na">cpu</span><span class="pi">:</span> <span class="s2">"</span><span class="s">250m"</span>
      <span class="na">limits</span><span class="pi">:</span>
        <span class="na">memory</span><span class="pi">:</span> <span class="s2">"</span><span class="s">128Mi"</span>
        <span class="na">cpu</span><span class="pi">:</span> <span class="s2">"</span><span class="s">500m"</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">cpu: "250m"</code> — CPU는 밀리코어(m) 단위다. 1000m이 코어 1개이므로 250m은 0.25코어.</li> <li><code class="language-plaintext highlighter-rouge">memory: "64Mi"</code> — 메모리는 바이트 단위이고, 보통 Mi·Gi 같은 이진 접두어로 적는다.</li> </ul> <p>requests와 limits의 역할은 명확히 다르다.</p> <table> <thead> <tr> <th> </th> <th>requests</th> <th>limits</th> </tr> </thead> <tbody> <tr> <td>의미</td> <td>컨테이너에 보장해야 할 최소량</td> <td>컨테이너가 쓸 수 있는 최대량</td> </tr> <tr> <td>누가 사용하나</td> <td><strong>kube-scheduler</strong> — 노드 선택의 기준</td> <td><strong>kubelet·커널</strong> — 초과 시 제재</td> </tr> <tr> <td>그 이상 쓰면</td> <td>노드에 여유가 있으면 더 쓸 수 있음</td> <td>CPU는 스로틀링, 메모리는 OOM kill</td> </tr> </tbody> </table> <p><strong>requests는 스케줄링의 기준이다.</strong> 4단계에서 스케줄러는 노드의 <strong>실제 사용량이 아니라, 그 노드에 이미 배정된 Pod들의 requests 합계</strong>를 보고 자리가 있는지 판단한다. 그래서 노드가 실제로는 한가해도 requests 장부가 꽉 차 있으면 새 Pod는 못 들어가고, 반대로 requests를 만족하는 노드가 하나도 없으면 필터링에서 전부 탈락해 Pod는 <code class="language-plaintext highlighter-rouge">Pending</code>에 머문다 — 다음 절에서 볼 <code class="language-plaintext highlighter-rouge">FailedScheduling</code>의 단골 원인이다.</p> <p><strong>limits를 초과했을 때의 운명은 CPU와 메모리가 다르다.</strong></p> <table> <thead> <tr> <th>자원</th> <th>limit 초과 시</th> <th>결과</th> </tr> </thead> <tbody> <tr> <td>CPU</td> <td>커널이 <strong>스로틀링</strong>으로 사용량을 깎음</td> <td>죽지 않는다. 대신 느려진다</td> </tr> <tr> <td>메모리</td> <td>커널이 <strong>OOM kill</strong>로 컨테이너를 종료</td> <td>컨테이너가 죽는다 (<code class="language-plaintext highlighter-rouge">OOMKilled</code>)</td> </tr> </tbody> </table> <p>CPU는 시간을 쪼개 나눠 쓰는 자원이라 초과분을 깎아서 줄 수 있지만, 이미 내준 메모리는 뺏을 방법이 없어서 프로세스를 죽이는 것 외에는 도리가 없기 때문이다.</p> <p>비유하면 requests는 <strong>계약 전력</strong>, limits는 <strong>차단기 용량</strong>이다. 전력 회사(스케줄러)는 실제 사용량이 아니라 계약 전력의 합계를 보고 이 건물(노드)에 새 계약을 받을지 정한다. CPU limit 초과는 전력 제한이 걸려 기기가 느려지는 것이고, 메모리 limit 초과는 차단기가 내려가 통째로 꺼지는 것이다.</p> <p>마지막으로, requests·limits를 어떻게 적었는지에 따라 Kubernetes는 Pod에 <strong>QoS(Quality of Service) 클래스</strong>를 자동으로 부여한다. 노드 자원이 부족해질 때 <strong>누구를 먼저 쫓아낼지(eviction)</strong>를 정하는 우선순위 기준이다.</p> <table> <thead> <tr> <th>QoS 클래스</th> <th>조건</th> <th>자원 부족 시</th> </tr> </thead> <tbody> <tr> <td><strong>Guaranteed</strong></td> <td>모든 컨테이너의 CPU·메모리에 requests와 limits가 있고, 둘의 값이 같음</td> <td>가장 나중에 축출</td> </tr> <tr> <td><strong>Burstable</strong></td> <td>Guaranteed는 아니지만, 최소 한 컨테이너에 CPU 또는 메모리 requests/limits가 있음</td> <td>중간</td> </tr> <tr> <td><strong>BestEffort</strong></td> <td>어느 컨테이너에도 CPU·메모리 requests/limits가 전혀 없음</td> <td>가장 먼저 축출</td> </tr> </tbody> </table> <p>축출당하면 곤란한 핵심 서비스의 requests와 limits를 같은 값으로 맞춰 Guaranteed로 만드는 이유가 이것이다.</p> <hr/> <h1 id="9-흔한-에러와-디버깅-루틴">9. 흔한 에러와 디버깅 루틴</h1> <p>Pod를 띄우다 보면 반드시 만나게 될 상태 세 가지를 미리 맛보고, 원인을 찾아 들어가는 순서를 루틴으로 정리하자. 원인 진단의 시작은 언제나 <code class="language-plaintext highlighter-rouge">kubectl describe pod &lt;이름&gt;</code>의 Events다.</p> <table> <thead> <tr> <th>증상</th> <th>대표 원인</th> <th>확인 포인트</th> </tr> </thead> <tbody> <tr> <td><strong>ImagePullBackOff</strong></td> <td>이미지 이름·태그 오타, 존재하지 않는 이미지, 프라이빗 레지스트리 인증 실패 — kubelet이 이미지 풀에 실패해 재시도 간격을 늘려가며(back-off) 대기 중</td> <td>Events의 이미지 풀 실패 메시지, 이미지 이름 철자</td> </tr> <tr> <td><strong>Pending에서 안 넘어감</strong></td> <td>스케줄 불가 — 요청한 CPU/메모리를 만족하는 노드가 없거나, 조건에 맞는 노드가 없음</td> <td>Events의 <code class="language-plaintext highlighter-rouge">FailedScheduling</code> 경고와 그 사유</td> </tr> <tr> <td><strong>CrashLoopBackOff</strong></td> <td>컨테이너가 뜨자마자 반복해서 죽는 중 — 앱 크래시, 잘못된 실행 명령, 성급한 livenessProbe 등. kubelet이 재시작 간격을 늘려가며(back-off) 다시 띄우는 상태</td> <td>직전 컨테이너의 로그 — <code class="language-plaintext highlighter-rouge">kubectl logs -p</code></td> </tr> </tbody> </table> <ul> <li><strong>ImagePullBackOff</strong>는 5단계 중 <strong>5단계(kubelet의 이미지 풀)</strong>에서 막힌 것이다. 컨테이너는 <code class="language-plaintext highlighter-rouge">Waiting</code> 상태로 남는다.</li> <li><strong>Pending 지속</strong>은 <strong>4단계(스케줄러의 노드 선택)</strong>에서 막힌 것이다. 필터링을 통과한 노드가 하나도 없으면 Pod는 배정되지 못하고 Pending에 머문다.</li> <li><strong>CrashLoopBackOff</strong>는 5단계를 모두 통과해 컨테이너가 뜨긴 했는데, 뜨자마자 죽기를 반복하는 상태다. Kubernetes는 할 일을 다 한 것이므로 원인은 대개 컨테이너 안(앱)에 있다.</li> </ul> <p>에러가 어느 단계에서 났는지 알면 원인의 절반은 찾은 셈이다. 5단계 그림을 머릿속에 넣어두자.</p> <h2 id="91-디버깅-루틴--보는-순서는-언제나-같다">9.1 디버깅 루틴 — 보는 순서는 언제나 같다</h2> <p>증상이 무엇이든, 원인을 찾아 들어가는 순서는 같다. 네 단계를 루틴으로 몸에 익혀두자.</p> <table> <thead> <tr> <th>순서</th> <th>명령</th> <th>보는 곳</th> <th>답하는 질문</th> </tr> </thead> <tbody> <tr> <td>1</td> <td><code class="language-plaintext highlighter-rouge">kubectl get pods</code></td> <td>STATUS·READY·RESTARTS</td> <td>무엇이 어떤 상태로 이상한가</td> </tr> <tr> <td>2</td> <td><code class="language-plaintext highlighter-rouge">kubectl describe pod &lt;이름&gt;</code></td> <td>Events, 컨테이너 State와 Reason</td> <td>5단계 중 어디서 막혔나</td> </tr> <tr> <td>3</td> <td><code class="language-plaintext highlighter-rouge">kubectl logs &lt;이름&gt;</code> (직전 컨테이너는 <code class="language-plaintext highlighter-rouge">-p</code>)</td> <td>앱 로그</td> <td>앱이 무슨 말을 남기고 죽었나</td> </tr> <tr> <td>4</td> <td><code class="language-plaintext highlighter-rouge">kubectl exec -it &lt;이름&gt; -- /bin/sh</code></td> <td>컨테이너 내부</td> <td>설정·파일·네트워크가 기대와 같은가</td> </tr> </tbody> </table> <ul> <li><strong>describe의 Events는 Kubernetes 쪽 사정</strong>(스케줄링·이미지 풀·재시작)을, <strong>logs는 앱 쪽 사정</strong>을 말해준다. 둘은 보는 층이 다르다.</li> <li><code class="language-plaintext highlighter-rouge">kubectl logs -p</code>(<code class="language-plaintext highlighter-rouge">--previous</code>)는 <strong>직전에 죽은 컨테이너</strong>의 로그를 보여준다. CrashLoopBackOff처럼 재시작이 반복되는 상황에서는 방금 새로 뜬 컨테이너의 로그가 비어 있기 일쑤라서, 죽기 직전의 마지막 메시지는 <code class="language-plaintext highlighter-rouge">-p</code>로 봐야 한다.</li> <li><code class="language-plaintext highlighter-rouge">kubectl exec -it &lt;이름&gt; -- /bin/sh</code>의 <code class="language-plaintext highlighter-rouge">--</code>는 kubectl의 옵션과 컨테이너 안에서 실행할 명령을 구분한다. 쉘조차 없는 초경량 이미지라면 <code class="language-plaintext highlighter-rouge">kubectl debug</code>로 디버깅용 임시 컨테이너(ephemeral container)를 붙이는 방법도 있다.</li> </ul> <p>이번 글의 에러들을 루틴에 매핑하면 이렇다.</p> <table> <thead> <tr> <th>증상</th> <th>막힌 단계</th> <th>원인이 보이는 곳</th> </tr> </thead> <tbody> <tr> <td>Pending 지속</td> <td>4단계 — 스케줄링</td> <td>2번 describe → Events의 <code class="language-plaintext highlighter-rouge">FailedScheduling</code></td> </tr> <tr> <td>ImagePullBackOff</td> <td>5단계 — 이미지 풀</td> <td>2번 describe → Events의 풀 실패 메시지</td> </tr> <tr> <td>CrashLoopBackOff</td> <td>5단계 이후 — 앱 실행</td> <td>3번 <code class="language-plaintext highlighter-rouge">logs -p</code> → 죽기 직전 앱 로그</td> </tr> </tbody> </table> <p>컨테이너가 아예 못 뜬 상태(Pending, ImagePullBackOff)에서는 읽을 로그 자체가 없다 — describe가 답이다. 반대로 컨테이너가 떴다가 죽는 상태(CrashLoopBackOff)는 Events에 “재시작했다”는 사실만 반복될 뿐 이유는 없다 — 앱 로그가 답이다.</p> <hr/> <h1 id="10-마무리">10. 마무리</h1> <p>이번 글의 핵심을 표 하나로 요약한다.</p> <table> <thead> <tr> <th>질문</th> <th>답</th> </tr> </thead> <tbody> <tr> <td>Pod란?</td> <td>Kubernetes의 최소 배포 단위. 하나의 IP와 볼륨을 공유하는 컨테이너 그룹</td> </tr> <tr> <td>왜 컨테이너가 아니라 Pod?</td> <td>사이드카처럼 “반드시 함께 움직여야 하는 컨테이너 묶음”의 경계를 표현하기 위해</td> </tr> <tr> <td>Pod는 어떻게 뜨나?</td> <td>YAML 작성 → kubectl 전송 → api-server가 etcd에 저장 → scheduler가 노드 선택 → kubelet이 실행</td> </tr> <tr> <td>상태는 어떻게 확인하나?</td> <td><code class="language-plaintext highlighter-rouge">get pods -o wide</code> → <code class="language-plaintext highlighter-rouge">describe pod</code>(Events) → <code class="language-plaintext highlighter-rouge">get events --sort-by=...</code></td> </tr> <tr> <td>phase는?</td> <td>Pending / Running / Succeeded / Failed / Unknown 다섯 가지</td> </tr> <tr> <td>Running이면 끝인가?</td> <td>아니다. liveness(재시작)·readiness(트래픽 제외)·startup(기동 보호) 3종 probe가 건강을 판정한다</td> </tr> <tr> <td>requests vs limits?</td> <td>requests는 스케줄러의 배치 기준, limits는 상한 — 초과 시 CPU는 스로틀링, 메모리는 OOMKilled</td> </tr> <tr> <td>Pod가 이상하면?</td> <td><code class="language-plaintext highlighter-rouge">get pods</code> → <code class="language-plaintext highlighter-rouge">describe</code>(Events) → <code class="language-plaintext highlighter-rouge">logs -p</code> → <code class="language-plaintext highlighter-rouge">exec</code> 순서로 본다</td> </tr> </tbody> </table> <p>직접 만든 Pod는 지우면 끝이고, 죽어도 되살아나지 않는다는 것도 확인했다. 실제 서비스에서 Pod를 날것으로 쓰지 않는 이유다.</p> <blockquote> <p>이전 글 <a href="/blog/2026/k8s-03-architecture/">03: Kubernetes 아키텍처 — Control Plane과 Node</a>에서 소개한 컴포넌트들이 이번 글의 5단계에서 어떻게 협업하는지 확인했다. 다음 글 <a href="/blog/2026/k8s-05-workloads/">05: 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</a>에서는 Pod가 죽어도 자동으로 되살리고, 스케일링과 롤링 업데이트까지 해주는 워크로드 리소스를 다룬다.</p> </blockquote> <hr/> <h1 id="참고-문헌">참고 문헌</h1> <ul> <li><a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua, SK Devocean, 2024) — 시리즈 로드맵 출처</li> <li><a href="https://kubernetes.io/docs/concepts/workloads/pods/">Pods</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/">Pod Lifecycle</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/concepts/workloads/pods/sidecar-containers/">Sidecar Containers</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/concepts/configuration/liveness-readiness-startup-probes/">Liveness, Readiness, and Startup Probes</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/">Configure Liveness, Readiness and Startup Probes</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/">Resource Management for Pods and Containers</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/concepts/workloads/pods/pod-qos/">Pod Quality of Service Classes</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/concepts/scheduling-eviction/kube-scheduler/">Kubernetes Scheduler</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/concepts/architecture/">Cluster Architecture</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/reference/kubectl/quick-reference/">kubectl Quick Reference</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/tasks/access-application-cluster/port-forward-access-application-cluster/">Use Port Forwarding to Access Applications in a Cluster</a> — Kubernetes 공식 문서</li> <li><a href="https://kubernetes.io/docs/tasks/debug/debug-application/debug-running-pod/">Debug Running Pods</a> — Kubernetes 공식 문서</li> </ul>]]></content><author><name></name></author><category term="infra"/><category term="kubernetes"/><category term="infra"/><category term="pod"/><category term="scheduling"/><summary type="html"><![CDATA[Kubernetes의 최소 배포 단위 Pod — 사이드카 패턴, nginx 실습, Pod가 뜨는 5단계, 생명주기 phase까지]]></summary></entry><entry><title type="html">Kubernetes 아키텍처 — Control Plane과 Node</title><link href="https://www.wonbeomjang.kr/blog/2026/k8s-03-architecture/" rel="alternate" type="text/html" title="Kubernetes 아키텍처 — Control Plane과 Node"/><published>2026-07-06T18:00:00+00:00</published><updated>2026-07-06T18:00:00+00:00</updated><id>https://www.wonbeomjang.kr/blog/2026/k8s-03-architecture</id><content type="html" xml:base="https://www.wonbeomjang.kr/blog/2026/k8s-03-architecture/"><![CDATA[<blockquote> <p>이 글은 <strong>K8s 입문 시리즈</strong>의 세 번째 글이다.</p> <ul> <li><a href="/blog/2026/k8s-01-history/">01: Kubernetes의 탄생 — Google Borg에서 CNCF까지</a></li> <li><a href="/blog/2026/k8s-02-local-setup/">02: 내 노트북에 클러스터 만들기 — kind와 kubectl</a></li> <li><strong>03: Kubernetes 아키텍처 — Control Plane과 Node</strong> ← 현재 글</li> <li><a href="/blog/2026/k8s-04-pod/">04: Pod의 모든 것 — 생성부터 스케줄링까지</a></li> <li><a href="/blog/2026/k8s-05-workloads/">05: 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</a></li> <li><a href="/blog/2026/k8s-06-networking/">06: 네트워킹 — Service와 Ingress</a></li> <li><a href="/blog/2026/k8s-07-storage-config/">07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</a></li> <li><a href="/blog/2026/k8s-08-rbac/">08: 권한 관리 — ServiceAccount와 RBAC</a></li> <li><a href="/blog/2026/k8s-09-operator-cncf/">09: 확장과 생태계 — Operator와 CNCF Projects</a></li> </ul> <p>이 시리즈의 커리큘럼은 SK Devocean의 <a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua) 글의 학습 로드맵을 바탕으로 구성했다.</p> </blockquote> <p><a href="/blog/2026/k8s-02-local-setup/">지난 글</a>에서 kind로 클러스터를 만들고 kubectl로 접속까지 해봤다. <code class="language-plaintext highlighter-rouge">kind create cluster</code> 한 줄로 만들어진 그 클러스터 안에는 사실 여러 개의 프로그램이 각자 다른 역할을 맡아 돌아가고 있다. 이번 글에서는 그 내부를 해부한다.</p> <p>Kubernetes 클러스터는 회사와 비슷하다. <strong>Control Plane은 본사 경영진</strong>이다. 회사의 목표를 정하고(“웹 서버 3대를 항상 유지한다”), 장부를 관리하고, 어느 지점에 어떤 일을 맡길지 결정한다. <strong>Node는 현장</strong>이다. 본사의 지시를 받아 실제로 일(컨테이너)을 수행한다. 중요한 건 경영진이 현장에 직접 내려가서 일하지 않는다는 점이다. 지시는 문서(API)로 전달되고, 현장 반장(kubelet)이 그걸 읽고 실행한다.</p> <p>이 글에서는 각 컴포넌트가 “무엇을 하는지”와 함께 “왜 그렇게 설계됐는지”를 본다. 여기서 잡은 그림이 다음 글(Pod가 뜨는 과정)의 뼈대가 된다.</p> <hr/> <h1 id="1-전체-아키텍처-한눈에-보기">1. 전체 아키텍처 한눈에 보기</h1> <p>먼저 전체 그림부터 본다. 공식 문서 기준으로 클러스터는 크게 <strong>Control Plane 컴포넌트</strong>와 <strong>Node 컴포넌트</strong>로 나뉜다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-03-architecture/cluster-architecture-480.webp 480w,/assets/post/image/k8s-03-architecture/cluster-architecture-800.webp 800w,/assets/post/image/k8s-03-architecture/cluster-architecture-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-03-architecture/cluster-architecture.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="Kubernetes 클러스터 아키텍처 — Control Plane(kube-apiserver, etcd, scheduler, controller-manager)과 Node(kubelet, kube-proxy, 컨테이너 런타임, Pod), 모든 통신이 kube-apiserver로 모인다" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>이 그림에서 핵심 관찰은 세 가지다.</p> <ul> <li><strong>모든 화살표가 kube-apiserver로 모인다.</strong> 사용자도, 스케줄러도, 컨트롤러도, 노드의 kubelet도 서로 직접 대화하지 않고 전부 API 서버를 거친다. 컴포넌트끼리 몰래 주고받는 사이드 채널이 없다.</li> <li><strong>etcd는 kube-apiserver 뒤에 숨어 있다.</strong> 클러스터의 모든 상태가 저장되는 곳이지만, 여기에 접근하는 컴포넌트는 API 서버뿐이다.</li> <li><strong>kubelet은 컨테이너를 직접 만들지 않는다.</strong> CRI(Container Runtime Interface)라는 표준 인터페이스를 통해 컨테이너 런타임에 실행을 위임한다.</li> </ul> <p>이 구조 덕분에 각 컴포넌트는 “API 서버에 저장된 상태를 읽고, 자기 몫의 일을 하고, 결과를 다시 API 서버에 기록”하는 단순한 패턴으로 동작한다. 컴포넌트 하나가 죽어도 나머지는 API 서버만 살아 있으면 계속 일할 수 있다. 이제 하나씩 뜯어보자.</p> <hr/> <h1 id="2-control-plane--클러스터의-두뇌">2. Control Plane — 클러스터의 두뇌</h1> <p>Control Plane은 클러스터 전체에 대한 결정을 내리는 곳이다. 새 Pod를 어느 노드에 배치할지, 복제본 수가 모자라면 언제 채울지 같은 판단이 전부 여기서 일어난다.</p> <h2 id="21-etcd--회사의-장부">2.1 etcd — 회사의 장부</h2> <p><strong>etcd는 클러스터의 모든 데이터가 저장되는 분산 key-value 저장소다.</strong> 공식 문서는 etcd를 “일관성 있고 고가용성인 key-value 저장소로, 모든 클러스터 데이터의 백업 저장소(backing store)”라고 정의한다. 어떤 Pod가 어디에 떠 있는지, Deployment 설정이 뭔지, Secret에 뭐가 들었는지 — 클러스터의 “진실”은 전부 etcd에 있다.</p> <p>회사 비유로는 <strong>등기부이자 장부</strong>다. 장부가 사라지면 회사가 뭘 가졌고 뭘 하기로 했는지 아무도 모른다. 그래서 etcd에는 두 가지 설계적 특징이 있다.</p> <p><strong>첫째, Raft 합의 알고리즘으로 여러 대가 하나처럼 동작한다.</strong> etcd는 리더 기반(leader-based)으로, 합의가 필요한 요청은 리더가 처리하고 팔로워는 요청을 리더에게 전달한다. 쓰기가 커밋되려면 전체 멤버의 과반, 즉 (n/2)+1개가 동의해야 한다. 그래서 프로덕션에서는 3대나 5대처럼 <strong>홀수</strong>로 구성한다. 3대 클러스터는 과반이 2이므로 1대가 죽어도 버틴다. 4대로 늘려도 과반이 3이 되어 허용 장애 수는 똑같이 1대다. 즉 짝수는 서버만 더 쓰고 얻는 게 없다.</p> <p><strong>둘째, 접근이 극도로 제한된다.</strong> 공식 문서는 “etcd에 대한 접근은 클러스터의 root 권한과 동등하므로, 이상적으로는 API 서버만 접근해야 한다”고 명시한다. etcd를 직접 읽고 쓸 수 있으면 인증도 권한 검사도 다 우회할 수 있기 때문이다. 그래서 그림에서 etcd로 향하는 화살표는 kube-apiserver 하나뿐이다.</p> <h2 id="22-kube-apiserver--모든-통신의-관문">2.2 kube-apiserver — 모든 통신의 관문</h2> <p><strong>kube-apiserver는 Kubernetes API를 노출하는 Control Plane의 프론트엔드다.</strong> kubectl 명령도, 스케줄러의 결정도, kubelet의 상태 보고도 전부 이 문을 지난다.</p> <p>비유하자면 <strong>회사의 민원 창구</strong>다. 아무나 장부(etcd)에 손대게 두지 않고, 창구에서 신원 확인과 권한 확인을 거친 요청만 장부에 반영한다. 요청 하나가 처리되는 과정은 파이프라인으로 정리된다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-03-architecture/apiserver-request-pipeline-480.webp 480w,/assets/post/image/k8s-03-architecture/apiserver-request-pipeline-800.webp 800w,/assets/post/image/k8s-03-architecture/apiserver-request-pipeline-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-03-architecture/apiserver-request-pipeline.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="kube-apiserver 요청 처리 파이프라인 — 인증, 인가, 어드미션 컨트롤, 오브젝트 검증을 거쳐 etcd에 저장된다" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li><strong>인증(Authentication)</strong>: 요청을 보낸 게 누구인지 확인한다. 클라이언트 인증서, 토큰 등 여러 모듈이 순서대로 시도되고, 전부 실패하면 401을 돌려준다.</li> <li><strong>인가(Authorization)</strong>: 그 사용자가 그 동작(예: default 네임스페이스에서 Pod 생성)을 할 권한이 있는지 확인한다. RBAC이 대표적인 인가 방식이고, 거부되면 403이다. 자세한 건 <a href="/blog/2026/k8s-08-rbac/">08편</a>에서 다룬다.</li> <li><strong>어드미션 컨트롤(Admission Control)</strong>: 통과한 요청을 <strong>수정하거나 거부</strong>할 수 있는 마지막 관문이다. 기본값을 채워 넣거나(예: 리소스 기본 할당), 정책 위반 요청을 막는다. 읽기 요청에는 적용되지 않고, 생성·수정·삭제 같은 변경 요청에만 동작한다.</li> </ul> <p>이 파이프라인을 통과한 오브젝트만 etcd에 저장된다. 여기서 중요한 설계 의도가 나온다. <strong>모든 통신을 한 관문으로 모으면 인증·인가·감사(audit)를 한 곳에서 처리할 수 있다.</strong> 컴포넌트끼리 직접 통신하는 구조였다면 보안 검사를 컴포넌트 쌍마다 구현해야 했을 것이다. 또한 API 서버 자체는 상태를 etcd에 두기 때문에(stateless), 공식 문서 표현대로 여러 인스턴스를 띄워 수평 확장할 수 있다.</p> <h2 id="23-kube-controller-manager--온도조절기들의-집합">2.3 kube-controller-manager — 온도조절기들의 집합</h2> <p><strong>kube-controller-manager는 Kubernetes API의 동작을 구현하는 컨트롤러들을 실행하는 컴포넌트다.</strong> 컨트롤러(controller)는 Kubernetes 설계의 심장에 해당하는 개념이라 조금 자세히 본다.</p> <p>공식 문서가 드는 비유는 <strong>온도조절기(thermostat)</strong>다. 온도를 25도로 설정하는 건 “바라는 상태(desired state)”를 알려주는 것이고, 실제 방 온도는 “현재 상태(current state)”다. 온도조절기는 이 둘을 계속 비교하면서 장비를 켜고 꺼서 현재 상태를 바라는 상태에 가깝게 만든다.</p> <p>Kubernetes의 컨트롤러도 똑같다. <strong>끝나지 않는 루프(control loop)를 돌면서 클러스터 상태를 관찰하고, 바라는 상태와 다르면 API 서버를 통해 변경을 요청한다.</strong> 이 과정을 reconcile(조정)이라 부른다.</p> <ul> <li>바라는 상태: 리소스의 <code class="language-plaintext highlighter-rouge">spec</code> 필드에 적힌 내용 (예: “replicas: 3”)</li> <li>현재 상태: 실제 클러스터의 모습 (예: “지금 Pod가 2개뿐”)</li> <li>컨트롤러의 일: 둘의 차이를 발견하고 메꾼다 (예: Pod 1개 생성 요청)</li> </ul> <p>여기서 눈여겨볼 설계 의도가 두 가지다. 첫째, <strong>컨트롤러는 직접 일하지 않고 API 서버에 요청만 남긴다.</strong> 예를 들어 Job 컨트롤러는 Pod를 직접 실행하는 게 아니라 “Pod를 만들어 달라”고 API 서버에 기록하고, 실제 실행은 노드의 kubelet이 담당한다. 둘째, <strong>하나의 거대한 루프 대신 작은 컨트롤러 여러 개를 쓴다.</strong> Node 컨트롤러, Job 컨트롤러, EndpointSlice 컨트롤러, ServiceAccount 컨트롤러처럼 각자 한 가지 상태만 책임진다. 하나가 오작동해도 나머지는 계속 돌고, 각 컨트롤러의 로직도 단순해진다. kube-controller-manager는 이 컨트롤러들을 하나의 프로세스로 묶어 실행하는 바이너리다.</p> <p>이 “선언하면 컨트롤러가 알아서 맞춘다”는 패턴은 <a href="/blog/2026/k8s-05-workloads/">05편의 Deployment</a>와 <a href="/blog/2026/k8s-09-operator-cncf/">09편의 Operator</a>에서 계속 다시 만난다.</p> <h2 id="24-kube-scheduler--배치-담당자">2.4 kube-scheduler — 배치 담당자</h2> <p><strong>kube-scheduler는 아직 노드가 배정되지 않은 Pod를 찾아서 적절한 노드에 할당하는 컴포넌트다.</strong> 회사 비유로는 <strong>인사팀의 배치 담당자</strong>다. 새 업무(Pod)가 들어오면 어느 지점(Node)에 맡길지 결정하되, 결정만 하고 실행은 현장에 맡긴다.</p> <p>공식 문서 기준으로 스케줄러는 노드 선택을 <strong>2단계</strong>로 수행한다.</p> <table> <thead> <tr> <th>단계</th> <th>하는 일</th> <th>예시</th> </tr> </thead> <tbody> <tr> <td><strong>1. 필터링(Filtering)</strong></td> <td>Pod를 배치할 수 <strong>있는</strong> 노드(feasible node)만 남기고 나머지를 걸러낸다</td> <td>요청한 CPU/메모리가 부족한 노드 제외 (<code class="language-plaintext highlighter-rouge">PodFitsResources</code> 필터)</td> </tr> <tr> <td><strong>2. 스코어링(Scoring)</strong></td> <td>살아남은 노드들에 점수를 매겨 순위를 정한다</td> <td>활성화된 스코어링 규칙에 따라 채점 후 <strong>최고점 노드</strong>를 선택, 동점이면 무작위 선택</td> </tr> </tbody> </table> <p>필터링 결과가 빈 리스트면, 즉 배치 가능한 노드가 하나도 없으면 그 Pod는 스케줄되지 못한 채 남는다. 나중에 자원이 생기거나 조건이 바뀌면 그때 배치된다. <code class="language-plaintext highlighter-rouge">kubectl get pods</code>에서 가끔 보게 되는 <code class="language-plaintext highlighter-rouge">Pending</code> 상태의 대표적인 원인이 바로 이것이고, <a href="/blog/2026/k8s-04-pod/">04편</a>에서 직접 확인해 본다.</p> <p>왜 “필터링 후 스코어링”일까. 조건을 만족하는지(하드 제약)와 얼마나 좋은 자리인지(소프트 선호)를 분리하면, 자격 미달 노드를 빠르게 쳐낸 뒤 남은 후보만 정밀하게 비교할 수 있다. 결정이 끝나면 스케줄러는 “이 Pod는 이 노드로”라는 바인딩 결과를 API 서버에 알릴 뿐, 노드에 직접 명령하지 않는다.</p> <hr/> <h1 id="3-node--일이-실제로-벌어지는-곳">3. Node — 일이 실제로 벌어지는 곳</h1> <p>Node 컴포넌트는 모든 노드에서 실행되며, Pod를 돌리고 런타임 환경을 제공한다. 본사가 아무리 계획을 잘 세워도 실제로 컨테이너를 띄우는 건 현장이다.</p> <h2 id="31-kubelet--현장-반장">3.1 kubelet — 현장 반장</h2> <p><strong>kubelet은 각 노드에서 실행되는 에이전트로, Pod 안의 컨테이너들이 확실히 실행되도록 책임진다.</strong> 공식 문서 표현을 빌리면, kubelet은 전달받은 PodSpec에 기술된 컨테이너들이 실행 중이고 건강한지(healthy) 확인한다. 반대로 Kubernetes가 만들지 않은 컨테이너는 관리하지 않는다.</p> <p>비유하자면 <strong>현장 반장</strong>이다. 본사(API 서버)에서 “이 노드에 이 Pod를 실행하라”는 지시가 내려오면, 반장이 그걸 읽고 작업자(컨테이너 런타임)에게 일을 시키고, 진행 상황을 본사에 보고한다. 스케줄러가 자리만 정해 주면, 그 자리에서 실제로 컨테이너를 띄우고 살아 있는지 감시하는 건 전부 kubelet 몫이다.</p> <h2 id="32-컨테이너-런타임과-cri--실행의-위임">3.2 컨테이너 런타임과 CRI — 실행의 위임</h2> <p><strong>컨테이너 런타임은 컨테이너 실행을 실제로 담당하는 소프트웨어다.</strong> 여기서 Kubernetes 설계의 또 다른 특징이 나온다. kubelet은 컨테이너를 직접 만들지 않고, <strong>CRI(Container Runtime Interface)</strong>라는 gRPC 기반 표준 프로토콜로 런타임에 위임한다.</p> <p>왜 이렇게 나눴을까. CRI라는 인터페이스만 지키면 어떤 런타임이든 갈아 끼울 수 있게 하기 위해서다. 공식 문서 기준으로 Kubernetes는 containerd, CRI-O 같은 CRI 구현체를 지원한다. kubelet 입장에서는 “컨테이너 하나 띄워줘”라는 gRPC 호출만 하면 되고, 그 아래에서 이미지를 받고 프로세스를 격리하는 세부 구현은 런타임의 책임이다. 표준 인터페이스 하나로 구현체를 교체 가능하게 만드는 이 패턴은 잠시 뒤 CNI에서도 똑같이 반복된다.</p> <h2 id="33-kube-proxy--service를-실현하는-교통-정리">3.3 kube-proxy — Service를 실현하는 교통 정리</h2> <p><strong>kube-proxy는 각 노드에서 네트워크 규칙을 유지하면서 Service 개념을 실제로 구현하는 네트워크 프록시다.</strong> 아직 Service를 안 배웠으니 여기서는 큰 그림만 잡는다. Kubernetes에서는 여러 Pod를 하나의 가상 IP(Service)로 묶어서 접근하는데, “이 가상 IP로 온 트래픽을 실제 Pod들에게 나눠주는 규칙”을 노드마다 세팅하는 게 kube-proxy다.</p> <p>이름과 달리 대부분의 모드에서 트래픽을 직접 중계하지 않는다. 대신 커널의 패킷 처리 규칙을 설정해 두고 커널이 트래픽을 전달하게 한다. 공식 문서 기준으로 Linux에서는 <strong>iptables 모드(기본값)</strong>, ipvs 모드, nftables 모드를 지원한다. iptables 모드는 Service의 가상 IP로 향하는 패킷을 백엔드 Pod로 DNAT하는 규칙을 커널 netfilter에 깔아두는 방식인데, 클러스터가 아주 커지면 규칙 수가 많아져 성능 이슈가 생길 수 있고, 그 대안으로 nftables 모드가 등장했다(ipvs 모드는 공식 문서 기준 deprecated 상태다). 흥미로운 점 하나: 공식 문서는 kube-proxy를 <strong>optional</strong>로 분류한다. Service 트래픽 처리를 직접 구현하는 네트워크 플러그인을 쓰면 kube-proxy 없이도 클러스터가 동작한다. 자세한 동작은 <a href="/blog/2026/k8s-06-networking/">06편</a>에서 다룬다.</p> <h2 id="34-네트워크-플러그인cni--pod들의-도로망">3.4 네트워크 플러그인(CNI) — Pod들의 도로망</h2> <p>마지막 조각은 <strong>네트워크 플러그인</strong>이다. Pod마다 IP를 할당하고, 서로 다른 노드에 있는 Pod끼리 통신할 수 있게 만드는 역할을 한다. 회사 비유로는 지점과 지점 사이를 잇는 <strong>도로망 공사</strong>다. kube-proxy가 “어디로 보낼지” 교통 규칙을 정한다면, CNI 플러그인은 그 전에 길 자체를 깔아준다.</p> <p>Kubernetes는 여기서도 구현을 직접 들고 있지 않고 CNI(Container Network Interface)라는 표준에 위임한다. 그래서 Calico, Cilium 같은 다양한 구현체가 존재한다. 우리가 쓰는 <strong>kind는 kindnetd라는 자체 기본 CNI를 탑재</strong>하고 있다. kind 공식 문서는 이를 “표준 CNI 플러그인 기반의 단순한 네트워킹 구현”이라고 설명하며, IP 마스커레이드 같은 기본 기능을 처리한다. 설정에서 <code class="language-plaintext highlighter-rouge">disableDefaultCNI: true</code>를 주면 kindnetd 대신 Calico 같은 다른 CNI를 직접 설치할 수도 있다(공식 문서상 파워 유저 기능이다).</p> <hr/> <h1 id="4-컴포넌트-요약-표">4. 컴포넌트 요약 표</h1> <p>지금까지 나온 컴포넌트를 한 표로 정리한다.</p> <table> <thead> <tr> <th>컴포넌트</th> <th>위치</th> <th>역할</th> <th>비유</th> </tr> </thead> <tbody> <tr> <td><strong>etcd</strong></td> <td>Control Plane</td> <td>클러스터 모든 상태의 유일한 저장소, Raft 합의로 고가용성</td> <td>회사 장부·등기부</td> </tr> <tr> <td><strong>kube-apiserver</strong></td> <td>Control Plane</td> <td>모든 통신의 관문, 인증→인가→어드미션, 유일하게 etcd에 접근</td> <td>민원 창구</td> </tr> <tr> <td><strong>kube-controller-manager</strong></td> <td>Control Plane</td> <td>desired state와 current state의 차이를 reconcile하는 컨트롤러 묶음</td> <td>온도조절기</td> </tr> <tr> <td><strong>kube-scheduler</strong></td> <td>Control Plane</td> <td>미배정 Pod에 노드를 골라줌 (필터링→스코어링)</td> <td>인사 배치 담당자</td> </tr> <tr> <td><strong>kubelet</strong></td> <td>Node</td> <td>지시받은 Pod의 컨테이너를 실행·감시하는 노드 에이전트</td> <td>현장 반장</td> </tr> <tr> <td><strong>컨테이너 런타임</strong></td> <td>Node</td> <td>CRI를 통해 위임받아 컨테이너를 실제로 실행</td> <td>현장 작업자</td> </tr> <tr> <td><strong>kube-proxy</strong></td> <td>Node</td> <td>Service 트래픽이 Pod에 도달하도록 커널에 규칙 설정</td> <td>교통 정리 요원</td> </tr> <tr> <td><strong>네트워크 플러그인(CNI)</strong></td> <td>Node</td> <td>Pod IP 할당과 Pod 간 통신 경로 구성 (kind는 kindnetd)</td> <td>도로망 공사</td> </tr> </tbody> </table> <hr/> <h1 id="5-실습--kind-클러스터에서-직접-눈으로-확인하기">5. 실습 — kind 클러스터에서 직접 눈으로 확인하기</h1> <p>이론은 충분하다. 02편에서 만든 kind 클러스터에서 이 컴포넌트들을 직접 찾아보자. Kubernetes는 시스템 컴포넌트를 <code class="language-plaintext highlighter-rouge">kube-system</code>이라는 네임스페이스에 모아둔다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get pods <span class="nt">-n</span> kube-system
</code></pre></div></div> <p>기본 단일 노드 kind 클러스터라면 대략 이런 출력을 보게 된다 (이름 뒤의 해시와 AGE는 환경마다 다르다).</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                                         READY   STATUS    RESTARTS   AGE
coredns-xxxxxxxxxx-xxxxx                     1/1     Running   0          10m
coredns-xxxxxxxxxx-xxxxx                     1/1     Running   0          10m
etcd-kind-control-plane                      1/1     Running   0          10m
kindnet-xxxxx                                1/1     Running   0          10m
kube-apiserver-kind-control-plane            1/1     Running   0          10m
kube-controller-manager-kind-control-plane   1/1     Running   0          10m
kube-proxy-xxxxx                             1/1     Running   0          10m
kube-scheduler-kind-control-plane            1/1     Running   0          10m
</code></pre></div></div> <p>이 출력에는 오늘 배운 내용이 전부 들어 있다. 한 줄씩 해석해 보자.</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">etcd-</code>, <code class="language-plaintext highlighter-rouge">kube-apiserver-</code>, <code class="language-plaintext highlighter-rouge">kube-controller-manager-</code>, <code class="language-plaintext highlighter-rouge">kube-scheduler-</code></strong>: 2장에서 본 Control Plane 4인방이 그대로 보인다. 이름 끝에 노드 이름(<code class="language-plaintext highlighter-rouge">-kind-control-plane</code>)이 붙어 있는 게 특징인데, 이들이 <strong>static Pod</strong>이기 때문이다. static Pod는 API 서버를 거치지 않고 <strong>kubelet이 지정된 디렉터리의 매니페스트 파일을 읽어 직접 관리</strong>하는 Pod다. 생각해 보면 당연하다 — API 서버 자신을 띄우는 일을 API 서버에게 시킬 수는 없다. kind는 kubeadm으로 클러스터를 구성하는데, kubeadm은 Control Plane 컴포넌트의 매니페스트를 노드의 <code class="language-plaintext highlighter-rouge">/etc/kubernetes/manifests</code>에 써 두고 kubelet이 이를 읽어 실행하게 한다. 지금 kubectl로 보이는 건 kubelet이 API 서버에 만들어 준 거울상, 즉 <strong>mirror Pod</strong>다.</li> <li><strong><code class="language-plaintext highlighter-rouge">kindnet-xxxxx</code></strong>: 3.4에서 말한 kind의 기본 CNI, kindnetd다.</li> <li><strong><code class="language-plaintext highlighter-rouge">kube-proxy-xxxxx</code></strong>: 3.3의 kube-proxy다. kindnet과 kube-proxy는 노드마다 하나씩 떠야 하는 컴포넌트라서, 멀티 노드 클러스터라면 노드 수만큼 보인다. 이렇게 “모든 노드에 하나씩”을 보장하는 워크로드가 DaemonSet인데, <a href="/blog/2026/k8s-05-workloads/">05편</a>에서 자세히 다룬다.</li> <li><strong><code class="language-plaintext highlighter-rouge">coredns-</code></strong>: 컴포넌트 목록에는 없던 얼굴이다. 공식 문서가 <strong>애드온(addon)</strong>으로 분류하는 클러스터 DNS로, Service 이름을 IP로 풀어주는 역할을 한다. 애드온은 Control Plane의 필수 부품은 아니지만 사실상 모든 클러스터가 쓰는 확장 기능이다.</li> </ul> <p>그런데 이상한 점이 하나 있다. <strong>kubelet이 목록에 없다.</strong> 당연한 일이다. kubelet은 Pod를 띄우는 주체이므로 자기 자신이 Pod일 수 없다. kubelet은 Pod가 아니라 노드 위에서 도는 일반 프로세스다. kind에서는 노드가 Docker 컨테이너이므로, 그 컨테이너 안에서 kubelet 프로세스가 돌고 있는 것이다.</p> <p>노드 정보도 확인해 보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes <span class="nt">-o</span> wide
</code></pre></div></div> <p>출력의 <code class="language-plaintext highlighter-rouge">CONTAINER-RUNTIME</code> 컬럼에서 이 노드의 kubelet이 CRI 너머로 어떤 런타임과 대화하고 있는지 직접 확인할 수 있다. 마지막으로 Control Plane의 접점도 다시 보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl cluster-info
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kubernetes control plane is running at https://127.0.0.1:xxxxx
CoreDNS is running at https://127.0.0.1:xxxxx/api/v1/namespaces/kube-system/services/kube-dns:dns/proxy
</code></pre></div></div> <p>02편에서 무심코 지나쳤던 이 출력이 이제 다르게 읽힐 것이다. 첫 줄의 주소가 바로 kube-apiserver의 엔드포인트고, kubectl이 보내는 모든 명령이 저 문을 통과한다.</p> <hr/> <h1 id="6-마무리">6. 마무리</h1> <p>오늘 배운 것을 한 문장으로 압축하면 이렇다. <strong>Kubernetes는 “바라는 상태”를 etcd에 적어두고, 모든 컴포넌트가 kube-apiserver라는 단일 관문을 통해 그 상태를 읽고 현실을 거기에 맞추는 시스템이다.</strong></p> <ul> <li>상태는 <strong>etcd</strong>에만 있고, 접근은 <strong>kube-apiserver</strong>만 한다.</li> <li><strong>kube-controller-manager</strong>는 바라는 상태와 현실의 차이를 끊임없이 메꾼다(reconcile).</li> <li><strong>kube-scheduler</strong>는 필터링→스코어링으로 Pod의 자리를 정한다.</li> <li>노드에서는 <strong>kubelet</strong>이 CRI로 런타임에 실행을 위임하고, <strong>kube-proxy</strong>와 <strong>CNI</strong>가 네트워크를 책임진다.</li> </ul> <p>이 그림이 머리에 있으면 다음 편이 훨씬 쉬워진다. <code class="language-plaintext highlighter-rouge">kubectl apply -f pod.yaml</code> 한 줄을 실행했을 때, 방금 배운 컴포넌트들이 릴레이 하듯 바통을 넘기며 컨테이너 하나를 띄워내는 과정을 처음부터 끝까지 추적할 것이다.</p> <blockquote> <p>다음 글 <a href="/blog/2026/k8s-04-pod/">04: Pod의 모든 것 — 생성부터 스케줄링까지</a>에서는 Pod를 직접 띄우면서, 오늘 배운 컴포넌트들이 실제로 어떻게 협업하는지 5단계로 따라가 본다.</p> </blockquote> <hr/> <h1 id="참고-문헌">참고 문헌</h1> <ul> <li><a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua, SK Devocean, 2024) — 시리즈 로드맵 출처</li> <li><a href="https://kubernetes.io/docs/concepts/overview/components/">Kubernetes Components — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/concepts/architecture/">Cluster Architecture — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/concepts/architecture/controller/">Controllers — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/concepts/scheduling-eviction/kube-scheduler/">Kubernetes Scheduler — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/concepts/security/controlling-access/">Controlling Access to the Kubernetes API — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/tasks/administer-cluster/configure-upgrade-etcd/">Operating etcd clusters for Kubernetes — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/concepts/architecture/cri/">Container Runtime Interface (CRI) — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/reference/networking/virtual-ips/">Virtual IPs and Service Proxies — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/tasks/configure-pod-container/static-pod/">Create static Pods — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/reference/setup-tools/kubeadm/implementation-details/">kubeadm Implementation details — kubernetes.io</a></li> <li><a href="https://etcd.io/docs/v3.6/faq/">etcd FAQ — etcd.io</a></li> <li><a href="https://kind.sigs.k8s.io/docs/user/configuration/">kind Configuration — kind.sigs.k8s.io</a></li> </ul>]]></content><author><name></name></author><category term="infra"/><category term="kubernetes"/><category term="infra"/><category term="architecture"/><category term="etcd"/><category term="scheduler"/><summary type="html"><![CDATA[etcd, kube-apiserver, scheduler, kubelet — 클러스터를 움직이는 컴포넌트들의 역할과 설계 의도를 kind 클러스터에서 직접 확인한다]]></summary></entry><entry><title type="html">내 노트북에 Kubernetes 클러스터 만들기 — kind와 kubectl</title><link href="https://www.wonbeomjang.kr/blog/2026/k8s-02-local-setup/" rel="alternate" type="text/html" title="내 노트북에 Kubernetes 클러스터 만들기 — kind와 kubectl"/><published>2026-07-06T17:00:00+00:00</published><updated>2026-07-06T17:00:00+00:00</updated><id>https://www.wonbeomjang.kr/blog/2026/k8s-02-local-setup</id><content type="html" xml:base="https://www.wonbeomjang.kr/blog/2026/k8s-02-local-setup/"><![CDATA[<blockquote> <p>이 글은 <strong>K8s 입문 시리즈</strong>의 두 번째 글이다.</p> <ul> <li><a href="/blog/2026/k8s-01-history/">01: Kubernetes의 탄생 — Google Borg에서 CNCF까지</a></li> <li><strong>02: 내 노트북에 클러스터 만들기 — kind와 kubectl</strong> ← 현재 글</li> <li><a href="/blog/2026/k8s-03-architecture/">03: Kubernetes 아키텍처 — Control Plane과 Node</a></li> <li><a href="/blog/2026/k8s-04-pod/">04: Pod의 모든 것 — 생성부터 스케줄링까지</a></li> <li><a href="/blog/2026/k8s-05-workloads/">05: 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</a></li> <li><a href="/blog/2026/k8s-06-networking/">06: 네트워킹 — Service와 Ingress</a></li> <li><a href="/blog/2026/k8s-07-storage-config/">07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</a></li> <li><a href="/blog/2026/k8s-08-rbac/">08: 권한 관리 — ServiceAccount와 RBAC</a></li> <li><a href="/blog/2026/k8s-09-operator-cncf/">09: 확장과 생태계 — Operator와 CNCF Projects</a></li> </ul> <p>이 시리즈의 커리큘럼은 SK Devocean의 <a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua) 글의 학습 로드맵을 바탕으로 구성했다.</p> </blockquote> <p><a href="/blog/2026/k8s-01-history/">지난 글</a>에서 Kubernetes가 왜 태어났는지 봤다. 이제부터는 직접 손을 움직인다. 수영을 책으로만 배울 수 없듯이, Kubernetes도 명령을 쳐보지 않으면 절대 몸에 붙지 않는다.</p> <p>문제는 연습장이다. 클라우드에 진짜 클러스터를 만들면 시간당 요금이 나가고, 회사 클러스터에서 연습하다가는 사고가 난다. 우리에게 필요한 건 <strong>마음껏 만들고, 부수고, 다시 만들 수 있는 공짜 연습장</strong>이다. 다행히 노트북에 Docker만 있으면 몇 분 만에 진짜 Kubernetes 클러스터를 띄울 수 있다. 이 글에서는 그 연습장을 만들고, 앞으로 시리즈 내내 쓸 조작 도구(kubectl)까지 손에 익힌다.</p> <hr/> <h1 id="1-어떤-도구로-만들까--kind-vs-minikube">1. 어떤 도구로 만들까 — kind vs minikube</h1> <p>로컬 Kubernetes 도구로 가장 널리 쓰이는 두 가지가 <strong>minikube</strong>와 <strong>kind</strong>다. 둘 다 Kubernetes 공식 SIG(Special Interest Group)에서 관리하는 프로젝트라 어느 쪽을 골라도 신뢰할 수 있다.</p> <ul> <li><strong>minikube</strong>: 공식 문서의 소개 문장 그대로 “local Kubernetes, focusing on making it easy to learn and develop for Kubernetes” — 학습과 로컬 개발의 편의에 집중한 도구다. Docker, QEMU, Hyper-V, KVM, VirtualBox 등 다양한 드라이버 위에서 VM 또는 컨테이너로 클러스터를 띄운다.</li> <li><strong>kind</strong>: 이름부터 <strong>K</strong>ubernetes <strong>IN</strong> <strong>D</strong>ocker다. Docker 컨테이너 하나하나를 “노드”로 사용해 클러스터를 만든다. 원래 Kubernetes 프로젝트 자체를 테스트하기 위해 설계됐고, 로컬 개발과 CI에도 널리 쓰인다.</li> </ul> <p>kind의 발상이 재미있다. 보통 “노드”라고 하면 물리 서버나 VM을 떠올리는데, kind는 <strong>컨테이너를 노드로 쓴다</strong>. 마트료시카 인형처럼, 컨테이너(노드) 안에서 다시 컨테이너(Pod)가 도는 구조다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-02-local-setup/kind-cluster-in-docker-480.webp 480w,/assets/post/image/k8s-02-local-setup/kind-cluster-in-docker-800.webp 800w,/assets/post/image/k8s-02-local-setup/kind-cluster-in-docker-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-02-local-setup/kind-cluster-in-docker.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="kind 클러스터 구조 — 내 노트북의 Docker 안에서 컨테이너 하나가 노드 하나가 되고, kubectl은 API 서버로 접속한다" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>노드 1개 = Docker 컨테이너 1개. VM을 부팅할 필요가 없어서 클러스터 생성·삭제가 가볍다.</li> <li>kubectl은 <code class="language-plaintext highlighter-rouge">127.0.0.1</code>의 무작위 포트로 노출된 API 서버에 접속한다 (뒤에서 직접 확인한다).</li> <li>노드가 컨테이너일 뿐, 그 안에서 도는 Kubernetes는 진짜다. 03편에서 해부할 컴포넌트가 전부 들어 있다.</li> </ul> <p>두 도구를 표로 비교하면 이렇다.</p> <table> <thead> <tr> <th>항목</th> <th>kind</th> <th>minikube</th> </tr> </thead> <tbody> <tr> <td>노드 구현</td> <td>Docker 컨테이너 1개 = 노드 1개</td> <td>드라이버(Docker, QEMU, Hyper-V, KVM, VirtualBox 등)별 VM/컨테이너</td> </tr> <tr> <td>설계 목적</td> <td>Kubernetes 자체 테스트, CI</td> <td>학습·로컬 개발 편의</td> </tr> <tr> <td>멀티노드</td> <td>YAML config 파일로 선언</td> <td><code class="language-plaintext highlighter-rouge">minikube start --nodes 2</code> 플래그</td> </tr> <tr> <td>필요 환경</td> <td>Docker(또는 Podman, nerdctl)</td> <td>컨테이너 또는 VM 매니저, 2 CPU·2GB 메모리·20GB 디스크 이상</td> </tr> <tr> <td>클러스터 정의</td> <td>선언적 YAML</td> <td>명령행 플래그와 프로파일</td> </tr> </tbody> </table> <p>이 시리즈는 <strong>kind</strong>를 쓴다. 이유는 세 가지다.</p> <ol> <li><strong>멀티노드 클러스터를 YAML 한 장으로 선언</strong>할 수 있다. 03편(아키텍처), 05편(DaemonSet)에서 노드가 여러 개여야 보이는 것들이 있다.</li> <li>Docker만 있으면 된다. VM 부팅 과정이 없어 만들고 부수는 반복이 빠르다.</li> <li>클러스터를 YAML로 정의하는 습관 자체가 Kubernetes의 선언적 철학(01편)과 맞닿아 있다.</li> </ol> <p>물론 minikube도 훌륭한 도구고, minikube로도 이 시리즈를 그대로 따라올 수 있다. 도구보다 중요한 건 그 위에서 배우는 Kubernetes 자체다.</p> <hr/> <h1 id="2-kind-설치와-첫-클러스터">2. kind 설치와 첫 클러스터</h1> <p>준비물은 <strong>Docker</strong>뿐이다 (kind는 docker 외에 podman, nerdctl도 자동 감지한다). Docker Desktop 또는 Docker Engine이 돌고 있는지 먼저 확인하자.</p> <h2 id="21-kind-설치">2.1 kind 설치</h2> <p>macOS는 Homebrew로 설치한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew <span class="nb">install </span>kind
</code></pre></div></div> <p>Linux(x86_64)는 바이너리를 직접 내려받는다. 이 글 작성 시점의 최신 버전은 v0.32.0인데, 버전은 계속 올라가므로 <a href="https://github.com/kubernetes-sigs/kind/releases">릴리스 페이지</a>에서 최신 버전을 확인하고 URL의 버전만 바꿔주면 된다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span> <span class="si">$(</span><span class="nb">uname</span> <span class="nt">-m</span><span class="si">)</span> <span class="o">=</span> x86_64 <span class="o">]</span> <span class="o">&amp;&amp;</span> curl <span class="nt">-Lo</span> ./kind https://kind.sigs.k8s.io/dl/v0.32.0/kind-linux-amd64
<span class="nb">chmod</span> +x ./kind
<span class="nb">sudo mv</span> ./kind /usr/local/bin/kind
</code></pre></div></div> <p>설치 확인.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kind version
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kind v0.32.0 go1.26.3 linux/amd64
</code></pre></div></div> <h2 id="22-클러스터-생성--명령-한-줄">2.2 클러스터 생성 — 명령 한 줄</h2> <p>이제 이 글의 하이라이트다. 클러스터 생성은 정말로 한 줄이다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kind create cluster
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Creating cluster "kind" ...
 ✓ Ensuring node image (kindest/node:v1.36.1) 🖼
 ✓ Preparing nodes 📦
 ✓ Writing configuration 📜
 ✓ Starting control-plane 🕹️
 ✓ Installing CNI 🔌
 ✓ Installing StorageClass 💾
Set kubectl context to "kind-kind"
You can now use your cluster with:

kubectl cluster-info --context kind-kind

Have a nice day! 👋
</code></pre></div></div> <p>(노드 이미지 버전은 kind 버전에 따라 다르다. kind v0.32.0의 기본 이미지는 Kubernetes v1.36.1이다.)</p> <p>출력의 각 줄이 곧 클러스터가 만들어지는 과정이다.</p> <table> <thead> <tr> <th>출력 줄</th> <th>실제로 일어나는 일</th> </tr> </thead> <tbody> <tr> <td>Ensuring node image</td> <td>“노드 역할을 할 컨테이너 이미지”(kindest/node)를 내려받는다</td> </tr> <tr> <td>Preparing nodes</td> <td>노드가 될 Docker 컨테이너를 만든다</td> </tr> <tr> <td>Writing configuration</td> <td>클러스터 초기화 도구(kubeadm)의 설정을 쓴다</td> </tr> <tr> <td>Starting control-plane</td> <td>클러스터의 두뇌인 Control Plane을 부팅한다 — 03편에서 해부한다</td> </tr> <tr> <td>Installing CNI</td> <td>Pod끼리 통신할 네트워크 플러그인을 깐다 — 03편에서 다룬다</td> </tr> <tr> <td>Installing StorageClass</td> <td>기본 스토리지 설정을 깐다 — 07편에서 다룬다</td> </tr> <tr> <td>Set kubectl context</td> <td>방금 만든 클러스터로 kubectl이 접속하도록 설정한다 — 이 글의 5~6장 주제</td> </tr> </tbody> </table> <p>정말 컨테이너가 노드 행세를 하고 있는지 Docker로 확인해 보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker ps
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CONTAINER ID   IMAGE                  COMMAND                  ...   PORTS                       NAMES
1f2a3b4c5d6e   kindest/node:v1.36.1   "/usr/local/bin/entr…"   ...   127.0.0.1:39663-&gt;6443/tcp   kind-control-plane
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">kind-control-plane</code>이라는 컨테이너 하나가 노드다. 클러스터 이름의 기본값이 <code class="language-plaintext highlighter-rouge">kind</code>이고, 노드 이름은 <code class="language-plaintext highlighter-rouge">&lt;클러스터 이름&gt;-&lt;역할&gt;</code> 규칙으로 붙는다.</li> <li><code class="language-plaintext highlighter-rouge">127.0.0.1:39663-&gt;6443/tcp</code>: 컨테이너 안의 API 서버 포트(6443)가 내 노트북의 무작위 포트로 연결돼 있다 (포트 번호는 만들 때마다 다르다). kubectl은 이 문으로 들어간다.</li> </ul> <p>만들어진 클러스터 목록도 확인할 수 있다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kind get clusters
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kind
</code></pre></div></div> <p>클러스터는 떴다. 그런데 지금은 서버만 있고 전화기가 없는 상태다. 클러스터에 말을 걸 도구, <strong>kubectl</strong>을 설치할 차례다.</p> <hr/> <h1 id="3-kubectl-설치와-첫-접속">3. kubectl 설치와 첫 접속</h1> <p>kubectl은 Kubernetes API 서버와 대화하는 공식 CLI다. 앞으로 시리즈가 끝날 때까지 가장 많이 치게 될 명령이다.</p> <p>macOS는 Homebrew로 설치한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>brew <span class="nb">install </span>kubectl
</code></pre></div></div> <p>Linux(x86_64)는 최신 안정 버전을 내려받아 설치한다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-LO</span> <span class="s2">"https://dl.k8s.io/release/</span><span class="si">$(</span>curl <span class="nt">-L</span> <span class="nt">-s</span> https://dl.k8s.io/release/stable.txt<span class="si">)</span><span class="s2">/bin/linux/amd64/kubectl"</span>
<span class="nb">sudo install</span> <span class="nt">-o</span> root <span class="nt">-g</span> root <span class="nt">-m</span> 0755 kubectl /usr/local/bin/kubectl
</code></pre></div></div> <p>다른 OS는 <a href="https://kubernetes.io/docs/tasks/tools/">공식 설치 문서</a>를 참고하면 된다. 한 가지 규칙만 기억하자. 공식 문서에 따르면 <strong>kubectl 버전은 클러스터와 마이너 버전 1 이내</strong>여야 한다. 예를 들어 v1.36 클라이언트는 v1.35, v1.36, v1.37 Control Plane과 통신할 수 있다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl version <span class="nt">--client</span>
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Client Version: v1.36.2
Kustomize Version: v5.8.1
</code></pre></div></div> <p>이제 첫 접속이다. 아까 kind가 알려준 명령을 그대로 쳐보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl cluster-info
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kubernetes control plane is running at https://127.0.0.1:39663
CoreDNS is running at https://127.0.0.1:39663/api/v1/namespaces/kube-system/services/kube-dns:dns/proxy

To further debug and diagnose cluster problems, use 'kubectl cluster-info dump'.
</code></pre></div></div> <p>(포트 번호는 환경마다 다르다.) 주소를 보면 아까 <code class="language-plaintext highlighter-rouge">docker ps</code>에서 본 <code class="language-plaintext highlighter-rouge">127.0.0.1:39663</code>과 같다. <strong>kubectl → 노트북의 무작위 포트 → 컨테이너 안의 API 서버</strong>로 연결이 이어진 것이다. 노드도 확인해 보자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                 STATUS   ROLES           AGE   VERSION
kind-control-plane   Ready    control-plane   2m    v1.36.1
</code></pre></div></div> <p>노드 하나짜리 클러스터다. 연습용으로는 충분하지만, 앞으로를 위해 좀 더 실전에 가까운 모양으로 바꿔보자.</p> <hr/> <h1 id="4-멀티노드-클러스터로-업그레이드">4. 멀티노드 클러스터로 업그레이드</h1> <p>실제 프로덕션 클러스터는 Control Plane과 Worker가 분리된 여러 노드로 구성된다. kind에서는 클러스터 구성을 YAML 파일로 선언하면 된다. 먼저 기존 클러스터를 지운다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kind delete cluster
</code></pre></div></div> <p>그리고 <code class="language-plaintext highlighter-rouge">kind-config.yaml</code> 파일을 만든다.</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># kind-config.yaml</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Cluster</span>
<span class="na">apiVersion</span><span class="pi">:</span> <span class="s">kind.x-k8s.io/v1alpha4</span>
<span class="na">nodes</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">role</span><span class="pi">:</span> <span class="s">control-plane</span>
  <span class="pi">-</span> <span class="na">role</span><span class="pi">:</span> <span class="s">worker</span>
  <span class="pi">-</span> <span class="na">role</span><span class="pi">:</span> <span class="s">worker</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">kind: Cluster</code> — 여기서 <code class="language-plaintext highlighter-rouge">kind</code>는 도구 이름이 아니라 “이 YAML이 어떤 종류의 리소스인지”를 나타내는 Kubernetes 스타일 필드다. 앞으로 모든 YAML에서 만나게 된다.</li> <li><code class="language-plaintext highlighter-rouge">nodes</code> 아래에 원하는 노드를 역할(<code class="language-plaintext highlighter-rouge">control-plane</code> / <code class="language-plaintext highlighter-rouge">worker</code>)과 함께 나열하면 끝이다.</li> </ul> <p>이 파일로 클러스터를 만든다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kind create cluster <span class="nt">--config</span> kind-config.yaml
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Creating cluster "kind" ...
 ✓ Ensuring node image (kindest/node:v1.36.1) 🖼
 ✓ Preparing nodes 📦 📦 📦
 ✓ Writing configuration 📜
 ✓ Starting control-plane 🕹️
 ✓ Installing CNI 🔌
 ✓ Installing StorageClass 💾
 ✓ Joining worker nodes 🚜
Set kubectl context to "kind-kind"
...
</code></pre></div></div> <p>아까와 달라진 점이 두 가지다. <code class="language-plaintext highlighter-rouge">Preparing nodes</code>의 📦가 노드 수만큼 3개가 됐고, <code class="language-plaintext highlighter-rouge">Joining worker nodes</code> 단계가 새로 생겼다. Worker 노드들이 Control Plane에 합류하는 과정이다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>NAME                 STATUS   ROLES           AGE   VERSION
kind-control-plane   Ready    control-plane   3m    v1.36.1
kind-worker          Ready    &lt;none&gt;          2m    v1.36.1
kind-worker2         Ready    &lt;none&gt;          2m    v1.36.1
</code></pre></div></div> <ul> <li>노드 이름은 <code class="language-plaintext highlighter-rouge">&lt;클러스터 이름&gt;-&lt;역할&gt;</code> 규칙이고, 같은 역할이 여러 개면 두 번째부터 숫자가 붙는다 (<code class="language-plaintext highlighter-rouge">kind-worker</code>, <code class="language-plaintext highlighter-rouge">kind-worker2</code>).</li> <li>Worker의 ROLES가 <code class="language-plaintext highlighter-rouge">&lt;none&gt;</code>으로 보이는 건 정상이다. ROLES 컬럼은 노드에 붙은 role 라벨을 보여주는 것인데, Worker에는 이 라벨이 자동으로 붙지 않는다.</li> </ul> <p>참고로 <code class="language-plaintext highlighter-rouge">--name</code> 플래그를 쓰면 클러스터를 여러 개 만들 수도 있다 (<code class="language-plaintext highlighter-rouge">kind create cluster --name kind-2</code>). 이름이 다르면 공존이 가능하고, <code class="language-plaintext highlighter-rouge">kind get clusters</code>로 모두 확인할 수 있다.</p> <p>노트북 한 대에 3노드 Kubernetes 클러스터가 생겼다. 그런데 궁금하지 않은가? 클러스터를 지우고 다시 만들어도 kubectl은 용케 새 클러스터를 찾아간다. <strong>kubectl은 어떻게 접속할 곳을 아는 걸까?</strong></p> <hr/> <h1 id="5-kubectl은-어떻게-클러스터를-찾나--kubeconfig-해부">5. kubectl은 어떻게 클러스터를 찾나 — kubeconfig 해부</h1> <p>답은 <code class="language-plaintext highlighter-rouge">~/.kube/config</code> 파일에 있다. kubectl은 기본적으로 이 파일(<strong>kubeconfig</strong>라고 부른다)을 읽어 어느 클러스터에 어떤 신원으로 접속할지 결정한다. kind가 클러스터를 만들 때 “Set kubectl context to …“라고 출력한 것이 바로 이 파일을 갱신했다는 뜻이다.</p> <p>kubeconfig는 세 가지 목록으로 구성된다.</p> <table> <thead> <tr> <th>요소</th> <th>답하는 질문</th> <th>담는 내용</th> <th>비유</th> </tr> </thead> <tbody> <tr> <td><strong>clusters</strong></td> <td><strong>어디로</strong> 접속하나</td> <td>API 서버 주소, CA 인증서</td> <td>주소록</td> </tr> <tr> <td><strong>users</strong></td> <td><strong>누구로</strong> 접속하나</td> <td>클라이언트 인증서, 토큰 등 인증 정보</td> <td>신분증 지갑</td> </tr> <tr> <td><strong>contexts</strong></td> <td>어떤 <strong>조합</strong>으로</td> <td>cluster + user + namespace 묶음</td> <td>단축 다이얼</td> </tr> </tbody> </table> <p>핵심은 <strong>context</strong>다. 공식 문서의 정의 그대로, context는 cluster·namespace·user 세 파라미터를 편한 이름 하나로 묶은 것이다. “어느 클러스터에(cluster), 어떤 신원으로(user), 기본 작업 공간은 어디로(namespace)”를 매번 지정하는 대신, 조합에 이름을 붙여 단축 다이얼처럼 쓴다.</p> <p>구조를 단순화한 예시를 보면 이렇다 (인증서 데이터는 생략).</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">apiVersion</span><span class="pi">:</span> <span class="s">v1</span>
<span class="na">kind</span><span class="pi">:</span> <span class="s">Config</span>
<span class="na">clusters</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">kind-kind</span>
    <span class="na">cluster</span><span class="pi">:</span>
      <span class="na">server</span><span class="pi">:</span> <span class="s">https://127.0.0.1:39663</span>
      <span class="na">certificate-authority-data</span><span class="pi">:</span> <span class="s">LS0t...</span> <span class="c1"># 생략</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">company-prod</span>
    <span class="na">cluster</span><span class="pi">:</span>
      <span class="na">server</span><span class="pi">:</span> <span class="s">https://prod.company.example.com:6443</span>
      <span class="na">certificate-authority-data</span><span class="pi">:</span> <span class="s">LS0t...</span> <span class="c1"># 생략</span>
<span class="na">users</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">kind-kind</span>
    <span class="na">user</span><span class="pi">:</span> <span class="pi">{}</span> <span class="c1"># 인증서 등 인증 정보</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">prod-admin</span>
    <span class="na">user</span><span class="pi">:</span> <span class="pi">{}</span> <span class="c1"># 토큰 등 인증 정보</span>
<span class="na">contexts</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">kind-kind</span>
    <span class="na">context</span><span class="pi">:</span>
      <span class="na">cluster</span><span class="pi">:</span> <span class="s">kind-kind</span>
      <span class="na">user</span><span class="pi">:</span> <span class="s">kind-kind</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">company-prod</span>
    <span class="na">context</span><span class="pi">:</span>
      <span class="na">cluster</span><span class="pi">:</span> <span class="s">company-prod</span>
      <span class="na">user</span><span class="pi">:</span> <span class="s">prod-admin</span>
      <span class="na">namespace</span><span class="pi">:</span> <span class="s">ml-serving</span>
<span class="na">current-context</span><span class="pi">:</span> <span class="s">kind-kind</span>
</code></pre></div></div> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-02-local-setup/kubeconfig-structure-480.webp 480w,/assets/post/image/k8s-02-local-setup/kubeconfig-structure-800.webp 800w,/assets/post/image/k8s-02-local-setup/kubeconfig-structure-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-02-local-setup/kubeconfig-structure.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="kubeconfig 구조 — current-context가 가리키는 context가 cluster(어디로)와 user(누구로)를 묶는다" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li><code class="language-plaintext highlighter-rouge">current-context</code>가 지금 kubectl이 사용하는 단축 다이얼이다. 별도 지정이 없으면 모든 kubectl 명령이 이 컨텍스트로 나간다.</li> <li>kind는 컨텍스트 이름을 <code class="language-plaintext highlighter-rouge">kind-&lt;클러스터 이름&gt;</code> 형식으로 등록한다. 기본 클러스터 이름이 <code class="language-plaintext highlighter-rouge">kind</code>라서 컨텍스트가 <code class="language-plaintext highlighter-rouge">kind-kind</code>가 된다.</li> <li>kubeconfig 파일을 여러 개 쓸 수도 있다. 환경 변수 <code class="language-plaintext highlighter-rouge">KUBECONFIG</code>에 콜론(<code class="language-plaintext highlighter-rouge">:</code>)으로 구분해 나열하면 kubectl이 병합해서 본다 (Linux/macOS 기준).</li> </ul> <hr/> <h1 id="6-컨텍스트-전환-실습--로컬과-회사-클러스터-오가기">6. 컨텍스트 전환 실습 — 로컬과 회사 클러스터 오가기</h1> <p>컨텍스트가 진짜 중요해지는 순간은 클러스터가 두 개 이상일 때다. 위 예시처럼 로컬 연습용 <code class="language-plaintext highlighter-rouge">kind-kind</code>와 회사 프로덕션 <code class="language-plaintext highlighter-rouge">company-prod</code>가 함께 등록된 상황을 가정하자. 컨텍스트 조작 명령은 세 개만 알면 된다.</p> <p>먼저 등록된 컨텍스트 목록.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config get-contexts
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CURRENT   NAME           CLUSTER        AUTHINFO     NAMESPACE
          company-prod   company-prod   prod-admin   ml-serving
*         kind-kind      kind-kind      kind-kind
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">*</code>가 현재 컨텍스트다. NAMESPACE가 비어 있으면 <code class="language-plaintext highlighter-rouge">default</code> 네임스페이스를 쓴다는 뜻이다.</li> </ul> <p>현재 컨텍스트만 빠르게 확인하려면.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config current-context
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kind-kind
</code></pre></div></div> <p>회사 클러스터로 전환하려면.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context company-prod
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Switched to context "company-prod".
</code></pre></div></div> <p>이 순간부터 모든 kubectl 명령은 프로덕션으로 날아간다. 그리고 여기에 <strong>입문자가 반드시 알아야 할 사고 패턴</strong>이 있다. 어제 회사 클러스터를 보다가 컨텍스트를 안 돌려놓고, 오늘 “로컬 연습장이겠지” 하며 <code class="language-plaintext highlighter-rouge">kubectl delete</code>를 치는 사고다. 터미널 화면만 봐서는 어느 클러스터를 가리키는지 티가 나지 않는다. 그래서 습관 두 가지를 권한다.</p> <ol> <li><strong>파괴적인 명령 전에는 <code class="language-plaintext highlighter-rouge">kubectl config current-context</code>부터</strong> 친다. 3초짜리 보험이다.</li> <li>일회성으로 다른 클러스터를 조회할 때는 전환 대신 <code class="language-plaintext highlighter-rouge">--context</code> 플래그를 쓴다. kubectl은 <code class="language-plaintext highlighter-rouge">--context</code> 플래그를 current-context보다 우선하므로, 현재 컨텍스트를 건드리지 않고 한 번만 다른 곳에 명령을 보낼 수 있다.</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get nodes <span class="nt">--context</span> kind-kind
</code></pre></div></div> <p>실습을 마쳤으면 로컬로 돌아오자.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl config use-context kind-kind
</code></pre></div></div> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Switched to context "kind-kind".
</code></pre></div></div> <hr/> <h1 id="7-마무리">7. 마무리</h1> <p>오늘 만든 것을 한 문장으로 요약하면 이렇다. <strong>Docker 컨테이너를 노드 삼아(kind) 노트북 안에 3노드짜리 진짜 Kubernetes 클러스터를 세웠고, kubectl이 kubeconfig의 컨텍스트를 통해 그 클러스터를 찾아가는 경로를 이해했다.</strong></p> <table> <thead> <tr> <th>명령</th> <th>역할</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">kind create cluster</code></td> <td>클러스터 생성 (기본 이름 <code class="language-plaintext highlighter-rouge">kind</code>)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kind create cluster --config ...</code></td> <td>YAML로 선언한 구성(멀티노드 등)으로 생성</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kind get clusters</code></td> <td>만들어진 클러스터 목록</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kind delete cluster</code></td> <td>클러스터 삭제</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubectl version --client</code></td> <td>클라이언트 버전 확인</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubectl cluster-info</code></td> <td>현재 클러스터의 API 서버 주소 확인</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubectl get nodes</code></td> <td>노드 목록과 상태 확인</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubectl config get-contexts</code></td> <td>등록된 컨텍스트 목록 (<code class="language-plaintext highlighter-rouge">*</code>가 현재)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubectl config current-context</code></td> <td>현재 컨텍스트 확인 — 파괴적 명령 전 필수</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">kubectl config use-context &lt;이름&gt;</code></td> <td>컨텍스트 전환</td> </tr> </tbody> </table> <p>방금 만든 멀티노드 클러스터는 <strong>지우지 말고 그대로 두자</strong>. <code class="language-plaintext highlighter-rouge">kind create cluster</code> 한 줄 뒤에서 조용히 부팅된 Control Plane 안에는 여러 컴포넌트가 각자의 역할을 맡아 돌아가고 있다. 다음 글에서 그 내부를 하나씩 해부한다.</p> <blockquote> <p>다음 글 <a href="/blog/2026/k8s-03-architecture/">03: Kubernetes 아키텍처 — Control Plane과 Node</a>에서는 오늘 만든 클러스터의 내부 — etcd, kube-apiserver, scheduler, kubelet — 를 해부한다.</p> </blockquote> <hr/> <h1 id="참고-문헌">참고 문헌</h1> <ul> <li><a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua, SK Devocean, 2024) — 시리즈 로드맵 출처</li> <li><a href="https://kind.sigs.k8s.io/docs/user/quick-start/">kind — Quick Start</a></li> <li><a href="https://kind.sigs.k8s.io/docs/user/configuration/">kind — Configuration</a></li> <li><a href="https://github.com/kubernetes-sigs/kind/releases">kind Releases — GitHub</a></li> <li><a href="https://minikube.sigs.k8s.io/docs/start/">minikube — Get Started</a></li> <li><a href="https://minikube.sigs.k8s.io/docs/tutorials/multi_node/">minikube — Using Multi-Node Clusters</a></li> <li><a href="https://kubernetes.io/docs/tasks/tools/install-kubectl-linux/">Install and Set Up kubectl on Linux — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/tasks/tools/install-kubectl-macos/">Install and Set Up kubectl on macOS — kubernetes.io</a></li> <li><a href="https://kubernetes.io/docs/concepts/configuration/organize-cluster-access-kubeconfig/">Organizing Cluster Access Using kubeconfig Files — kubernetes.io</a></li> </ul>]]></content><author><name></name></author><category term="infra"/><category term="kubernetes"/><category term="infra"/><category term="kind"/><category term="minikube"/><category term="kubectl"/><summary type="html"><![CDATA[kind로 노트북에 멀티노드 Kubernetes 클러스터를 띄우고, kubectl과 kubeconfig(clusters/users/contexts)로 클러스터를 다루는 법]]></summary></entry><entry><title type="html">Kubernetes의 탄생 — Google Borg에서 CNCF까지</title><link href="https://www.wonbeomjang.kr/blog/2026/k8s-01-history/" rel="alternate" type="text/html" title="Kubernetes의 탄생 — Google Borg에서 CNCF까지"/><published>2026-07-06T16:00:00+00:00</published><updated>2026-07-06T16:00:00+00:00</updated><id>https://www.wonbeomjang.kr/blog/2026/k8s-01-history</id><content type="html" xml:base="https://www.wonbeomjang.kr/blog/2026/k8s-01-history/"><![CDATA[<blockquote> <p>이 글은 <strong>K8s 입문 시리즈</strong>의 첫 번째 글이다.</p> <ul> <li><strong>01: Kubernetes의 탄생 — Google Borg에서 CNCF까지</strong> ← 현재 글</li> <li><a href="/blog/2026/k8s-02-local-setup/">02: 내 노트북에 클러스터 만들기 — kind와 kubectl</a></li> <li><a href="/blog/2026/k8s-03-architecture/">03: Kubernetes 아키텍처 — Control Plane과 Node</a></li> <li><a href="/blog/2026/k8s-04-pod/">04: Pod의 모든 것 — 생성부터 스케줄링까지</a></li> <li><a href="/blog/2026/k8s-05-workloads/">05: 워크로드 — ReplicaSet, Deployment, StatefulSet, DaemonSet</a></li> <li><a href="/blog/2026/k8s-06-networking/">06: 네트워킹 — Service와 Ingress</a></li> <li><a href="/blog/2026/k8s-07-storage-config/">07: 스토리지와 설정 — PV/PVC, ConfigMap, Secret</a></li> <li><a href="/blog/2026/k8s-08-rbac/">08: 권한 관리 — ServiceAccount와 RBAC</a></li> <li><a href="/blog/2026/k8s-09-operator-cncf/">09: 확장과 생태계 — Operator와 CNCF Projects</a></li> </ul> <p>이 시리즈의 커리큘럼은 SK Devocean의 <a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua) 글의 학습 로드맵을 바탕으로 구성했다.</p> </blockquote> <h1 id="1-docker-run-하나면-충분한데-왜-kubernetes인가">1. docker run 하나면 충분한데, 왜 Kubernetes인가</h1> <p>컨테이너 하나를 띄우는 건 정말 쉽다. Docker가 설치된 서버에서 명령 한 줄이면 끝난다.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run <span class="nt">-d</span> <span class="nt">-p</span> 80:80 nginx
</code></pre></div></div> <p>개발 서버 한 대에 컨테이너 몇 개를 올리는 수준이라면 이걸로 충분하다. 문제는 서비스가 커지면서 시작된다. 구체적인 시나리오로 따라가 보자.</p> <p>서비스가 잘 돼서 서버가 3대가 됐다. 컨테이너는 30개다. 이제 이런 질문이 생긴다.</p> <ul> <li>새 컨테이너는 <strong>어느 서버에</strong> 띄워야 하나? 메모리가 남는 서버를 사람이 일일이 확인해야 하나?</li> <li>새벽 3시에 컨테이너가 죽으면 <strong>누가 재시작</strong>하나? 당직을 세워야 하나?</li> <li>트래픽이 몰리면 컨테이너를 늘려야 하는데, 서버마다 SSH로 들어가서 <code class="language-plaintext highlighter-rouge">docker run</code>을 반복해야 하나?</li> <li>컨테이너가 재시작되면 IP가 바뀌는데, 서로를 <strong>어떻게 찾아가나?</strong> 설정 파일을 매번 고쳐야 하나?</li> <li>새 버전을 배포할 때 서비스가 <strong>끊기지 않게</strong> 하려면? 30개를 하나씩 손으로 교체해야 하나?</li> </ul> <p>서버가 100대, 컨테이너가 1,000개가 되면 이 질문들은 “불편함”이 아니라 “불가능”이 된다. 사람이 스프레드시트로 서버별 컨테이너 배치를 관리하고, 장애 알림을 받고 수동으로 복구하는 방식은 규모가 커지는 순간 무너진다.</p> <p>식당에 비유하면 이렇다. 주방장 혼자 요리하는 동네 식당은 사장이 머릿속으로 다 관리할 수 있다. 하지만 지점이 100개인 프랜차이즈라면 본사가 필요하다. 어느 지점에 인력을 배치할지, 재료가 떨어진 지점을 어떻게 채울지, 신메뉴를 전 지점에 어떻게 순차 도입할지 — 이걸 자동으로 해주는 <strong>본사 시스템</strong>이 바로 컨테이너 오케스트레이터(orchestrator)이고, 그 사실상 표준이 <strong>Kubernetes</strong>다.</p> <p>이 문제를 세상에서 가장 먼저, 가장 큰 규모로 겪은 회사가 Google이다. Kubernetes의 이야기는 Google 내부 시스템 <strong>Borg</strong>에서 시작한다.</p> <hr/> <h1 id="2-google-borg--kubernetes의-조상">2. Google Borg — Kubernetes의 조상</h1> <p>Google은 검색, Gmail, Google Docs 같은 서비스를 전 세계 규모로 운영하면서 일찍부터 컨테이너 기술을 사용했다. 그리고 이 컨테이너들을 관리하기 위해 <strong>Borg</strong>라는 내부 클러스터 매니저를 만들었다.</p> <p>Borg의 존재는 오랫동안 외부에 자세히 공개되지 않다가, 2015년 EuroSys 학회에서 논문으로 발표됐다.</p> <blockquote> <p><a href="https://research.google/pubs/large-scale-cluster-management-at-google-with-borg/">Large-scale cluster management at Google with Borg</a> (Verma et al., Google, EuroSys 2015)</p> </blockquote> <p>논문이 밝힌 Borg의 규모는 이렇다.</p> <ul> <li>클러스터 하나가 <strong>최대 수만 대(tens of thousands)의 머신</strong>으로 구성된다.</li> <li>수천 개 애플리케이션에서 나온 <strong>수십만 개(hundreds of thousands)의 job</strong>을 실행한다.</li> <li>Gmail, Google Docs, 웹 검색 같은 사용자 대면 서비스와 내부 인프라 서비스가 모두 Borg 위에서 돌아간다.</li> <li>admission control, 효율적인 task packing, 오버커밋, 머신 공유로 <strong>높은 자원 활용률</strong>을 달성한다.</li> <li>선언적(declarative) job 명세 언어, 네임 서비스 연동, 실시간 모니터링을 제공한다.</li> </ul> <p>주목할 점은 “선언적 명세”라는 단어다. 사용자가 “이 작업을 이런 조건으로 실행해 달라”고 <strong>선언</strong>하면, 어느 머신에 배치할지·죽으면 어떻게 복구할지는 Borg가 알아서 처리한다. 이 철학이 Kubernetes에 그대로 이어진다.</p> <h2 id="21-borg--omega--kubernetes-계보">2.1 Borg → Omega → Kubernetes 계보</h2> <p>Google은 10년 넘게 컨테이너를 대규모로 운영하면서 컨테이너 관리 시스템을 세 번 만들었다. 이 경험은 ACM Queue에 실린 회고 글에 정리되어 있다.</p> <blockquote> <p><a href="https://queue.acm.org/detail.cfm?id=2898444">Borg, Omega, and Kubernetes</a> (Burns et al., Google, ACM Queue 2016)</p> </blockquote> <p>회고의 요지는 이렇다. 세 시스템은 성격이 서로 달랐다. <strong>Borg</strong>는 배치(batch) 작업과 상시 서비스라는 이질적인 워크로드를 같은 머신에 섞어 배치해 자원 활용률을 끌어올린 <strong>실전 시스템</strong>이고, <strong>Omega</strong>는 Borg 생태계를 소프트웨어 공학적으로 더 깨끗하게 재설계해 보려던 <strong>연구 프로젝트</strong>였으며, <strong>Kubernetes</strong>는 그 둘의 교훈을 Google 바깥의 개발자를 위해 풀어낸 <strong>오픈소스</strong>다. 저자들이 꼽는 10년의 가장 큰 교훈은 개별 기능이 아니라 관점의 전환이다 — 컨테이너 덕분에 데이터센터의 관리 단위가 <strong>머신에서 애플리케이션으로</strong> 바뀌었다는 것. 개발자는 “어느 서버에 무엇을 설치할까”를 고민하는 대신 “내 애플리케이션이 어떤 상태여야 하는가”를 선언하면 되고, 머신·OS 세부 사항은 인프라가 흡수한다. 이 교훈이 구체적으로 어떤 설계로 이어졌는지는 2.2절의 표에서 정리한다.</p> <p>두 번째 시스템인 <strong>Omega</strong>는 Borg의 아키텍처적 한계를 개선하려는 연구였다. 하나의 거대한(monolithic) 스케줄러가 모든 결정을 내리는 대신, 여러 스케줄러가 <strong>공유 상태(shared state)</strong> 를 보며 <strong>낙관적 동시성 제어(optimistic concurrency control)</strong> 로 병렬로 스케줄링하는 구조를 제안했다.</p> <blockquote> <p><a href="https://research.google/pubs/omega-flexible-scalable-schedulers-for-large-compute-clusters/">Omega: flexible, scalable schedulers for large compute clusters</a> (Schwarzkopf et al., Google, EuroSys 2013)</p> </blockquote> <p>그리고 세 번째가 <strong>Kubernetes</strong>다. Borg와 Omega가 Google 내부 전용이었던 것과 달리, Kubernetes는 처음부터 <strong>오픈소스</strong>로, Google 바깥의 개발자를 위해 만들어졌다.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-01-history/borg-to-cncf-480.webp 480w,/assets/post/image/k8s-01-history/borg-to-cncf-800.webp 800w,/assets/post/image/k8s-01-history/borg-to-cncf-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-01-history/borg-to-cncf.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="Borg에서 Omega, Kubernetes를 거쳐 CNCF로 이어지는 계보" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>다이어그램에서 눈여겨볼 점은 다음과 같다.</p> <ul> <li><strong>시스템의 순서와 논문 공개 순서가 다르다.</strong> Borg는 훨씬 오래된 시스템이지만 논문은 Omega(2013)보다 늦은 2015년에 나왔다. Kubernetes가 공개(2014)된 뒤에야 그 뿌리인 Borg가 논문으로 세상에 알려진 셈이다.</li> <li>Kubernetes는 무에서 만들어진 것이 아니라 <strong>Borg와 Omega에서 10년 넘게 쌓인 운영 경험과 교훈의 결정체</strong>다. “왜 Kubernetes는 이렇게 설계됐지?”라는 질문의 답이 이 계보에 숨어 있는 경우가 많다.</li> <li>Kubernetes는 v1.0이 되는 날 CNCF에 기부되며 <strong>한 회사의 프로젝트에서 커뮤니티의 프로젝트</strong>가 됐다.</li> </ul> <h2 id="22-borg가-kubernetes에-남긴-유산">2.2 Borg가 Kubernetes에 남긴 유산</h2> <p>ACM Queue 회고 글은 Borg·Omega에서 배운 교훈이 Kubernetes 설계에 어떻게 반영됐는지를 구체적으로 짚는다. 앞으로 이 시리즈에서 만날 핵심 개념들이 사실은 전부 이 유산이다.</p> <table> <thead> <tr> <th>Borg/Omega에서의 경험</th> <th>Kubernetes의 설계</th> <th>시리즈에서 다루는 편</th> </tr> </thead> <tbody> <tr> <td>컨테이너 하나가 아니라 <strong>함께 배치되는 컨테이너 묶음</strong>이 실제 배포 단위였다</td> <td>최소 배포 단위를 컨테이너가 아닌 <strong>Pod</strong>로 정의</td> <td>04편</td> </tr> <tr> <td>노드 IP를 공유하고 포트를 나눠 쓰는 방식은 포트 충돌 등 복잡성이 컸다</td> <td><strong>Pod마다 고유 IP</strong>를 부여해 포트 관리 문제를 제거</td> <td>04·06편</td> </tr> <tr> <td>경직된 job 계층 구조로는 워크로드를 유연하게 묶기 어려웠다</td> <td>자유롭게 붙이고 조회하는 <strong>Label</strong> 기반 그룹핑</td> <td>05편</td> </tr> <tr> <td>중앙 시스템이 모든 것을 명령하는 구조는 확장에 불리했다</td> <td><strong>API 서버 + 선언적 상태 + reconcile 컨트롤러</strong> 구조</td> <td>03·05편</td> </tr> <tr> <td>Omega의 <strong>공유 상태(shared state)</strong> 아키텍처 실험</td> <td>모든 컴포넌트가 API 서버를 통해 상태 저장소를 공유하는 구조</td> <td>03편</td> </tr> </tbody> </table> <p>한 가지 더, Borg의 컨테이너 격리는 리눅스 커널의 <strong>cgroups</strong>를 기반으로 했다. Docker를 포함한 오늘날의 컨테이너 기술 역시 같은 커널 기능 위에 서 있으니, Kubernetes와 Docker는 뿌리에서부터 같은 토양을 공유하는 셈이다.</p> <p>정리하면, 뒤에서 배울 Pod·Label·컨트롤러 같은 개념은 누군가 책상에서 발명한 것이 아니라 <strong>Google이 10년 넘게 실제 서비스를 운영하며 검증한 패턴</strong>이다. “왜 이런 개념이 있지?”라는 의문이 들 때마다 이 표로 돌아오면 답이 보인다.</p> <hr/> <h1 id="3-오픈소스로--2014년-공개-2015년-v10">3. 오픈소스로 — 2014년 공개, 2015년 v1.0</h1> <h2 id="31-2014년-6월-kubernetes-공개">3.1 2014년 6월: Kubernetes 공개</h2> <p>2014년 6월, Google은 Kubernetes를 오픈소스 프로젝트로 공개했다. 당시는 Docker의 등장으로 컨테이너가 폭발적으로 대중화되던 시점이었다. 개별 컨테이너를 만들고 실행하는 문제는 Docker가 풀었지만, 1장에서 본 “수백 대 서버 위 수천 개 컨테이너”의 운영 문제는 여전히 비어 있었고, Google은 Borg의 경험을 오픈소스로 풀어내며 이 자리를 차지했다.</p> <p>이름에 대한 상식 하나. <strong>Kubernetes</strong>는 그리스어 <strong>κυβερνήτης</strong>(kubernētēs)에서 왔고, <strong>키잡이(helmsman)·조타수(pilot)</strong> 라는 뜻이다. 컨테이너를 실은 배(Docker의 고래 로고를 떠올려 보자)를 조종하는 키잡이라는 의미가 담겨 있다. 흔히 쓰는 약어 <strong>K8s</strong>는 K와 s 사이의 8글자를 숫자로 줄인 표기(numeronym)다.</p> <h2 id="32-2015년-7월-21일-v10과-cncf-창립">3.2 2015년 7월 21일: v1.0과 CNCF 창립</h2> <p>약 1년 뒤인 <strong>2015년 7월 21일</strong>, Kubernetes v1.0이 릴리스됐다. 개발팀이 “실제 프로덕션에서 쓸 수 있을 만큼 안정적”이라고 선언한 첫 버전이다.</p> <p>그리고 <strong>같은 날</strong>, Linux Foundation은 <strong>CNCF(Cloud Native Computing Foundation)</strong> 창립을 발표했다. Google은 Kubernetes를 CNCF에 <strong>시드(seed) 기술로 기부</strong>했고, Google 외에도 Red Hat, Docker, IBM, Intel, Cisco, VMware, CoreOS, Twitter 등 21개 조직이 창립 멤버로 참여했다.</p> <p>여기서 “왜”를 물어볼 필요가 있다. Google은 왜 애써 만든 프로젝트를 재단에 넘겼을까?</p> <ul> <li><strong>벤더 중립성</strong> 때문이다. Kubernetes가 Google 소유로 남아 있었다면, 경쟁사인 AWS·Microsoft·Red Hat이 자사 제품의 기반으로 채택하기 어려웠을 것이다. 중립 재단이 상표권과 거버넌스를 관리하면 모든 회사가 안심하고 올라탈 수 있다.</li> <li><strong>생태계 선점</strong> 효과도 있다. 당시 컨테이너 오케스트레이션은 Docker Swarm, Apache Mesos 등과의 경쟁 구도였다. 오픈 거버넌스는 커뮤니티와 기업의 기여를 끌어모으는 가장 강력한 무기였고, 실제로 Kubernetes는 이 경쟁에서 사실상 표준의 자리를 가져갔다.</li> </ul> <p>연도가 헷갈리기 쉬우니 표로 정리한다.</p> <table> <thead> <tr> <th>연도</th> <th>사건</th> <th>의미</th> </tr> </thead> <tbody> <tr> <td>2013</td> <td>Omega 논문 (EuroSys 2013)</td> <td>Borg 후속 연구, 스케줄러 아키텍처 실험</td> </tr> <tr> <td>2014년 6월</td> <td>Kubernetes 오픈소스 공개</td> <td>Borg/Omega의 교훈을 담은 3세대 시스템, 처음부터 오픈소스</td> </tr> <tr> <td>2015년 4월</td> <td>Borg 논문 (EuroSys 2015)</td> <td>베일에 싸여 있던 Kubernetes의 뿌리가 공식 공개</td> </tr> <tr> <td>2015년 7월 21일</td> <td>Kubernetes v1.0 + CNCF 창립</td> <td>프로덕션 준비 선언, Google이 K8s를 CNCF에 기부</td> </tr> <tr> <td>2016</td> <td>“Borg, Omega, and Kubernetes” (ACM Queue)</td> <td>세 시스템에서 얻은 교훈을 Google이 직접 회고</td> </tr> </tbody> </table> <hr/> <h1 id="4-cncf는-무엇을-하는-조직인가">4. CNCF는 무엇을 하는 조직인가</h1> <p><strong>CNCF(Cloud Native Computing Foundation)</strong> 는 Linux Foundation 산하의 비영리 재단으로, 클라우드 네이티브 오픈소스 생태계를 키우는 것이 목적이다. 하는 일을 요약하면 다음과 같다.</p> <ul> <li><strong>프로젝트 육성</strong>: Kubernetes를 비롯한 오픈소스 프로젝트를 받아들여 거버넌스, 법적 지원(상표·라이선스), 인프라를 제공한다.</li> <li><strong>벤더 중립 거버넌스</strong>: 특정 회사가 아니라 재단과 커뮤니티가 프로젝트의 방향을 결정한다.</li> <li><strong>커뮤니티와 표준</strong>: KubeCon + CloudNativeCon 같은 컨퍼런스를 열고, 자격증(CKA 등)과 교육 프로그램을 운영한다.</li> </ul> <p>CNCF 프로젝트에는 성숙도에 따라 <strong>Sandbox → Incubating → Graduated</strong>라는 3단계 등급이 있다. 프로젝트가 얼마나 널리 채택됐는지, 거버넌스가 건강한지 등을 기준으로 승급하는데, 자세한 기준과 대표 프로젝트들은 이 시리즈의 마지막 글(09편)에서 다룬다. 지금은 “Kubernetes는 CNCF의 첫 번째이자 대표 Graduated 프로젝트”라는 것만 기억하면 된다.</p> <p>비유하면 CNCF는 <strong>국제 표준화 기구 + 인큐베이터</strong>다. 유망한 오픈소스 프로젝트를 받아 중립적인 운동장에서 키우고, 졸업(Graduated)시켜 업계 표준으로 만든다.</p> <p>Kubernetes를 배우는 입장에서 CNCF가 중요한 이유는 두 가지다.</p> <ul> <li><strong>기술 선택의 나침반이 된다.</strong> 모니터링, 로깅, 서비스 메시 같은 주변 도구를 고를 때 CNCF 프로젝트 목록과 성숙도 등급은 “커뮤니티가 검증한 선택지”를 알려주는 지도 역할을 한다.</li> <li><strong>한 회사에 종속되지 않는다는 보증이다.</strong> Kubernetes를 배워두면 특정 클라우드(AWS, GCP, Azure)나 특정 벤더에 갇히지 않는다. 같은 개념과 명령이 어디서나 통한다. 이 시리즈에 시간을 투자할 가치가 있는 이유이기도 하다.</li> </ul> <hr/> <h1 id="5-그래서-kubernetes는-무슨-일을-하는가">5. 그래서 Kubernetes는 무슨 일을 하는가</h1> <p>역사를 알았으니, Kubernetes가 실제로 해주는 일을 1장의 질문에 대응시켜 보자. Kubernetes는 흔히 <strong>오케스트라 지휘자</strong>에 비유된다. 바이올린(컨테이너) 하나하나를 직접 연주하지는 않지만, 누가 언제 무엇을 연주할지 조율해서 전체가 하나의 곡(서비스)이 되게 만든다.</p> <table> <thead> <tr> <th>기능</th> <th>하는 일</th> <th>한 줄 비유</th> </tr> </thead> <tbody> <tr> <td><strong>스케줄링 (Scheduling)</strong></td> <td>컨테이너를 자원 여유·조건에 맞는 서버(노드)에 자동 배치한다</td> <td>호텔 프런트가 빈 방 상태를 보고 손님 방을 배정한다</td> </tr> <tr> <td><strong>자동 복구 (Self-healing)</strong></td> <td>컨테이너가 죽으면 감지해서 자동으로 다시 띄운다</td> <td>쓰러진 볼링핀을 자동으로 다시 세우는 기계</td> </tr> <tr> <td><strong>스케일링 (Scaling)</strong></td> <td>부하에 따라 컨테이너 개수를 늘리고 줄인다</td> <td>점심시간에 계산대를 더 열고 한가하면 닫는 마트</td> </tr> <tr> <td><strong>서비스 디스커버리 &amp; 로드밸런싱</strong></td> <td>컨테이너 IP가 바뀌어도 고정된 이름으로 찾아가게 하고 트래픽을 분산한다</td> <td>담당자가 바뀌어도 번호는 그대로인 회사 대표 전화</td> </tr> <tr> <td><strong>자동 롤아웃 &amp; 롤백 (Rollout/Rollback)</strong></td> <td>새 버전을 무중단으로 점진 배포하고, 문제가 생기면 이전 버전으로 되돌린다</td> <td>비행기 조종을 한 명씩 교대해 비행이 끊기지 않게 하는 것</td> </tr> </tbody> </table> <p>이 기능들을 관통하는 설계 철학이 하나 있다. Borg에서 물려받은 <strong>선언적(declarative) 모델</strong>이다.</p> <ul> <li>사용자는 “nginx 컨테이너 3개가 항상 떠 있어야 한다”라는 <strong>원하는 상태(desired state)</strong> 를 선언한다.</li> <li>Kubernetes는 <strong>현재 상태(current state)</strong> 를 계속 관찰하면서, 둘이 어긋나면 스스로 맞춘다. 컨테이너가 하나 죽어 2개가 되면, 사람이 명령하지 않아도 1개를 새로 띄워 3개로 되돌린다.</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/post/image/k8s-01-history/declarative-loop-480.webp 480w,/assets/post/image/k8s-01-history/declarative-loop-800.webp 800w,/assets/post/image/k8s-01-history/declarative-loop-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/post/image/k8s-01-history/declarative-loop.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="선언적 모델의 reconcile 루프 — 원하는 상태와 현재 상태를 비교해 차이를 메우는 조치를 반복한다" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <ul> <li>이 루프는 한 번 실행되고 끝나는 것이 아니라 <strong>끝없이 반복</strong>된다. 그래서 새벽 3시에 컨테이너가 죽어도 사람 개입 없이 몇 초 안에 복구된다.</li> <li>화살표가 사용자에게 돌아가지 않는다는 점이 핵심이다. 선언 이후의 모든 판단과 조치는 Kubernetes의 몫이다.</li> <li>위 다섯 가지 기능(스케줄링·복구·스케일링·디스커버리·롤아웃)은 전부 이 루프의 변주다. “무엇이 어긋났고, 어떻게 메꾸는가”의 대상만 다를 뿐이다.</li> </ul> <p><u>"서버에 들어가서 이 명령을 실행해라"(명령형)가 아니라 "결과적으로 이 상태여야 한다"(선언형)로 운영 방식을 바꾼 것이 Kubernetes의 본질이다. 이 동작 원리(reconcile 루프)는 03편에서 아키텍처와 함께 자세히 해부한다.</u></p> <p>1장의 시나리오로 돌아가 보면 이렇게 정리된다. 어느 서버에 띄울지는 <strong>스케줄러</strong>가, 새벽 3시의 재시작은 <strong>자동 복구</strong>가, 트래픽 대응은 <strong>스케일링</strong>이, 바뀌는 IP 문제는 <strong>서비스 디스커버리</strong>가, 무중단 배포는 <strong>롤아웃/롤백</strong>이 대신한다. 사람은 원하는 상태를 선언하는 일만 하면 된다.</p> <hr/> <h1 id="6-그럼-docker와-kubernetes는-무슨-관계인가">6. 그럼 Docker와 Kubernetes는 무슨 관계인가</h1> <p>입문자가 가장 자주 헷갈리는 지점이라 짚고 넘어간다. 결론부터 말하면 둘은 <strong>경쟁 관계가 아니라 서로 다른 층을 담당하는 보완 관계</strong>다.</p> <ul> <li><strong>Docker</strong>는 컨테이너 이미지를 만들고, 한 대의 머신에서 컨테이너를 실행하는 도구다.</li> <li><strong>Kubernetes</strong>는 여러 대의 머신에 걸쳐 수많은 컨테이너를 배치·복구·확장하는 오케스트레이터다. 컨테이너 실행 자체는 각 노드의 컨테이너 런타임에게 맡긴다.</li> </ul> <p>항만 물류에 비유하면, Docker는 화물을 규격 컨테이너 박스에 담고 옮기는 <strong>지게차</strong>이고, Kubernetes는 수천 개 컨테이너를 어느 배에 어떤 순서로 실을지 결정하는 <strong>항만 관제 시스템</strong>이다. 지게차가 없으면 관제 시스템이 할 일이 없고, 관제 시스템이 없으면 지게차 수백 대가 뒤엉킨다.</p> <table> <thead> <tr> <th>질문</th> <th>답</th> </tr> </thead> <tbody> <tr> <td>Kubernetes는 Docker를 대체하나?</td> <td>아니다. 담당 계층이 다르다. 이미지 빌드에는 여전히 Docker(또는 호환 도구)를 쓴다</td> </tr> <tr> <td>Docker 없이도 Kubernetes를 쓸 수 있나?</td> <td>가능하다. Kubernetes는 CRI라는 표준 인터페이스로 containerd 등 여러 런타임을 지원한다 (03편에서 상세히)</td> </tr> <tr> <td>Docker를 모르면 이 시리즈를 못 따라오나?</td> <td><code class="language-plaintext highlighter-rouge">docker run</code>으로 컨테이너를 한 번이라도 띄워봤다면 충분하다. 나머지는 시리즈 안에서 설명한다</td> </tr> </tbody> </table> <p>실제로 다음 편에서 쓸 kind라는 도구는 “Kubernetes <strong>in</strong> Docker”라는 이름 그대로, Docker 컨테이너 안에 Kubernetes 클러스터를 통째로 띄운다. 두 기술이 얼마나 자연스럽게 맞물리는지 직접 확인하게 될 것이다.</p> <hr/> <h1 id="7-이-시리즈에서-배울-것">7. 이 시리즈에서 배울 것</h1> <p>이 시리즈는 Kubernetes를 처음 접하는 사람이 로컬 환경에서 직접 손을 움직이며 핵심 개념을 잡는 것을 목표로 한다. 순서는 SK Devocean의 로드맵 글(참고 문헌)을 바탕으로 재구성했다.</p> <table> <thead> <tr> <th>편</th> <th>주제</th> <th>이 글이 답하는 질문</th> </tr> </thead> <tbody> <tr> <td>01</td> <td>역사와 배경 (현재 글)</td> <td>Kubernetes는 왜, 어떻게 태어났나?</td> </tr> <tr> <td>02</td> <td>kind와 kubectl</td> <td>내 노트북에 클러스터를 어떻게 만드나?</td> </tr> <tr> <td>03</td> <td>아키텍처</td> <td>클러스터 내부는 어떤 부품으로 돌아가나?</td> </tr> <tr> <td>04</td> <td>Pod</td> <td>배포의 최소 단위는 무엇이고 어떻게 스케줄링되나?</td> </tr> <tr> <td>05</td> <td>워크로드</td> <td>Pod를 여러 개, 안전하게 운영하려면?</td> </tr> <tr> <td>06</td> <td>네트워킹</td> <td>Pod끼리, 그리고 외부와 어떻게 통신하나?</td> </tr> <tr> <td>07</td> <td>스토리지와 설정</td> <td>데이터와 설정을 컨테이너 밖에서 어떻게 관리하나?</td> </tr> <tr> <td>08</td> <td>권한 관리</td> <td>누가 무엇을 할 수 있는지 어떻게 통제하나?</td> </tr> <tr> <td>09</td> <td>Operator와 CNCF 생태계</td> <td>Kubernetes를 어떻게 확장하고, 그다음엔 뭘 공부하나?</td> </tr> </tbody> </table> <p>02편에서 환경을 만들고, 03편에서 구조를 이해한 뒤, 04~08편에서 핵심 리소스를 하나씩 실습하고, 09편에서 생태계로 시야를 넓히는 흐름이다.</p> <hr/> <h1 id="8-마무리">8. 마무리</h1> <p>핵심을 한 문장으로 요약하면 이렇다. <strong>Kubernetes는 Google이 Borg와 Omega로 10년 넘게 축적한 대규모 컨테이너 운영 경험을 오픈소스로 풀어낸 3세대 시스템이고, CNCF라는 중립 재단 아래에서 컨테이너 오케스트레이션의 사실상 표준이 됐다.</strong></p> <table> <thead> <tr> <th>질문</th> <th>답</th> </tr> </thead> <tbody> <tr> <td>왜 필요한가</td> <td>수백 대 서버, 수천 개 컨테이너의 수동 운영은 불가능하다</td> </tr> <tr> <td>어디서 왔나</td> <td>Google Borg(내부 운영) → Omega(아키텍처 연구) → Kubernetes(오픈소스)</td> </tr> <tr> <td>언제 태어났나</td> <td>2014년 6월 공개, 2015년 7월 21일 v1.0 + CNCF 창립</td> </tr> <tr> <td>누가 관리하나</td> <td>Linux Foundation 산하 벤더 중립 재단 CNCF</td> </tr> <tr> <td>핵심 철학은</td> <td>선언적 모델 — 원하는 상태를 선언하면 시스템이 현재 상태를 맞춘다</td> </tr> </tbody> </table> <p>역사는 여기까지다. 다음 글부터는 직접 손을 움직인다.</p> <blockquote> <p>다음 글 <a href="/blog/2026/k8s-02-local-setup/">02: 내 노트북에 클러스터 만들기 — kind와 kubectl</a>에서는 kind로 내 노트북에 진짜 Kubernetes 클러스터를 띄우고 kubectl로 조작해 본다.</p> </blockquote> <hr/> <h1 id="참고-문헌">참고 문헌</h1> <ul> <li><a href="https://devocean.sk.com/blog/techBoardDetail.do?ID=165905&amp;boardType=techBlog">Kubernetes(쿠버네티스)를 처음 공부하려면 무엇을 공부해야 할까?</a> (seungkyua, SK Devocean, 2024) — 시리즈 로드맵 출처</li> <li><a href="https://research.google/pubs/large-scale-cluster-management-at-google-with-borg/">Large-scale cluster management at Google with Borg</a> (Verma et al., Google, EuroSys 2015)</li> <li><a href="https://research.google/pubs/omega-flexible-scalable-schedulers-for-large-compute-clusters/">Omega: flexible, scalable schedulers for large compute clusters</a> (Schwarzkopf et al., Google, EuroSys 2013)</li> <li><a href="https://queue.acm.org/detail.cfm?id=2898444">Borg, Omega, and Kubernetes</a> (Burns et al., Google, ACM Queue 2016)</li> <li><a href="https://kubernetes.io/docs/concepts/overview/">Kubernetes Overview — kubernetes.io</a></li> <li><a href="https://www.cncf.io/announcements/2015/06/21/new-cloud-native-computing-foundation-to-drive-alignment-among-container-technologies/">New Cloud Native Computing Foundation to drive alignment among container technologies</a> (Linux Foundation, 2015)</li> <li><a href="https://en.wikipedia.org/wiki/Kubernetes">Kubernetes — Wikipedia</a></li> </ul>]]></content><author><name></name></author><category term="infra"/><category term="kubernetes"/><category term="infra"/><category term="docker"/><category term="cncf"/><category term="borg"/><summary type="html"><![CDATA[docker run으로 충분하지 않은 순간은 언제 오는가 — Google Borg에서 시작해 CNCF까지, Kubernetes가 태어난 이유와 역사]]></summary></entry><entry><title type="html">Tamper-Resistant Safeguards (TAR) — Fine-tuning 자체에 견디는 safety</title><link href="https://www.wonbeomjang.kr/blog/2026/tamirisa-tamper-resistant-safeguards/" rel="alternate" type="text/html" title="Tamper-Resistant Safeguards (TAR) — Fine-tuning 자체에 견디는 safety"/><published>2026-05-30T00:00:00+00:00</published><updated>2026-05-30T00:00:00+00:00</updated><id>https://www.wonbeomjang.kr/blog/2026/tamirisa-tamper-resistant-safeguards</id><content type="html" xml:base="https://www.wonbeomjang.kr/blog/2026/tamirisa-tamper-resistant-safeguards/"><![CDATA[<blockquote> <p><a href="https://arxiv.org/abs/2408.00761">Tamper-Resistant Safeguards for Open-Weight LLMs</a> (Tamirisa et al., CAIS / UIUC / UC Berkeley / Lapis Labs / Gray Swan, ICLR 2025)</p> </blockquote> <h1 id="introduction">Introduction</h1> <h2 id="이-시리즈의-마지막--가장-어려운-방어-문제">이 시리즈의 마지막 — 가장 어려운 방어 문제</h2> <p><a href="/blog/2026/zou-circuit-breakers/">Circuit Breakers (#11)</a>는 prompt-level 공격(GCG, AutoDAN, prefilling)을 잘 막았지만 <strong>fine-tuning 공격에는 취약</strong>했다. 사용자가 LoRA로 수백 step 학습하면 representation 방어가 덮인다.</p> <p>TAR는 이 마지막 구멍을 노린다.</p> <blockquote> <p><strong>fine-tuning 공격 자체에 견디도록</strong> safety를 학습한다. <strong>수천 step의 adversarial fine-tuning을 가해도</strong> safety가 남아 있도록 (논문 abstract 핵심 주장).</p> </blockquote> <p>핵심: <strong>장기 fine-tuning attack에도 부분적으로 견디는 최초의 방법론.</strong> 공격을 완전히 막진 못해도 비용을 크게 끌어올린다. (구체적 step 수 ↔ ASR 곡선은 <a href="https://arxiv.org/abs/2408.00761">논문 본문</a> 참조)</p> <h2 id="비유--백신과-항체">비유 — 백신과 항체</h2> <p>한 번 백신을 맞으면 그 병원체에 대한 항체가 생긴다. 새로 노출되어도 면역 시스템이 빠르게 반응. TAR는 <strong>fine-tuning attack 시도 자체에 면역</strong>을 학습시킨다.</p> <h1 id="background">Background</h1> <h2 id="왜-fine-tuning-attack이-가장-어려운-방어인가">왜 fine-tuning attack이 가장 어려운 방어인가</h2> <p>이 시리즈에서 본 공격은 셋으로 분류된다.</p> <table> <thead> <tr> <th>표면</th> <th>방어 가능성</th> </tr> </thead> <tbody> <tr> <td>Prompt (GCG, PAIR 등)</td> <td>RLHF 후처리·input filter로 부분 방어</td> </tr> <tr> <td>Representation (<a href="/blog/2026/refusal-direction-abliteration/">Abliteration</a>)</td> <td><a href="/blog/2026/zou-circuit-breakers/">Circuit Breakers</a>로 부분 방어</td> </tr> <tr> <td><strong>Fine-tuning</strong></td> <td><strong>모델 가중치를 합법적으로 학습 → 기존 방어 모두 우회</strong></td> </tr> </tbody> </table> <p>Fine-tuning은 본질적으로 “허용된 행동”이다. 사용자가 자기 GPU에서 자기 데이터로 학습하는 걸 막을 수 없다. 따라서 방어는 <strong>모델 학습 단계 자체에서 미래의 fine-tuning에 견디도록</strong> 설계되어야 한다.</p> <h2 id="meta-learning-발상">Meta-Learning 발상</h2> <p>TAR의 핵심 도구는 meta-learning이다.</p> <blockquote> <p>학습 중에 <strong>“적이 fine-tuning할 것”을 시뮬레이션</strong>하고, 시뮬레이션 후에도 safety가 유지되도록 원본 가중치를 조정한다.</p> </blockquote> <p>즉 <strong>“공격 후의 모델이 안전하도록” 원본 가중치를 학습</strong>한다. MAML(Model-Agnostic Meta-Learning) 류의 발상을 alignment에 적용한 것.</p> <h1 id="method">Method</h1> <h2 id="학습-알고리즘">학습 알고리즘</h2> <p>각 학습 step마다:</p> <ol> <li><strong>Inner loop (시뮬레이션 공격)</strong>: 가짜 fine-tuning attack을 N step 실행 (예: 유해 데이터로 SGD 10 step)</li> <li><strong>시뮬레이션 후 모델 평가</strong>: 공격 후 safety 손실 측정</li> <li><strong>Outer loop (원본 업데이트)</strong>: “공격 후에도 safety가 유지되도록” 원본 가중치를 업데이트</li> </ol> <p>수식 형태:</p> \[\theta^* = \arg\min_\theta \mathbb{E}_{\mathcal{D}_{\text{attack}}} \bigl[ \mathcal{L}_{\text{safety}}\bigl( \text{FT}_N(\theta, \mathcal{D}_{\text{attack}}) \bigr) \bigr]\] <p>기호 풀이:</p> <ul> <li>\(\theta\): 원본 모델 가중치</li> <li>\(\text{FT}_N(\theta, \mathcal{D})\): \(\theta\)를 \(\mathcal{D}\)로 \(N\) step fine-tuning한 결과</li> <li>\(\mathcal{L}_{\text{safety}}\): 공격 후 모델의 safety 손실 (높을수록 unsafe)</li> <li>\(\mathcal{D}_{\text{attack}}\): 가상 공격 데이터 분포</li> </ul> <p>해석: <strong>“내가 어떻게 fine-tuning되든 safety를 유지할 수 있는 초기값”</strong>을 찾는다.</p> <h2 id="두-단계-학습">두 단계 학습</h2> <h3 id="stage-1-일반-safety-alignment">Stage 1: 일반 safety alignment</h3> <p>표준 RLHF로 거부 행동 학습.</p> <h3 id="stage-2-tar-meta-training">Stage 2: TAR meta-training</h3> <ul> <li>매 step: inner loop 5–20 fine-tuning step 시뮬레이션</li> <li>Outer loop: safety가 inner loop 후에도 유지되도록 원본 update</li> <li>이 시뮬레이션 + outer를 수천 step 반복</li> </ul> <p>학습 비용: 표준 RLHF의 <strong>5–10배</strong>. inner loop 시뮬레이션 때문.</p> <h2 id="시뮬레이션-공격-데이터">시뮬레이션 공격 데이터</h2> <p>inner loop에서 사용할 가상 공격 데이터:</p> <ul> <li>유해 QA (Shadow Alignment 스타일)</li> <li>AOA persona 데이터 (<a href="/blog/2026/qi-fine-tuning-compromises-safety/">Qi Tier 2</a>)</li> <li>일반 Alpaca 데이터 (Benign FT 시뮬레이션)</li> <li>Adversarial LoRA 업데이트</li> </ul> <p>다양한 공격 분포에 견디려면 시뮬레이션 분포가 다양해야 한다.</p> <h1 id="results">Results</h1> <h2 id="fine-tuning-attack-내성">Fine-tuning attack 내성</h2> <blockquote> <p><em>주의: 아래는 논문이 주장하는 정성적 패턴이다. 구체적 공격별 ASR·step 곡선은 <a href="https://arxiv.org/abs/2408.00761">논문 본문</a> Figure를 참조하라.</em></p> </blockquote> <p>LLaMA-2/3-class 모델에 TAR 적용 후 핵심 관찰:</p> <ul> <li><strong>공격 표준 시나리오 (수십~수백 step의 adversarial FT)</strong> 에 대해 기존 RLHF 모델 대비 ASR이 크게 낮음</li> <li><strong>수천 step 규모의 long-horizon adversarial fine-tuning</strong>에도 safety가 부분적으로 유지되는 것이 abstract의 핵심 주장</li> <li>비교: 정상 RLHF / Circuit Breakers는 수십~수백 step 안에 무너지는 경우가 많은 반면, TAR는 그 시간선을 한 자리수 이상 연장</li> </ul> <h2 id="공격-비용-증가">공격 비용 증가</h2> <p>TAR은 공격을 완전히 막진 못하지만 <strong>공격이 성공하기까지 필요한 학습량(step·컴퓨트)을 크게 끌어올린다</strong>. 결정적 공격자(충분한 컴퓨트 보유)에게는 결국 깨지지만, casual misuse·소규모 공격자에게는 효과적인 장벽이 된다.</p> <h1 id="limitations">Limitations</h1> <h2 id="1-완전한-방어가-아님">1. 완전한 방어가 아님</h2> <p>충분한 컴퓨트만 있으면 여전히 깨진다. <strong>공격 비용을 올릴 뿐</strong>, 0으로 만들지는 못한다. 결정적 공격자(국가 행위자 등)에게는 부족.</p> <h2 id="2-학습-비용">2. 학습 비용</h2> <p>meta-learning inner loop가 매 step마다 시뮬레이션 fine-tuning을 돌리므로 표준 RLHF보다 큰 폭으로 비싸다 (정확한 배수는 본문 참조). 모든 모델에 적용하기에는 부담.</p> <h2 id="3-시뮬레이션-공격-다양성에-의존">3. 시뮬레이션 공격 다양성에 의존</h2> <p>inner loop의 공격 분포에 없는 새 공격 종류에는 일반화가 약할 수 있음. <strong>시뮬레이션하지 못한 공격에는 취약</strong>.</p> <h2 id="4-prompt-level-공격엔-차별점-없음">4. Prompt-level 공격엔 차별점 없음</h2> <p>TAR는 fine-tuning attack 내성에 특화. prompt jailbreak 방어는 기존 RLHF나 Circuit Breakers 수준. 두 가지를 결합하는 게 권장된다.</p> <h2 id="5-후속-비판--우회-방법-발견">5. 후속 비판 — 우회 방법 발견</h2> <p>이 시리즈 작성 시점 기준, TAR 발표 이후 일부 후속 연구가 TAR 가중치에 대한 더 정교한 공격(예: 더 긴 학습 + 다양한 데이터)으로 안전성을 깨는 것을 보였다. 군비 경쟁이 진행 중.</p> <h1 id="implications">Implications</h1> <h2 id="tamper-resistance--새-평가-기준">“Tamper-Resistance” — 새 평가 기준</h2> <p>TAR이 정립한 핵심 개념은 <strong>safety가 fine-tuning에 견디는 능력</strong>을 모델 평가 항목으로 추가해야 한다는 것.</p> <p>기존 safety 평가:</p> <ul> <li>AdvBench refusal rate</li> <li>prompt jailbreak benchmark (HarmBench 등)</li> </ul> <p>추가되어야 할 평가:</p> <ul> <li><strong>PEFT-attack resistance</strong>: 100 step LoRA 후 ASR</li> <li><strong>Full SFT resistance</strong>: 1000 step SFT 후 ASR</li> <li><strong>Backdoor resistance</strong>: RLHF poisoning 후 universal trigger 효과</li> </ul> <p>이 흐름은 <a href="https://arxiv.org/abs/2409.18169">Huang et al. 방어 서베이 (arXiv:2409.18169)</a>에 정리되어 있다.</p> <h2 id="open-weight-정책-함의">Open-weight 정책 함의</h2> <p>TAR가 완벽 방어가 아니지만, <strong>open-weight 모델 공개의 위험을 줄이는 방향</strong>을 제시한다.</p> <ul> <li>Meta·Mistral·DeepSeek 같은 open-weight 공개자가 TAR 같은 기법 적용 가능</li> <li>공격 비용이 높아지면 casual misuse는 차단됨</li> <li>결정적 공격자만 막을 수 없는 상태로 수렴</li> </ul> <p>이건 “open-weight = 완전 무방어” 가정을 부분적으로 갱신한다.</p> <h2 id="시리즈-전체의-마무리">시리즈 전체의 마무리</h2> <p>이 12편 시리즈가 보인 큰 그림:</p> <ol> <li><strong>공격 (#1–#9)</strong>: shallow safety alignment의 9가지 변주 공격 — abliteration, fine-tuning, poisoning, covert FT, emergent misalignment</li> <li><strong>진단 (#10)</strong>: 모든 공격이 통하는 이유 = RLHF가 첫 5 토큰만 reshape</li> <li><strong>방어 (#11–#12)</strong>: representation-level (Circuit Breakers) + tamper-resistant (TAR)</li> </ol> <p>방어 두 편이 합쳐도 <strong>완전한 해결은 아니다</strong>. 군비 경쟁이 계속된다. 다만 비용 비대칭(공격 $0.20 vs 학습 수억 $)이 조금이라도 좁혀진다.</p> <h1 id="conclusion">Conclusion</h1> <blockquote> <p><strong>TAR는 fine-tuning attack 자체에 견디는 safety를 meta-learning으로 학습한다.</strong> 공격을 완전히 막진 못해도 비용을 수십 배 올린다. open-weight 시대의 마지막 방어선이고, “tamper-resistance”라는 새 평가 축을 도입했다.</p> </blockquote> <p>이 시리즈가 본 약점은 <strong>여전히 열려 있다</strong>. open-weight 모델의 가중치가 공격자 손에 있는 한, 결정적 공격자는 막을 수 없다. 그러나 비용을 올리는 모든 방어가 의미 있다. casual misuse를 차단하고, 산업·정책의 시간 여유를 만든다.</p> <h2 id="시리즈-전체-회고">시리즈 전체 회고</h2> <blockquote> <p><strong>White-Box Safety 시리즈 12편이 끝났다.</strong> abliteration의 단일 방향 발견에서 시작해, fine-tuning attack 8편을 거쳐, shallow safety 진단과 두 가지 방어로 닫혔다. 이 시리즈가 보여준 한 가지 메시지가 있다면:</p> <p><strong>open-weight 모델의 safety는 본질적으로 fragile하고, 그 fragility는 RLHF의 구조적 얕음(shallowness)에 기인한다. deep alignment와 tamper-resistance가 만나는 곳에서 미래의 답이 있다.</strong></p> </blockquote> <h1 id="참고-문헌">참고 문헌</h1> <ul> <li><a href="https://arxiv.org/abs/2408.00761">Tamirisa et al., 2025 — Tamper-Resistant Safeguards for Open-Weight LLMs (ICLR 2025)</a></li> <li><a href="https://arxiv.org/abs/1703.03400">MAML — Finn et al., ICML 2017</a> — meta-learning 원형</li> <li><a href="https://arxiv.org/abs/2409.18169">Huang et al., 2024 — Harmful Fine-tuning Survey</a> — 방어 taxonomy</li> <li><a href="/blog/2026/zou-circuit-breakers/">Circuit Breakers (시리즈 #11)</a> — 짝이 되는 방어</li> <li><a href="/blog/2026/qi-shallow-safety-alignment/">Qi et al. — Shallow Safety Alignment (시리즈 #10)</a> — TAR이 대응하는 문제 진단</li> <li><a href="/blog/2026/refusal-direction-abliteration/">이 시리즈 전체 #1–#12</a></li> </ul>]]></content><author><name></name></author><category term="paper"/><category term="llm"/><category term="red-teaming"/><category term="safety"/><category term="paper"/><category term="defense"/><category term="tamper-resistance"/><category term="meta-learning"/><category term="fine-tuning-defense"/><summary type="html"><![CDATA[White-Box Safety 시리즈 #12 (마지막) — adversarial fine-tuning을 수천 step 가해도 safety가 견디도록 학습한 tamper-resistant safeguards, open-weight 시대의 마지막 방어선 (Tamirisa et al., CAIS / UIUC / UC Berkeley 외, ICLR 2025)]]></summary></entry></feed>